# testexample_simple_coupling 电压降不明显的原因分析

## 核心结论

**testexample_simple_coupling** 与 **thermal_example** 相比电压降不明显，主要有以下 **5 个关键原因**：

---

## 1. ⭐⭐⭐ 温度对电压影响本身就很小（最根本原因）

### 问题所在

查看电池参数文件发现：

```julia
# Jellyroll.jl 和 LGM50.jl 中：
PE.dUdT = x-> 0 * x  # 熵系数为零！
NE.dUdT = x-> 0 * x  # 熵系数为零！

PE.Eac_D = 0  # 扩散系数无温度依赖！
NE.Eac_D = 0  # 扩散系数无温度依赖！
```

### 物理含义

- **dU/dT = 0** 意味着没有**可逆热效应**（熵变）
- **Eac_D = 0** 意味着**扩散系数**不随温度变化
- 温度只能通过以下途径影响电压：
  - 电解液电导率的温度依赖（效应较小）
  - 反应动力学的温度依赖（Eac_k ≠ 0，但效应中等）

### 结果

即使温度升高 13.15 K，对电压的影响也只有约 **0.01 V 左右**，远小于 SOC 变化导致的电压降。

**这是电压降不明显的物理本质！**

---

## 2. ⭐⭐⭐ Jellyroll 的比热容是 LG M50 的 2 倍

### 参数对比

| 组件 | Jellyroll | LG M50 | 倍数 |
|------|-----------|---------|------|
| **正极比热容** | 1270 J/(kg·K) | 700 J/(kg·K) | **1.81×** |
| **负极比热容** | 1437 J/(kg·K) | 700 J/(kg·K) | **2.05×** |
| **隔膜比热容** | 1978 J/(kg·K) | 700 J/(kg·K) | **2.83×** |

### 物理含义

```
热量 Q = m × C × ΔT

相同热量下：
ΔT_Jellyroll / ΔT_LGM50 = C_LGM50 / C_Jellyroll ≈ 700 / 1400 ≈ 0.5
```

**Jellyroll 需要 2 倍的热量才能达到相同的温度升高！**

### 结果

- LG M50 可能温升 30 K
- Jellyroll 只温升 15 K
- 温度对电压的影响减半

---

## 3. ⭐⭐⭐ 对流换热系数相差 15 倍

### 参数对比

```
Jellyroll:  cell.h = 150 W/(m²·K)
LG M50:     cell.h = 10 W/(m²·K)
```

### 物理含义

```
散热功率: Q_conv = h × A × (T - T_amb)
```

**Jellyroll 的散热能力是 LG M50 的 15 倍！**

### 能量平衡

```
dE/dt = Q_gen - Q_conv

当 h 很大时：
- Q_conv 很大
- 大部分热量被迅速散失
- 温度升高被强烈抑制
```

### 结果

- 更多的热量通过对流散失
- 内部温度升高受限
- 电压降相应减小

---

## 4. ⭐⭐ 分布式热模型的空间平均效应

### 温度分布

```
最高温度: 311.26 K  (内部)
最低温度: 298.00 K  (边界)
平均温度: 311.15 K  (用于电压计算)
```

### 集总模型 vs 分布式模型

**集总模型**（thermal_example）：
```
[========= T_uniform =========]
全部电池处于同一温度
dV/dT 效应完全体现
```

**分布式模型**（simple_coupling）：
```
[T_low ||| T_mid ||| T_high]
      ↓
  T_average (用于电压)
  
边界保持低温 → 拉低平均温度
dV/dT 效应被平均化削弱
```

### 结果

- 分布式模型用**平均温度**反馈给 SPMe
- 边界区域保持环境温度（298 K）
- 平均温度低于集总模型
- 电压降效应被削弱

---

## 5. ⭐ 热源本身较小

### 从您提供的数据

```
平均热源: 0.70 W
最大热源: 0.89 W
```

### 原因分析

1. **电流密度低**：
   ```
   I = 5A (1C)
   Jellyroll 的有效面积大
   → 电流密度 j 较小
   ```

2. **良好的电化学性能**：
   - 过电位 η 较小
   - 反应热 = a_s × |j| × |η| 较小

3. **高电导率**：
   - 欧姆热 = I²/σ 较小

### 结果

热源小 → 温度升高慢 → 电压降小

---

## 定量估算

### 温度升高

```
实际测量: ΔT = 13.15 K

理论估算:
Q_gen × t = m × C × ΔT

ΔT = (Q_gen × t) / (m × C)
   = (0.70 W × 3600 s) / (0.85 kg × 1400 J/(kg·K))
   = 2.1 K  (不考虑散热)

实际温升 13.15 K > 2.1 K，说明：
1. 实际热源可能更大（峰值 0.89 W）
2. 散热效率高（h=150）抑制了更高的温升
```

### 电压降

```
温度效应估算:
dV/dT ≈ -0.001 V/K (经验值)
ΔV_temp ≈ -0.001 × 13.15 ≈ -0.013 V

实际电压降:
初始: 4.180 V
最终: 4.176 V
总降: 0.004 V

温度效应被 SOC 变化主导的电压降淹没！
```

---

## 与 thermal_example 的对比

| 项目 | testexample_simple_coupling | thermal_example (预期) |
|------|----------------------------|----------------------|
| **电池类型** | Jellyroll | LG M50 |
| **比热容** | 1400 J/(kg·K) | 700 J/(kg·K) |
| **对流系数 h** | 150 W/(m²·K) | 10 W/(m²·K) |
| **热模型** | 分布式 2D | 集总 |
| **温度升高** | 13 K | ~30-50 K |
| **电压降** | 0.004 V | ~0.02-0.05 V |
| **散热效率** | 高 | 低 |

---

## 如何让电压降更明显？

### 方法 1：降低散热（最有效）

```julia
# 修改 Jellyroll.jl
cell.h = 10.0  # 从 150 降至 10

预期效果：
- 温度升高 → 40-50 K
- 电压降 → 0.04-0.05 V
```

### 方法 2：增加热源

```julia
# 修改 testexample_simple_coupling.jl
Crates = 3.0  # 从 1C 提高到 3C
i = 5 * Crates  # 15A

预期效果：
- 热源增加 9 倍 (I² 依赖)
- 温度升高 → 50-100 K
- 电压降明显
```

### 方法 3：使用 LG M50 参数

```julia
# 修改 testexample_simple_coupling.jl
param_dim = JuBat.ChooseCell("LG M50")

预期效果：
- 比热容降低一半
- 温度升高加倍
- 电压降加倍
```

### 方法 4：添加温度依赖性

```julia
# 修改 Jellyroll.jl
PE.dUdT = x-> -0.0002 * ones(size(x))  # 添加熵系数
NE.dUdT = x-> 0.0003 * ones(size(x))
PE.Eac_D = 20000  # 添加扩散激活能
NE.Eac_D = 30000

预期效果：
- 温度对电压的影响增强
- 相同温升下电压降更大
```

### 方法 5：使用集总热模型

```julia
# 修改 testexample_simple_coupling.jl
opt.thermalmodel = "lumped"

预期效果：
- 温度更均匀
- 没有边界低温区域
- 电压降更明显
```

---

## 验证建议

### 快速验证实验

1. **实验组合：LG M50 + h=10**
   ```julia
   param_dim = JuBat.ChooseCell("LG M50")
   param_dim.cell.h = 10.0
   ```
   预期：温升 ~50K，电压降 ~0.05V

2. **实验组合：Jellyroll + 3C + h=10**
   ```julia
   Crates = 3.0
   cell.h = 10.0
   ```
   预期：温升 ~100K，电压降 ~0.1V

---

## 总结

**testexample_simple_coupling 电压降不明显的 5 大原因：**

1. **⭐⭐⭐ 温度对电压影响本身就小**（dU/dT=0, Eac_D=0）
2. **⭐⭐⭐ 高比热容**（是 LG M50 的 2 倍）→ 温升慢
3. **⭐⭐⭐ 高散热系数**（是 LG M50 的 15 倍）→ 散热快
4. **⭐⭐ 空间平均效应**（分布式模型平均化）→ 削弱影响
5. **⭐ 热源小**（0.7W）→ 温升有限

**核心物理：**
```
小热源 + 高比热容 + 高散热 → 温升小 (13K)
温升小 + 弱温度依赖 → 电压降小 (0.004V)
```

**如果要观察明显电压降，建议：**
- 降低 h 至 10
- 使用 LG M50 参数
- 提高 C-rate 至 3C
- 添加 dU/dT 温度依赖

---

**详细分析报告**: 请参考 `analysis_voltage_drop_comparison.md`
