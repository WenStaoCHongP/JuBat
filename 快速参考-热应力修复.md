# 热应力修复 - 快速参考

## 问题
```
热应力 = 0  ← 所有时间步用相同温度 → ΔT = 0
```

## 解决方案
```
保存温度历史 → 每步用对应温度 → ΔT ≠ 0 → 热应力 ≠ 0
```

## 修改的文件

| 文件 | 行号 | 修改 |
|------|------|------|
| `src/Variables.jl` | 122 | ➕ 添加温度历史初始化 |
| `src/Solve.jl` | 207-210 | ➕ 每步保存温度到历史 |
| `example/testexample.jl` | 206-230 | 🔄 使用温度历史计算应力 |
| `example/spme_thermal2d_example.jl` | 215 | 🔧 修正函数名 |
| `tools/plot_thermal_stress.jl` | 32 | 🔧 修正函数名 |

## 核心代码

### 1. 保存温度历史 (Solve.jl)
```julia
if haskey(variables_hist, "thermal2D temperature")
    Tref = case.param_dim.scale.T_ref
    variables["thermal2D temperature"] = T_nodes .* Tref
end
```

### 2. 使用温度历史 (testexample.jl)
```julia
# 获取完整历史
T_nodes_hist_K = result["thermal2D temperature [K]"]  # (nT × num_steps)

# 循环中使用对应时刻的温度
for step in 1:num_steps
    T_nodes_K = T_nodes_hist_K[:, step]  # ← 关键：每步不同
    variables_step["T_nodes"] = T_nodes_K ./ T_ref
    
    # 计算应力
    variables_step = JuBat.thermal_diffusion_stress_2D(case, variables_step)
end
```

## 数据流
```
┌────────────────┐
│ Variables.jl   │  初始化: thermal2D temperature = zeros(nT, num_steps)
└────────┬───────┘
         │
         ↓
┌────────────────┐
│ Solve.jl       │  每步: thermal2D temperature = T_nodes * Tref
└────────┬───────┘      自动保存到历史矩阵
         │
         ↓
┌────────────────┐
│ result 字典    │  thermal2D temperature [K] = (nT × num_steps) 矩阵
└────────┬───────┘
         │
         ↓
┌────────────────┐
│ testexample.jl │  每步读取: T_nodes_hist_K[:, step]
└────────┬───────┘
         │
         ↓
┌────────────────┐
│ mechanical.jl  │  计算: dT = T(t) - T₀  → σ_thermal ≠ 0 ✓
└────────────────┘
```

## 验证
```bash
# 运行测试
cd /workspace/example
julia testexample.jl

# 查看输出（应该非零）
# 热应力峰值范围: [X.XX, Y.YY] MPa
```

## 检查点
- [ ] `Variables.jl` 第122行有 `thermal2D temperature` 初始化
- [ ] `Solve.jl` 第207-210行有温度保存代码
- [ ] `testexample.jl` 使用 `thermal2D temperature [K]`
- [ ] 应力计算结果非零

## 技术细节

### 温度单位
- `T_nodes`: 无量纲 (T/T_ref)
- `thermal2D temperature`: 有量纲 (K)
- 转换: `T_K = T_nodes * Tref`

### 热应力公式
```
ΔT(x,y,t) = T(x,y,t) - T₀
ε_thermal = α_eff · ΔT
σ_thermal = E_eff / (1-ν_eff) · ε_thermal
```

### 内存开销
```
温度历史大小 = nT × num_steps × 8 bytes

示例：
  10,000 节点 × 1,000 步 = 76 MB
  50,000 节点 × 500 步  = 190 MB
```

## 常见问题

**Q: 为什么之前热应力是0？**  
A: 所有时间步用同一个温度 → ΔT = 0 → σ_thermal = 0

**Q: 修改后会增加多少内存？**  
A: nT × num_steps × 8 bytes，通常几十到几百MB

**Q: 旧版结果文件还能用吗？**  
A: 可以，代码有后备方案，会用最终温度（但热应力仍为0）

**Q: 如何减少内存使用？**  
A: 减少 num_steps 或在 `opt.outputType` 中使用稀疏采样

## 相关文档
- `热应力修复说明.md` - 简要说明
- `CHANGELOG_热应力修复.md` - 详细更改日志
- `热应力修复总结.txt` - ASCII格式总结

---
**修复日期**: 2025-12-23  
**状态**: ✅ 完成
