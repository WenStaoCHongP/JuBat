# Jellyroll 边界识别使用指南

## 快速开始

### 情况1：您想识别物理边界（推荐）⭐

**需求**：识别最内层（r ≈ Rin）和最外层（r ≈ Rout）的节点

**使用方法**：

```julia
# 在 case.opt 中设置
case.opt.boundary_method = :radial  # 使用基于半径的识别
case.opt.boundary_radial_tol = 1e-3  # 容差（可选，默认1e-3）

# 或者直接调用 API
for i in 1:mesh.nlen
    if edge_boundary(:radial, mesh, i, param_dim; which=:outer, tol=1e-3)
        # 识别到外边界节点（r ≈ Rout）
        # 施加对流换热等边界条件
    end
end
```

**优势**：
- ✅ 物理意义明确：r ≈ Rout 就是与外界接触的边界
- ✅ 适用任何网格类型
- ✅ 自动应用于 `ThermalDistributed2D_BC`（对流换热）

---

### 情况2：您想识别螺旋线上的节点

**需求**：识别"内螺旋线"和"外螺旋线"上的节点（collector_seed_mesh 特有）

**使用方法**：

```julia
# 在 case.opt 中设置（默认值）
case.opt.boundary_method = :spiral  # 使用基于螺旋线的识别

# 或者直接调用 API
for i in 1:mesh.nlen
    if edge_boundary(:node_on, mesh, i, param_dim; which=:outer)
        # 识别到外螺旋线节点
    end
end
```

**适用场景**：
- collector_seed_mesh 网格（内圈161个 + 外圈161个节点）
- 需要区分网格结构中的内外圈

---

## 详细说明

### Collector_seed_mesh 的结构限制

**您的原始需求**：
> 内边界为内螺旋线起点外推一个周期 [0, 2π]  
> 外边界为外螺旋线终点内推一个周期 [2π(N-1), 2π*N]

**无法实现的原因**：

```
collector_seed_mesh 网格结构：
  内圈节点（1-161）：θ ∈ [0, 2π]，r = a + b*θ
  外圈节点（162-322）：θ ∈ [0, 2π]，r = a + b*θ + t_repeat
                       ↑↑↑ 相同的 θ 范围！

实际覆盖：只覆盖第0圈（或某一圈），不覆盖第N圈（最外层）
```

**结论**：网格本身不覆盖"第1圈"和"第N圈"，无法基于 θ 范围区分。

---

## 两种边界识别方法对比

### 方法1：基于螺旋线 θ（`:spiral` / `:node_on`）

**判断依据**：
- 内边界：θ_cum_in ∈ [0, 2π]
- 外边界：mod(θ_cum_out, 2π) ∈ [0, 2π]（任意一圈的外螺旋）

**识别结果**（collector_seed_mesh, nθ=160）：
- 内螺旋线：161 个节点
- 外螺旋线：161 个节点

**半径分布**：
- 内螺旋线：r ∈ [Rin, Rin + 2πb]（第0圈内螺旋）
- 外螺旋线：r ∈ [Rin + t_repeat, Rin + t_repeat + 2πb]（第0圈外螺旋）

**适用场景**：
- 需要识别网格拓扑结构（内圈 vs 外圈）
- collector_seed_mesh 专用

**代码示例**：
```julia
# 施加边界条件（方法1）
case.opt.boundary_method = :spiral  # 默认值

# 手动识别
is_outer = [edge_boundary(:node_on, mesh, i, param_dim; which=:outer) 
            for i in 1:mesh.nlen]
```

---

### 方法2：基于半径 r（`:radial`）⭐ 推荐

**判断依据**：
- 内边界：|r - Rin| < tol
- 外边界：|r - Rout| < tol

**识别结果**（取决于网格和容差）：
- 内边界：r ≈ Rin 的节点（可能0个，取决于网格是否覆盖 Rin）
- 外边界：r ≈ Rout 的节点（对于 collector_seed_mesh，外圈的部分节点）

**物理意义**：
- 内边界 = 最靠近中心的节点（与内芯接触）
- 外边界 = 最靠近外壳的节点（与环境接触）

**适用场景**：
- **对流换热**：应施加在 r ≈ Rout（与外界接触）✅
- **内边界绝热**：应施加在 r ≈ Rin
- 任何网格类型

**代码示例**：
```julia
# 施加边界条件（方法2，推荐）
case.opt.boundary_method = :radial
case.opt.boundary_radial_tol = 1e-3  # 可选，默认1e-3

# 手动识别
is_outer = [edge_boundary(:radial, mesh, i, param_dim; which=:outer, tol=1e-3) 
            for i in 1:mesh.nlen]

# 示例：施加外边界对流换热
for i in 1:mesh.nlen
    if edge_boundary(:radial, mesh, i, param_dim; which=:outer, tol=1e-3)
        # r ≈ Rout 的节点，施加 h*(T - T_amb)
    end
end
```

---

## 容差选择指南

### 方法1（基于螺旋线）的容差

**参数**：`tol`（长度容差）

**计算**：
```julia
eps_theta = tol / b  # 转换为角度容差
```

**推荐值**：
- 默认：自适应 `min(1e-4, 0.05 * |b|)`，确保 eps_theta < 0.05 弧度（~3°）
- 对于小螺距（b ~ 1e-5）：自动调整为 tol ≈ 1e-6

**示例**：
```julia
# 使用默认自适应容差（推荐）
is_outer = edge_boundary(:node_on, mesh, i, param_dim; which=:outer)

# 手动指定
is_outer = edge_boundary(:node_on, mesh, i, param_dim; which=:outer, tol=1e-6)
```

---

### 方法2（基于半径）的容差

**参数**：`tol`（长度容差，单位：m）

**判断**：
```julia
abs(r - Rout) < tol  # 外边界
abs(r - Rin) < tol   # 内边界
```

**推荐值**：

| 容差 | 效果 | 适用场景 |
|------|------|----------|
| `1e-4` | 严格 | 网格节点恰好在 Rin/Rout 上 |
| `1e-3` | 推荐 | 通用，捕获接近边界的节点 |
| `0.01 * (Rout - Rin)` | 自适应 | 根据环域宽度调整 |

**示例**：
```julia
# 推荐值
case.opt.boundary_radial_tol = 1e-3

# 自适应
p = jellyroll_spiral_params(param_dim)
tol = 0.01 * (p.Rout - p.Rin)
is_outer = edge_boundary(:radial, mesh, i, param_dim; which=:outer, tol=tol)
```

---

## 完整示例

### 示例1：使用基于半径的对流换热（推荐）

```julia
using JuBat

# 加载参数
param_dim = ChooseCell("Jellyroll")

# 创建网格
mesh_th = jellyroll_collector_seed_mesh(param_dim; nθ=160, gsorder=2)

# 设置 case
case = Case(param_dim, ...)
case.mesh["thermal2D"] = mesh_th

# 设置边界识别方法为"基于半径"
case.opt.boundary_method = :radial
case.opt.boundary_radial_tol = 1e-3

# 边界条件将自动应用于 r ≈ Rout 的节点
# ThermalDistributed2D_BC 会识别外边界节点并施加对流换热
```

---

### 示例2：手动识别并统计

```julia
# 对比两种方法
p = jellyroll_spiral_params(param_dim)

# 方法1：螺旋线
is_outer_spiral = [edge_boundary(:node_on, mesh_th, i, param_dim; which=:outer) 
                   for i in 1:mesh_th.nlen]
println("方法1识别的外边界节点数: ", count(identity, is_outer_spiral))

# 方法2：半径
is_outer_radial = [edge_boundary(:radial, mesh_th, i, param_dim; which=:outer, tol=1e-3) 
                   for i in 1:mesh_th.nlen]
println("方法2识别的外边界节点数: ", count(identity, is_outer_radial))

# 半径分布
x = mesh_th.node[:,1]; y = mesh_th.node[:,2]
r_all = hypot.(x, y)
r_spiral = r_all[is_outer_spiral]
r_radial = r_all[is_outer_radial]
println("方法1外边界半径范围: [", minimum(r_spiral), ", ", maximum(r_spiral), "]")
println("方法2外边界半径范围: [", minimum(r_radial), ", ", maximum(r_radial), "]")
println("Rout = ", p.Rout)
```

---

### 示例3：运行对比脚本

```bash
# 运行预设的对比脚本
julia tools/compare_boundary_methods.jl
```

**输出示例**：
```
边界识别方法对比：collector_seed_mesh 网格（nθ=160）
======================================================================
网格信息:
  总节点数: 322
  Rin = 0.010 m, Rout = 0.020 m
  ...

方法 1：基于螺旋线 θ 范围的边界识别 (:node_on)
======================================================================
  内螺旋线节点数: 161
  外螺旋线节点数: 161
  内螺旋线节点半径范围: [0.01012, 0.01043] m
  外螺旋线节点半径范围: [0.01030, 0.01062] m

方法 2：基于半径的边界识别 (:radial)
======================================================================
  内边界节点数 (|r - Rin| < 1e-3): 0
  外边界节点数 (|r - Rout| < 1e-3): 0
  尝试更宽松的容差 (tol = 1e-3):
    外边界节点数: 15

总结与建议
======================================================================
推荐使用方法2（基于半径）：
  - 内边界：r ≈ Rin（物理上的最内层）
  - 外边界：r ≈ Rout（物理上的最外层）
```

---

## API 参考

### `edge_boundary` 函数

**签名**：
```julia
edge_boundary(kind::Symbol, args...; kwargs...)
```

**参数**：

| `kind` | 功能 | 参数 | 关键字参数 |
|--------|------|------|------------|
| `:r` | 计算给定 θ 的螺旋线半径 | `θ, param_dim` | `which=:inner/:outer` |
| `:theta_range` | 返回边界的 θ 范围 | `param_dim` | `which=:inner/:outer` |
| `:is_on` | 判断点 (x,y) 是否在边界上 | `x, y, param_dim` | `which=:inner/:outer, tol=1e-4` |
| `:node_on` | 判断网格节点是否在边界上 | `mesh, nidx, param_dim` | `which=:inner/:outer, tol=1e-4` |
| `:radial` | 判断节点是否在物理边界上 | `mesh, nidx, param_dim` | `which=:inner/:outer, tol=1e-3` |

**示例**：
```julia
# 螺旋线方法
is_outer = edge_boundary(:node_on, mesh, i, param_dim; which=:outer)

# 半径方法
is_outer = edge_boundary(:radial, mesh, i, param_dim; which=:outer, tol=1e-3)
```

---

## 常见问题

### Q1：为什么我的需求无法实现？

**A**：您的需求是"内边界在第1圈，外边界在第N圈"，但 collector_seed_mesh 网格只覆盖一个 θ 段（通常是 [0, 2π]），不覆盖第N圈。

**解决方案**：
1. 使用基于半径的识别（`:radial`）- 推荐
2. 创建全螺旋网格（需要新的网格生成函数）

---

### Q2：方法1和方法2有什么区别？

**A**：
- **方法1（螺旋线）**：识别网格拓扑结构（内圈 vs 外圈节点）
- **方法2（半径）**：识别物理边界位置（r ≈ Rin vs r ≈ Rout）

对于对流换热等物理边界条件，**推荐使用方法2**。

---

### Q3：如何选择容差？

**A**：
- **螺旋线方法**：使用默认自适应容差（`min(1e-4, 0.05*|b|)`）
- **半径方法**：
  - 默认 `tol = 1e-3`（1mm）
  - 或者 `tol = 0.01 * (Rout - Rin)`（环域宽度的1%）

---

### Q4：我的对流换热施加在错误的位置？

**A**：检查 `case.opt.boundary_method` 设置：
```julia
# 推荐设置
case.opt.boundary_method = :radial
case.opt.boundary_radial_tol = 1e-3
```

如果使用 `:spiral` 方法，对流换热会施加在"外螺旋线"上（r ≈ Rin + t_repeat），而不是物理外边界（r ≈ Rout）。

---

## 修改文件清单

| 文件 | 修改内容 | 状态 |
|------|----------|------|
| `src/Jellyrollmodel.jl` | 修复 `:node_on` 外圈判断（mod 2π） | ✅ |
| `src/Jellyrollmodel.jl` | 新增 `:radial` 边界识别方法 | ✅ |
| `src/ThermalDistributed.jl` | 支持可选边界识别方法 | ✅ |
| `tools/debug_outer_nodes.jl` | 更新预期范围说明 | ✅ |
| `tools/compare_boundary_methods.jl` | 创建对比测试脚本 | ✅ |

---

## 总结

✅ **已完成**：
1. 修复螺旋线边界识别（对于 collector_seed_mesh）
2. 新增基于半径的边界识别（推荐用于物理边界条件）
3. 更新 `ThermalDistributed2D_BC` 支持两种方法
4. 创建对比测试脚本和详细文档

⭐ **推荐使用**：
```julia
case.opt.boundary_method = :radial
case.opt.boundary_radial_tol = 1e-3
```

📊 **验证**：
```bash
julia tools/compare_boundary_methods.jl
```

🎉 **状态**：代码已完成，等待您的测试！
