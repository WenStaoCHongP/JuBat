# testexample.jl - 使用说明

## 📋 概述

这是一个基于最新**多SPMe并行架构**的Jellyroll电池电化学-热耦合仿真测试案例。

**核心特性**:
- ✅ 多SPMe并行模式（每个热单元独立SPMe模型）
- ✅ Jellyroll螺旋结构网格（collector-seeded）
- ✅ 逐单元电流分布（非线性分流求解）
- ✅ 逐单元热源计算（精确η和dUdT）
- ✅ 详细调试信息输出
- ✅ 力学耦合已关闭（专注电化学-热耦合）

---

## 🚀 快速开始

### 运行仿真

```bash
julia testexample.jl
```

**预期运行时间**: 约2-5分钟（取决于CPU性能）

---

## 📊 输出结果

### 控制台输出

仿真过程中会输出以下调试信息：

```
[Solve] 多SPMe模式：状态向量维度 = XXXX
  ne = XX, n_chem = 60, nT = XXX
```

以及每个阶段的进度：
- [1/6] 参数设置
- [2/6] 创建案例和Jellyroll网格
- [3/6] 运行多SPMe并行求解器
- [4/6] 提取结果和诊断
- [5/6] 生成基本图像
- [6/6] 生成最终温度场图像

### 生成的图像文件

1. **testexample_voltage.png** - 放电曲线
   - 电压随时间演化
   - 显示截止电压线

2. **testexample_temperature.png** - 温度演化
   - 平均温度随时间变化

3. **testexample_current_snapshots.png** - 逐单元电流分布快照
   - 5个时间点的电流空间分布
   - 径向-角度极坐标显示

4. **testexample_current_heterogeneity.png** - 电流异质性演化
   - 电流分布的变异系数(CV)随时间变化
   - 反映逐单元电流的不均匀程度

5. **testexample_Tfield.png** - 最终温度场
   - Jellyroll结构的温度分布
   - 高斯核插值平滑显示

6. **testexample_Tfield.svg** - 最终温度场（矢量图）
   - 可缩放的矢量格式

---

## 🔧 参数调整

### 修改电流

```julia
Crates = 1.0  # C-rate，可修改为 0.5, 1, 2, 3等
i = 5 * Crates  # 电流 (A)
```

### 修改仿真时间

```julia
opt.time = [0.0, 30.0]  # [起始, 结束] 秒
```

**建议**:
- 快速测试: 10-30秒
- 标准测试: 50-100秒
- 完整放电: 根据C-rate调整（1C约3600秒）

### 修改网格分辨率

```julia
nθ = 40  # 周向单元数
```

**影响**:
- nθ=20: 快速（约1分钟）
- nθ=40: 中等（约2-5分钟）
- nθ=80: 精细（约10-20分钟）

**注意**: 
- 网格数 ne ≈ nθ × n_radial
- 状态向量维度 ≈ ne × 60 + nT
- 计算时间 ∝ ne²

### 修改电化学网格

```julia
opt.Nn = 10  # 负极网格数
opt.Ns = 5   # 隔膜网格数
opt.Np = 10  # 正极网格数
opt.Nrn = 10 # 负极颗粒径向网格数
opt.Nrp = 10 # 正极颗粒径向网格数
```

**建议**: 调试时使用较小值（5-10），正式仿真使用较大值（15-20）

---

## 🔍 调试技巧

### 启用详细调试

已默认启用：
```julia
opt.debug_multi_spme = true  # 输出状态向量维度等信息
```

### 检查电流守恒

在输出的"逐单元变量统计"中查看：
```
电流守恒: I_total=X.XXe+XX, Σ(w·I_e)=X.XXe+XX, 误差=X.XXe-XX
```

**正常**: 误差 < 1e-6

### 检查电压范围

确保电压在合理范围内：
```julia
param_dim.cell.v_l = 2.5  # 下限
param_dim.cell.v_h = 4.2  # 上限
```

### 性能监控

如果仿真很慢：
1. 减少 nθ（如改为20）
2. 减少电化学网格数（Nn, Np等改为5）
3. 缩短仿真时间（如改为10秒）

---

## 📈 预期结果

### 正常输出

**电压**:
- 初始: ~4.0 V（取决于初始SOC）
- 最终: ~3.5-3.8 V（取决于放电深度）
- 电压降: 0.2-0.5 V

**温度**:
- 初始: 298.15 K (25°C)
- 温升: 2-10 K（取决于C-rate和仿真时间）

**电流异质性 (CV)**:
- 初始: < 5%（温度均匀）
- 演化: 可能增加到10-20%（如果出现温度梯度）

### 异常情况

**电压降太快** (>1V):
- 可能原因: 电流过大或初始SOC过低
- 解决方案: 减小Crates或增加初始SOC

**温升过大** (>20K):
- 可能原因: 电流过大或热传导不足
- 解决方案: 减小Crates或检查热参数

**电流守恒误差大** (>1e-4):
- 可能原因: 分流求解器收敛问题
- 解决方案: 减小时间步长或检查网格质量

---

## 🆚 与单SPMe模式对比

### 单SPMe模式（原实现）

```julia
opt.per_element_spme = false  # 关闭多SPMe
```

**特点**:
- 单一全局电化学状态
- 热源使用全局过电位（近似）
- 计算快（约30x）

### 多SPMe模式（新实现）

```julia
opt.per_element_spme = true  # 启用多SPMe
```

**特点**:
- 每个热单元独立电化学状态
- 热源使用逐单元过电位（精确）
- 计算慢（约30x），但更准确

**对比**:
| 项目 | 单SPMe | 多SPMe |
|-----|-------|--------|
| 状态向量 | ~260 | ~3000 (ne=50) |
| 计算时间 | 1x | 30x |
| 热源精度 | 近似 | 精确 |
| 异质性 | 无法捕捉 | 完整描述 |

---

## 🛠️ 扩展功能

### 启用力学耦合

目前已关闭，如需启用：

1. 在第214-226行取消注释 `thermal_stress` 相关代码
2. 添加应力场可视化（参考 spme_thermal2d_example.jl）

### 非均匀初始SOC

```julia
# 在 ModelInitialisation_MultiSPMe 中使用
initial_soc_distribution = [0.9, 0.85, 0.9, ...]  # 每个单元的初始SOC
y0 = JuBat.ModelInitialisation_MultiSPMe(case; 
                                          initial_soc_distribution=initial_soc_distribution)
```

### 自定义边界条件

修改热边界条件（在ThermalDistributed2D_BC中定义）：
- 内径: 默认绝热
- 外径: 默认对流

---

## 📚 相关文档

- `多SPMe并行架构修改计划.md` - 架构设计文档
- `阶段1-4完成总结.md` - 各阶段实现总结
- `多SPMe并行架构_总进度报告.md` - 总体进度
- `spme_thermal2d_example.jl` - 原示例（单SPMe+自定义热源）

---

## ⚠️ 注意事项

1. **内存占用**: 
   - ne=50: ~100MB
   - ne=500: ~1GB
   - ne=1000: ~5GB

2. **计算时间**:
   - 与 ne² 成正比
   - 建议先用小网格（nθ=20）测试

3. **数值稳定性**:
   - 如果求解失败，尝试减小时间步长
   - 如果电流守恒误差大，检查网格质量

4. **Julia版本**: 需要 Julia 1.6 及以上

---

## 🐛 故障排除

### 问题1: "multi_spme_layout not found"

**原因**: 未正确初始化多SPMe模式

**解决**: 确保：
- `opt.per_element_spme = true`
- `opt.thermalmodel = "distributed2D"`
- `case.mesh["thermal2D"]` 已创建

### 问题2: 求解器崩溃

**原因**: 电压超出范围或数值不稳定

**解决**:
- 检查 `param_dim.cell.v_l` 和 `v_h`
- 减小电流或时间步长
- 检查初始SOC是否合理

### 问题3: 绘图失败

**原因**: Plots.jl或依赖库问题

**解决**:
```julia
using Pkg
Pkg.add("Plots")
Pkg.add("GR")  # 后端
```

---

## 📧 反馈

如有问题或建议，请参考：
- 项目文档
- 测试脚本（test_*.jl）
- 源代码注释

---

**创建日期**: 2025-11-17  
**版本**: v1.0  
**基于**: 多SPMe并行架构 阶段1-4完整实现
