#项目名称：Jubat热分布模型开发

这是一个基于Julia语言的锂电池多场耦合仿真模型库，目前已完成物理建模、网格划分、有限元离散、瞬态求解、后处理等完整有限元功能。
目前库支持电化学-热-力耦合仿真，其中电化学模型包括SPM、SPMe、P2D和SP2D，热模型仅为集总模型，先要在已有模型的基础上开发分布式热模型。

项目目标如下：

1. 开发通用的一维分布式热传导模型，能够处理任意形状的电池模型；
2. 开发适用于Jellyroll的二维分布式热传导模型，能够处理圆柱形电池模型；
3.后续开发电化学-热-力耦合模型。

#研究对象

本项目的研究对象为锂离子电池，一维模型研究电池型号为已有的LGM50和Northrop，二维电池模型为Jellyroll模型，其真实三维物理结构为圆柱形电池，内部结构为阿基米德螺旋线，即正极隔膜负极绕电芯缠绕形成的果冻卷结构，根据已有研究其高度方向的温度场基本不变，采用高度均一化假设降阶为二维模型，研究其卷面结构的温度场分布，边界条件为果冻卷最内侧绝热，上下表面和外侧面为空气强制对流条件。



##项目结构
一.待修改子程序
1.Jellyroll.jl
-定义圆柱形电池的材料参数，暂时不需要修改。

2.Option.jl
-电化学网格和热网格不同，需要定义thermalmeshType，可以选择L2、L3或Q4。

3.Variables.jl
-添加分布式热模型相关状态：T_nodes, T_prev, heat_source_fields。

4.SetParams.jl
-修改ChooseCell函数以支持Jellyroll；
-添加分布式热模型必要的参数。

5.SetCase.jl
-添加分布式热模型条件判断，1D和2D，"temperature"为不同数据类型；
-添加热网格的Case结构体，thermalmesh。

6.Initialisation.jl
-添加分布式热模型条件判断，y0分布给每个热网格节点。

7.jellyroll.jl
-仅针对二维问题，研究Jellyroll模型，添加坐标映射函数，将物理模型按真实的果冻卷结构建模，要求JellyrollMesh直接调用SetMesh.jl中Q4单元生成网格。

8.SetMesh.jl
-在保持原逻辑的基础上，修改代码支持二维Q4单元的生成和Q4单元形函数的计算，考虑到物理模型为果冻卷结构，该Q4单元能够支持异形结构的网格划分。

9.和ThermalDistributed2D.jl
-添加二维分布式热传导模型，ThermalDistributed2D函数用于计算MT、KT和FT，ThermalDistributed2D_BC用于添加边界条件，ThermalDistributed2D_variables用于定义变量。
-函数编写的过程中调用Assemble.jl和Tools.jl中的已有函数简化代码。
-添加heat_source函数，用于计算内热源Q_rxn、Q_ohm、Q_rev，采用单元平均分布结合空间分布映射到热网格；
-此处需解决一维电化学网格和二维热网格维度不匹配问题，热模型所需内热源为热网格单元平均，每个单元都是一个小的SPMe模型，而电化学模型所需温度信息也为单元平均温度。
-质量矩阵 M：面积积分，$MT = \iint_S c\rho N^T N dS$
刚度矩阵 K：面积积分（导数项）+ 边界对流项，$KT = \iint_S B^T D B dS + \iint_L N^T N dL$
载荷向量 F：面积热源项 + 边界对流项，$FT = \iint_S Q_{gen} N^T dS + \iint_L N^T T_{amb} dL$
其中 $B$ 是形函数对物理坐标的导数，$D$ 是热导率（各向同性时为标量 $\lambda$），$N$ 是形函数。

10.ThermalDistributed1D.jl
-添加一维分布式热传导模型，与ThermalDistributed2D.jl逻辑基本相同；
-与二维热模型区别在于不存在维度不同的问题。

11.Thermal.jl
-与 distributed 版本共存：保留 lumped，新增一个抽象 ThermalModel 类型。

12.Solve.jl
-CallModel函数修改以支持分布式热模型。

13.PostProcess.jl
-添加分布式热模型后处理函数，输出合理的数据格式。

14.JuBat.jl
-添加新增函数的导入导出；
-导出新类型 + 统一接口 run(case)。

附加功能函数：
15.Tools.jl和Assemble.jl在保持原逻辑的基础上添加二维拓展。

三.项目调试
-thermalDistributed_example作为分布式热模型的例子，用于各函数的调试，能够监视资源的占用情况，统计计算时间，输出可视化图，包括网格划分示意图，等温线图（参数 sweep、性能计时、验证输出存储格式）。

四.项目优化
在完成以上基础功能的情况下对项目进行迭代优化，以保证仿真精度的前提下，尽可能减少资源占用和计算时间。


    # Temporarily skip automatic classification. Instead, call edge_boundary to verify it's implemented.
    if hasproperty(case.param_dim, :tab) && hasproperty(case.param_dim, :t_repeat)
        try
            tin = edge_boundary(:theta_range, case.param_dim; which=:inner)
            tout = edge_boundary(:theta_range, case.param_dim; which=:outer)
            @info "[thermal BC debug] edge_boundary ranges" inner=tin outer=tout
        catch err
            @warn "[thermal BC debug] edge_boundary call failed" err
        end
    else
        @info "[thermal BC debug] edge_boundary not tested: param_dim not identified as jellyroll"
