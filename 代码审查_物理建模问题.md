# 代码审查：物理建模问题

## 审查范围

基于 `docs/SPMe_Thermal2D_Theory_vs_Code.md` 对代码进行系统检查。

---

## ✅ 已验证正确的部分

### 1. 几何与网格 ✅

**理论**（文档第1节）：
- collector_seed_mesh 覆盖从 Rin 到 Rout 的整个螺旋
- 每个条带单元跨越完整层序（PCC → PE → SP → NE → NCC）

**代码实现**（`src/Jellyrollmodel.jl` 第435-491行）：
```julia
θ0 = max(0.0, (Rin - a - s_in)/b)
θ1 = min((Rout - a - s_out)/b, (Rout - a)/b)
# 实际覆盖 θ ∈ [0, ~997] ≈ [0, 2π×158]
```
✅ **正确**

### 2. 材料参数 ✅

**理论**（文档第34-36行）：
- 径向（串联）：λ_r,eff = 1 / Σ(f_k / λ_k) - 调和平均
- 切向（并联）：λ_t,eff = Σ(f_k · λ_k) - 算术平均

**代码实现**（`src/Jellyrollmodel.jl` 第95-96行）：
```julia
λ_r_eff = 1 / (fracs.NE/λ_an + fracs.SP/λ_sep + fracs.PE/λ_ca + fracs.PCC/λ_Al + fracs.NCC/λ_Cu)
λ_t_eff = fracs.NE*λ_an + fracs.SP*λ_sep + fracs.PE*λ_ca + fracs.PCC*λ_Al + fracs.NCC*λ_Cu
```
✅ **正确**

### 3. 各向异性导热张量装配 ✅

**理论**（文档第104-106行）：
- K_i = λ_r,eff e_r e_r^T + λ_θ,eff e_θ e_θ^T
- 旋转至全局坐标：Kxx, Kxy, Kyy

**代码实现**（`src/ThermalDistributed.jl` 第150-169行）：
```julia
θ = atan(gy[g], gx[g])
c = cos(θ); s = sin(θ)
Kxx[g] = lr*c*c + lt*s*s
Kxy[g] = (lt - lr)*s*c
Kyy[g] = lr*s*s + lt*c*c
# 加负号以与电化学约定统一
cxx = -Kxx .* wJ
```
✅ **正确**

### 4. 质量矩阵 ✅

**理论**（文档第154行）：
- MT = ∬ (ρc/ρc_ref) N^T N dΩ*

**代码实现**（`src/ThermalDistributed.jl` 第68-89行）：
```julia
ρc_e[e] = f_NE*ρc_NE + f_SP*ρc_SP + f_PE*ρc_PE + f_PCC*ρc_PCC + f_NCC*ρc_NCC
ρc_weights = ρc_e[mesh.gs.ele] .* (wJ ./ (L_th^2))
MT = Assemble(Vi, Vj, Ni, Ni, ρc_weights, nnode)
```
✅ **正确**

---

## ❌ 发现的问题

### 问题1：内热源计算没有分层 🔴 **严重**

**理论要求**（文档第477-481行）：

应该**分层计算**热源：
```
- 负极：Q_NE = a_s,n j_n η_n + a_s,n j_n T dU_n/dT + q_ohm,s,ne + q_ohm,e,ne
- 隔膜：Q_SP = q_ohm,e,sp
- 正极：Q_PE = a_s,p j_p η_p + a_s,p j_p T dU_p/dT + q_ohm,s,pe + q_ohm,e,pe
- 集流体：Q_PCC = I_app²/(3σ_PCC), Q_NCC = I_app²/(3σ_NCC)

然后映射：q_e = Σ f_k(e) · Q_k
```

**实际代码**（`src/ThermalDistributed.jl` 第423-453行）：

```julia
# ❌ 问题1：反应热没有分层
Q_rxn = abs(I_app * (eta_p - eta_n))  # 只算了总的，没分正负极

# ❌ 问题2：可逆热没有分层
Q_rev = abs(I_app) * T_nd_e * (param.PE.dUdT(csp_surf) - param.NE.dUdT(csn_surf))

# ✓ 欧姆热计算了各层，但...
P_s_ne = I_nd^2 * (t_n / sig_n_eff) / 3.0
P_e_ne = I_nd^2 * (t_n / kappa_ne) / 3.0
...

# ❌ 问题3：最终没有按层聚合
Q_ele = Q_rxn + Q_rev + Q_ohm  # 这是总和，不是分层的！
q_elem[e] = (fks[e,1] + fks[e,2] + fks[e,3]) * Q_ele + fks[e,4] * Q_PCC + fks[e,5] * Q_NCC
# 这相当于把电极层的热源平均分配给 NE/SP/PE，而不是各层独立计算！
```

**正确的做法应该是**：
```julia
# 分层计算
Q_NE = (反应热_n + 可逆热_n + 欧姆热_s_n + 欧姆热_e_n)
Q_SP = 欧姆热_e_sp
Q_PE = (反应热_p + 可逆热_p + 欧姆热_s_p + 欧姆热_e_p)
Q_PCC = I²/(3σ_PCC)
Q_NCC = I²/(3σ_NCC)

# 按层权重聚合
q_elem[e] = f_NE*Q_NE + f_SP*Q_SP + f_PE*Q_PE + f_PCC*Q_PCC + f_NCC*Q_NCC
```

**影响**：
- 🔴 **严重**：热源分布不准确
- 反应热和可逆热应该主要在电极层（NE/PE），而不是隔膜（SP）
- 当前实现会把这些热源错误地分配到隔膜

---

### 问题2：欧姆热公式细节

**文档公式**（第475-476行）：
```
固相：q_ohm,s ≈ (I_app²) · (t/σ_eff) / 3 ÷ t = I_app² / (3 σ_eff)
电解液：q_ohm,e ≈ (I_app²) · (t/κ_eff) / 3 ÷ t = I_app² / (3 κ_eff)
```

**代码实现**（第441-446行）：
```julia
P_s_ne = I_nd^2 * (t_n / sig_n_eff) / 3.0  # 功率 P (W/m²)
...
Q_ohm = P_e_ne / t_n + P_s_ne / t_n + ...  # 除以厚度转为体热源 (W/m³)
```

**问题**：
- P_s_ne 是**面功率密度**（W/m²）
- 除以 t_n 后变成**体热源密度**（W/m³）
- 但公式推导：P = I² · R = I² · (t/σ) / 3
- 体热源：q = P / t = I² / (3 σ t) = I² / (3 σ_eff)，其中 σ_eff = σ·t
- ⚠️ 需要确认 `sig_n_eff` 的定义是否包含厚度

**代码中**（第433行）：
```julia
sig_n_eff = param.NE.sig .* param.NE.eps_s
```
- 这里 `sig_n_eff = σ · ε_s`（有效电导率，不含厚度）
- 所以 P = I² · t / (3 σ ε_s)
- q = P / t = I² / (3 σ ε_s) ✓

**结论**：欧姆热计算**基本正确**，但需要确认 σ_eff 的定义。

---

### 问题3：隔膜欧姆热公式

**代码**（第444行）：
```julia
P_e_sp = I_nd^2 * (t_sp / kappa_sp)  # ❌ 缺少 /3 因子？
```

**文档公式**（第476行）：
```
隔膜为 I_app² / κ_sp（没有/3因子）
```

**分析**：
- 对于负极/正极：电流是分布的，积分得到 1/3 因子
- 对于隔膜：电流是均匀的（无反应），所以**没有** 1/3 因子
- ✓ **代码正确**

**但是**（第446行）：
```julia
Q_ohm = ... + P_e_sp / t_sp
```
- P_e_sp / t_sp 转换为体热源
- ✓ **正确**

---

## 建议修复方案

### 修复1：重写内热源计算（分层）

需要将 `heatQ_Source` 函数重构为：

```julia
# 1. 计算各层的体热源密度
# 负极
Q_rxn_NE = as_n * jn * eta_n
Q_rev_NE = as_n * jn * T * dUdT_n
Q_ohm_s_NE = I_nd^2 / (3.0 * sig_n_eff)
Q_ohm_e_NE = I_nd^2 / (3.0 * kappa_ne)
Q_NE = Q_rxn_NE + Q_rev_NE + Q_ohm_s_NE + Q_ohm_e_NE

# 隔膜（只有电解液欧姆热）
Q_SP = I_nd^2 / kappa_sp

# 正极
Q_rxn_PE = as_p * jp * eta_p
Q_rev_PE = as_p * jp * T * dUdT_p
Q_ohm_s_PE = I_nd^2 / (3.0 * sig_p_eff)
Q_ohm_e_PE = I_nd^2 / (3.0 * kappa_pe)
Q_PE = Q_rxn_PE + Q_rev_PE + Q_ohm_s_PE + Q_ohm_e_PE

# 集流体
Q_PCC = I_nd^2 / (3.0 * σ_PCC)
Q_NCC = I_nd^2 / (3.0 * σ_NCC)

# 2. 按层权重聚合
q_elem[e] = fks[e,1]*Q_NE + fks[e,2]*Q_SP + fks[e,3]*Q_PE + fks[e,4]*Q_PCC + fks[e,5]*Q_NCC
```

**关键变化**：
1. 需要从 SPMe 中提取：`as_n, as_p, jn, jp`（不是 I_app）
2. 每层独立计算反应热和可逆热
3. 按层权重 f_k 聚合

---

## 优先级

| 问题 | 严重性 | 优先级 | 修复复杂度 |
|------|--------|--------|-----------|
| 内热源分层计算 | 🔴 严重 | P0 | 高 |
| 欧姆热公式验证 | ⚠️ 中等 | P1 | 低 |
| 边界条件（已正确） | ✅ 正常 | - | - |

---

## 下一步

1. **立即修复**：重构 `heatQ_Source` 函数，实现分层热源计算
2. **验证**：确认 σ_eff, κ_eff 的定义是否包含厚度
3. **测试**：运行热分布算例，检查结果的物理合理性

---

**状态**：🔴 发现重大物理建模错误，需要修复后才能保证结果准确性！
