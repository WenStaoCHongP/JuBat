# 简化耦合模型 - 快速参考卡片

## 🎯 一句话总结
单个SPMe计算内热源 + 2D热传导求解温度场 + 平均温度反馈

---

## 📐 模型架构

```
┌─────────────────────────────────────────┐
│         单个SPMe（全局平均）              │
│  - 输入：总电流I, 平均温度T_avg          │
│  - 输出：总内热源Q_total                 │
│  - 状态：cs_n, cs_p, ce（单组）          │
└──────────────┬──────────────────────────┘
               │ Q_total
               ▼
     ┌─────────────────────┐
     │   热源均匀分配        │
     │ q(x,y) = Q_total/V  │
     └──────────┬───────────┘
                │ q(x,y)
                ▼
┌─────────────────────────────────────────┐
│       2D热传导（空间分布）                │
│  - 输入：热源场q(x,y)                    │
│  - 输出：温度场T(x,y,t)                  │
│  - 网格：12,608单元，25,218节点          │
└──────────────┬──────────────────────────┘
               │ T(x,y,t)
               ▼
        ┌──────────────┐
        │  平均温度计算  │
        │ T_avg=mean(T) │
        └──────┬────────┘
               │ T_avg
               ▼（反馈）
           回到SPMe
```

---

## 🔧 关键配置

```julia
# 1. 关闭多SPMe模式
opt.per_element_spme = false

# 2. 启用简化耦合
opt.simple_thermal_coupling = true

# 3. 使用2D热传导
opt.thermalmodel = "distributed2D"

# 4. 启用热模型
opt.thermal_enabled = true
```

---

## 📁 新建文件（3个）

| 文件 | 功能 | 行数 |
|-----|------|------|
| `src/ModelInitialisation_SimpleCoupling.jl` | 初始化 | ~80 |
| `src/CallModel_SimpleCoupling.jl` | 核心求解 | ~250 |
| `example/testexample_simple_coupling.jl` | 测试脚本 | ~150 |

---

## ✏️ 修改文件（4个，共20行）

| 文件 | 修改位置 | 新增行数 |
|-----|---------|---------|
| `src/CallModel.jl` | 开头添加分支 | 3 |
| `src/Solve.jl` | 初始化部分 | 5 |
| `src/Variables.jl` | 变量定义 | 10 |
| `src/JuBat.jl` | 模块导出 | 2 |

---

## 🔑 核心函数（5个）

### 1. 初始化
```julia
y = [cs_n(Nrn), cs_p(Nrp), T(nT)]
```

### 2. 温度平均
```julia
T_avg = mean(T_all) * T0_scale
```

### 3. 热源计算
```julia
Q_total = Q_rxn + Q_ohm + Q_rev  # W
```

### 4. 热源分配
```julia
q_vol = Q_total / V_total  # W/m³
```

### 5. 矩阵组装
```julia
M = [M_chem  0    ]
    [0       M_T  ]
```

---

## 💾 内存需求对比

| 模式 | ne | nT | 内存 | 相对 |
|-----|----|----|------|------|
| 多SPMe | 12,608 | 25,218 | 30GB | 10× |
| 简化耦合 | - | 25,218 | 3GB | 1× |
| 单SPM | - | - | 0.1GB | 0.03× |

**公式**：
- 多SPMe：`ne × 6 × num_steps × 8 bytes`
- 简化耦合：`nT × num_steps × 8 bytes`

---

## ⚡ 性能对比

| 指标 | 多SPMe | 简化耦合 | 单SPM |
|-----|--------|---------|-------|
| 计算时间 | 60分钟 | 6分钟 | 5分钟 |
| 内存 | 30GB | 3GB | 0.1GB |
| 温度分布 | ✅ 详细 | ✅ 有 | ❌ 无 |
| 电流分布 | ✅ 有 | ❌ 无 | ❌ 无 |
| 局部SOC | ✅ 有 | ❌ 无 | ❌ 无 |

---

## ✅ 优势

1. **内存低**：3GB（多SPMe的1/10）
2. **速度快**：6分钟（多SPMe的1/10）
3. **有温度场**：仍保留空间温度分布
4. **代码简单**：复用率75%

---

## ⚠️ 局限

1. **无电流分布**：假设均匀
2. **无局部SOC**：全局平均
3. **温度反馈简化**：使用平均值
4. **不适用**：需要分析局部热点的场景

---

## 📊 适用场景

### ✅ 适合
- 整体热行为评估
- 参数敏感性分析
- 冷却策略优化
- 快速设计迭代

### ❌ 不适合
- 局部热失控分析
- 电流分布不均问题
- 极耳效应详细研究
- 逐单元老化分析

---

## 🚀 快速开始

### 步骤1：配置（1分钟）
```julia
opt.simple_thermal_coupling = true
opt.per_element_spme = false
```

### 步骤2：运行（6分钟）
```bash
julia example/testexample_simple_coupling.jl
```

### 步骤3：查看结果（1分钟）
```julia
# 温度场
open("temperature_field.png")

# 温度历史
open("temperature_history.png")
```

---

## 🔍 调试清单

- [ ] 状态向量维度：n_chem + nT
- [ ] 温度范围：280-320K
- [ ] 能量守恒：Q_gen ≈ Q_diss
- [ ] 平均温度：与单SPM接近
- [ ] 无NaN/Inf

---

## 📞 问题排查

### Q1: OutOfMemoryError
```julia
# 解决：减少时间步或网格
opt.dt = [0.5, 10.0]
nθ = 40
```

### Q2: 温度不稳定
```julia
# 解决：限制温度变化
dT = min(abs(dT), 5.0) * sign(dT)
```

### Q3: 能量不守恒
```julia
# 检查：热源计算
println("Q_gen = ", Q_total, " W")
println("Q_diss = ", Q_dissipated, " W")
```

---

## 📚 相关文档

1. **详细计划**：`docs/简化耦合模型开发计划.md`
2. **实施步骤**：`docs/简化耦合模型实施步骤.md`
3. **代码模板**：（待创建）

---

## ⏱️ 时间估算

| 阶段 | 时间 |
|-----|------|
| 编码 | 6小时 |
| 测试 | 2小时 |
| 调试 | 2小时 |
| **总计** | **10小时** |

---

## 🎓 理论基础

### 电化学
- **模型**：单个SPMe
- **方程**：固相扩散+电解质+动力学
- **假设**：电流均匀分布

### 热传导
- **模型**：2D稳态/瞬态
- **方程**：∇·(λ∇T) = -q
- **边界**：对流+辐射

### 耦合
- **正向**：SPMe → 热源 → 温度
- **反向**：温度平均 → SPMe
- **假设**：弱耦合（显式）

---

准备好了吗？我可以立即开始编码！🚀
