# 修改方案：修正电流设置

## 🎯 问题核心

**testexample.jl 和 testexample_simple_coupling.jl 使用了错误的电流值：**

```
Jellyroll参数: I1C = 60A, capacity = 60Ah
实际电流设置: i = 5A  ❌
正确电流设置: i = 60A ✓

结果：
- 电流过小（仅为额定的8.3%）
- 电压几乎不变（4.13V → 4.176V）
- 热源过小（0.07W vs 应该10-50W）
- 温升过小（1.35K vs 应该10-30K）
```

---

## 📝 修改清单

### 修改1：testexample.jl - 电流设置 ⭐⭐⭐

**文件：** `example/testexample.jl`  
**位置：** 第40-42行

```julia
# ========== 修改前 ==========
Crates = 1.0  # C-rate
i = 5 * Crates  # 电流 (A)
opt.Current = x -> i

# ========== 修改后 ==========
Crates = 1.0  # C-rate
i = param_dim.cell.I1C * Crates  # 使用参数文件中的I1C（60A）
opt.Current = x -> i
```

**效果：**
- 电流从 5A → 60A
- 无量纲电流从 0.083 → 1.0
- 热源从 0.07W → 预期 10-50W

---

### 修改2：testexample_simple_coupling.jl - 电流设置 ⭐⭐⭐

**文件：** `example/testexample_simple_coupling.jl`  
**位置：** 第44-46行

```julia
# ========== 修改前 ==========
Crates = 1.0  # C-rate
i = 5 * Crates  # 电流 (A)
opt.Current = x -> i

# ========== 修改后 ==========
Crates = 1.0  # C-rate
i = param_dim.cell.I1C * Crates  # 使用参数文件中的I1C（60A）
opt.Current = x -> i
```

---

### 修改3：testexample.jl - 延长放电时间（可选）⭐⭐

**文件：** `example/testexample.jl`  
**位置：** 第53行

```julia
# ========== 修改前 ==========
opt.time = [0.0, 360]  # 仿真时间 (s) - 6分钟

# ========== 修改后（推荐）==========
opt.time = [0.0, 3600]  # 仿真时间 (s) - 1小时，完整放电
```

**效果：**
- 放电时间：6分钟 → 1小时
- 放电容量：6Ah(10%) → 60Ah(100%)
- 电压降：部分 → 完整（4.13V → 2.5V）

---

### 修改4：testexample_simple_coupling.jl - 保持长时间（已经正确）✓

**文件：** `example/testexample_simple_coupling.jl`  
**位置：** 第57行

```julia
opt.time = [0.0, 3600]  # 已经是3600s，不需要修改 ✓
```

---

### 修改5：Jellyroll参数 - 正极电导率（辅助）⭐

**文件：** `src/parameters/Jellyroll.jl`  
**位置：** 第26行

```julia
# ========== 修改前 ==========
PE.sig = 0.18

# ========== 修改后 ==========
PE.sig = 10.0  # 提高到合理值（参考Northrop: 3.8, 或更高）
```

**理由：**
- 0.18 S/m 太低（正常值 1-100 S/m）
- 会导致过大的欧姆热和极化

---

### 修改6：Jellyroll参数 - 添加熵变系数（可选）⭐

**文件：** `src/parameters/Jellyroll.jl`  
**位置：** 第36、64行

```julia
# ========== 修改前 ==========
PE.dUdT = x-> 0 * x
# ... 
NE.dUdT = x-> 0 * x

# ========== 修改后（可参考Northrop.jl）==========
PE.dUdT = x-> -0.001 * (0.199521039 .- 0.928373822*x + 1.364550689000003*x.^2 - 0.6115448939999998*x.^3) ./ (1 .- 5.661479886999997*x + 11.47636191*x.^2 - 9.82431213599998*x.^3 + 3.048755063*x.^4)

NE.dUdT = x-> 0.001 * (0.005269056 .+ 3.299265709*x - 91.79325798*x.^2 + 1004.911008*x.^3 - 5812.278127*x.^4 + 19329.7549*x.^5 - 37147.8947*x.^6 + 38379.18127*x.^7 - 16515.05308*x.^8) ./ (1 .- 48.09287227*x + 1017.234804*x.^2 - 10481.80419*x.^3 + 59431.3*x.^4 - 195881.6488*x.^5 + 374577.3152*x.^6 - 385821.1607*x.^7 + 165705.8597*x.^8)
```

**理由：**
- 添加可逆热源（熵变热）
- 提高总热源10-30%

---

## 🚀 快速修改方案（推荐）

### 方案A：最小修改（必须）

只修改电流设置：
1. ✅ 修改1：testexample.jl 电流
2. ✅ 修改2：testexample_simple_coupling.jl 电流

**预期结果：**
```
电流：5A → 60A
热源：0.07W → 10-50W
温升：1.35K → 10-30K（360s放电）
电压降：0.046V → 0.3-0.5V（360s放电）
```

### 方案B：推荐修改（完整）

包含所有关键修改：
1. ✅ 修改1：testexample.jl 电流
2. ✅ 修改2：testexample_simple_coupling.jl 电流
3. ✅ 修改3：testexample.jl 延长时间到3600s
4. ✅ 修改5：Jellyroll.jl 正极电导率

**预期结果：**
```
电流：5A → 60A
时间：360s → 3600s（testexample.jl）
热源：0.07W → 10-50W
温升：1.35K → 50-100K（3600s完整放电）
电压降：0.046V → 1.5-2V（完整放电到2.5V）
```

---

## 📊 预期结果对比

### 修改前（当前状态）

```
testexample_simple_coupling.jl 输出：
  实际时间步数: 365
  最终电压: 4.176 V        ❌ 几乎没变
  平均温度: 299.35 K       ❌ 仅升温1.35K
  温度升高: 1.35 K         ❌ 太小
  平均热源: 0.07 W         ❌ 太小
```

### 修改后（方案A：仅改电流，360s）

```
预期输出：
  实际时间步数: ~30-50
  最终电压: 3.8-4.0 V      ✓ 部分放电
  平均温度: 308-318 K      ✓ 升温10-20K
  温度升高: 10-20 K        ✓ 明显升温
  平均热源: 10-30 W        ✓ 合理
```

### 修改后（方案B：改电流+时间，3600s）

```
预期输出：
  实际时间步数: ~300-400
  最终电压: 2.5-2.8 V      ✓ 完整放电
  平均温度: 348-398 K      ✓ 升温50-100K
  温度升高: 50-100 K       ✓ 显著升温
  平均热源: 10-50 W        ✓ 合理
  
注：温度可能超过安全范围（80°C），需要关注热失控风险
```

---

## ⚠️ 注意事项

### 1. 热失控风险

修改后电流增大12倍（5A→60A），热源增大144倍！
- 360s放电：温升10-20K（相对安全）
- 3600s完整放电：温升50-100K（**可能超过安全温度**）

**建议：**
- 先用360s测试
- 监控最高温度，不要超过80°C（353K）
- 或降低C-rate（如0.5C）

### 2. 计算资源

多SPMe模式（testexample.jl）内存需求：
- 原来：很小（因为电流小，收敛快）
- 修改后：可能增大（更多时间步）

**建议：**
- 先测试 simple_coupling 模式
- 如果内存不足，降低网格分辨率（nθ = 80 → 40）

### 3. 参考thermal_example.jl

thermal_example.jl 是正确的用法：
```julia
param_dim = JuBat.ChooseCell("LG M50")  // I1C = 5A
I1C = 5  // 与参数文件匹配
opt.Current = x-> I1C * Crates  // 5A × 1C = 5A ✓
```

所以正确做法是：
```julia
param_dim = JuBat.ChooseCell("Jellyroll")  // I1C = 60A
opt.Current = x-> param_dim.cell.I1C * Crates  // 60A × 1C = 60A ✓
```

---

## 🧪 验证步骤

### 1. 检查修改是否生效

在修改后的代码中添加调试输出（第43行后）：

```julia
println("="^80)
println("参数检查")
println("="^80)
@printf("  I1C（参数文件）: %.1f A\n", param_dim.cell.I1C)
@printf("  capacity（参数文件）: %.1f Ah\n", param_dim.cell.capacity)
@printf("  Crates: %.1f C\n", Crates)
@printf("  设置电流 i: %.1f A\n", i)
@printf("  预期无量纲电流: %.2f\n", i / param_dim.cell.I1C)
println("="^80)
```

**预期输出：**
```
================================================================================
参数检查
================================================================================
  I1C（参数文件）: 60.0 A
  capacity（参数文件）: 60.0 Ah
  Crates: 1.0 C
  设置电流 i: 60.0 A
  预期无量纲电流: 1.00
================================================================================
```

### 2. 运行并检查结果

```bash
cd /workspace
julia example/testexample_simple_coupling.jl
```

**检查关键指标：**
- [ ] 电流是否为60A
- [ ] 热源是否在10-50W范围
- [ ] 温升是否明显（>10K for 360s）
- [ ] 电压是否下降（<4.0V for 360s）

---

## 📚 相关文件位置

| 文件 | 作用 | 关键参数 |
|------|------|---------|
| `example/thermal_example.jl` | 正确的示例（LG M50） | I1C=5, i=5 ✓ |
| `example/testexample.jl` | 需修改（Jellyroll） | I1C=60, i=5 ❌ |
| `example/testexample_simple_coupling.jl` | 需修改（Jellyroll） | I1C=60, i=5 ❌ |
| `src/parameters/Jellyroll.jl` | Jellyroll参数 | I1C=60, capacity=60 |
| `src/parameters/LGM50.jl` | LG M50参数 | I1C=5, capacity=5 |

---

## 总结

**问题根源：电流设置与参数文件不匹配**

```
Jellyroll参数定义: I1C = 60A
代码中设置:       i = 5A ❌
正确设置:         i = param_dim.cell.I1C * Crates = 60A ✓
```

**一行代码解决问题：**
```julia
i = param_dim.cell.I1C * Crates
```

所有异常现象（电压不变、热源过小、温升不明显）都源于此！
