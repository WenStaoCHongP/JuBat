# 热应力计算修复说明

## 问题描述

之前 `result["thermal2D T_nodes [K]"]` 只保存了最终时刻的温度场，没有时间历史。导致在计算热应力时，所有时间步都使用同一个温度值，温度变化 ΔT = 0，因此热应力 σ_thermal = 0。

## 修复内容

### 1. Variables.jl (第 101-122 行)

在多SPMe模式下添加了温度场历史记录：

```julia
# 添加温度场历史（用于热应力计算）
variables["thermal2D temperature"] = zeros(Float64, nT, num)
```

### 2. Solve.jl (第 197-213 行)

在每个时间步，将当前温度场保存到历史记录中：

```julia
# 将温度场复制到 thermal2D temperature 以便记录历史（用于热应力计算）
if haskey(variables_hist, "thermal2D temperature")
    Tref = case.param_dim.scale.T_ref
    variables["thermal2D temperature"] = T_nodes .* Tref
end
```

### 3. testexample.jl (第 205-230 行)

修改应力计算代码，使用温度场的时间历史：

```julia
# 获取温度历史（优先使用完整历史）
T_nodes_hist_K = if haskey(result, "thermal2D temperature [K]")
    result["thermal2D temperature [K]"]  # (nT × num_steps)
else
    # 后备方案：使用最终温度（所有时间步相同）
    T_final = result["thermal2D T_nodes [K]"]
    repeat(T_final, 1, num_steps)
end

# ... 在循环中使用对应时间步的温度
T_nodes_K = T_nodes_hist_K[:, step]
```

## 工作原理

1. **初始化**: 在 `StandardVariables()` 中为 `thermal2D temperature` 预分配空间 (nT × num_steps)

2. **记录**: 在求解过程中，每个时间步的温度场被保存到 `variables["thermal2D temperature"]`

3. **更新**: `Variable_update!()` 自动将当前温度场更新到历史矩阵的对应列

4. **输出**: 求解结束后，完整的温度历史被保存到 `result["thermal2D temperature [K]"]`

5. **使用**: 在后处理中，每个时间步使用对应的温度场计算热应力

## 效果

- 每个时间步使用该时刻的实际温度场
- 温度变化 ΔT = T(t) - T₀ 反映真实的温度演化
- 热应力 σ_thermal = E·α·ΔT/(1-ν) 正确计算
- 应力历史能正确反映热-力耦合效应

## 兼容性

- 对于已有的简化耦合模式（simple_thermal_coupling），无影响
- 对于不使用热应力计算的情况，增加的内存开销可忽略
- 向后兼容：如果 `thermal2D temperature [K]` 不存在，会使用 `thermal2D T_nodes [K]` 作为后备
