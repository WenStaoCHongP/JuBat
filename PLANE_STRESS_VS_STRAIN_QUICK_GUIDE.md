# 平面应力 vs 平面应变 - 快速参考

## 🎯 一句话总结

- **平面应力**：薄板，垂直方向自由，应力较小
- **平面应变**：厚体，垂直方向受约束，应力较大

## 📋 快速对比表

| 项目 | 平面应力 | 平面应变 |
|------|---------|---------|
| **适用结构** | 薄板（厚度 << 宽度） | 厚体（厚度 ~ 宽度） |
| **约束条件** | σ_zz = 0 | ε_zz = 0 |
| **自由度** | z方向可变形 | z方向固定 |
| **电池类型** | 软包电池 | 圆柱/硬壳电池 |
| **应力量级** | 较小（基准） | 1.5-2.5倍 |
| **代码设置** | `:stress`（默认） | `:strain` |

## 🔧 代码使用

### 方法1：设置选项

```julia
# 平面应力（默认）
opt.plane_type = :stress

# 平面应变
opt.plane_type = :strain

case = JuBat.SetCase(param, opt)
variables = JuBat.diffusion_stress_2D(case, variables)
```

### 方法2：修改现有案例

```julia
# 已有案例，改为平面应变
case.opt.plane_type = :strain
variables = JuBat.diffusion_stress_2D(case, variables)
```

## 📐 关键公式

### 弹性矩阵

**平面应力**：
```
[D] = E/(1-ν²) × [1  ν  0]
                  [ν  1  0]
                  [0  0  (1-ν)/2]
```

**平面应变**：
```
[D] = E/((1+ν)(1-2ν)) × [1-ν  ν    0]
                         [ν    1-ν  0]
                         [0    0    (1-2ν)/2]
```

### 初始应力

**平面应力**：
```
σ = E/(1-ν²) × (1+ν) × ε_0
```

**平面应变**：
```
σ = E/((1+ν)(1-2ν)) × (1+2ν) × ε_0
```

其中：`ε_0 = α×ΔT + β_c×ΔSOC`

## 🔢 数值示例

**假设**：E = 100 GPa, ν = 0.3, ε_0 = 0.001

| 参数 | 平面应力 | 平面应变 | 比值 |
|------|---------|---------|------|
| 弹性模量因子 | 109.9 GPa | 134.6 GPa | 1.22× |
| 初始应力系数 | 1.3 | 1.6 | 1.23× |
| **总应力** | **143 MPa** | **215 MPa** | **1.51×** |

**结论**：平面应变的应力约为平面应力的 **1.5倍**

## 🔬 物理解释

### 平面应力（软包电池）

```
初始状态:  ████████
            ↓ 温升/充电
膨胀后:    ██████████  ← 厚度增加
           ←自由膨胀→
```

- z方向可以自由变形
- σ_zz = 0
- 应力释放，数值较小

### 平面应变（硬壳电池）

```
初始状态:  ┌────────┐
           │████████│
           └────────┘
           ↓ 温升/充电
约束后:    ┌────────┐
           │████████│ ← 厚度固定
           └────────┘
           ← 受约束 →
```

- z方向被壳体约束
- ε_zz = 0，但 σ_zz ≠ 0
- 应力积累，数值较大

## ⚠️ 注意事项

### 1. 泊松比限制

| 平面类型 | 泊松比范围 | 说明 |
|---------|-----------|------|
| 平面应力 | 0 < ν < 0.5 | 可接近0.5 |
| 平面应变 | 0 < ν < 0.45 | **超过0.45会不稳定** |

### 2. Von Mises 应力

**平面应力**：
```julia
σ_vm = sqrt(σ_xx² + σ_yy² - σ_xx×σ_yy + 3σ_xy²)
```

**平面应变**：
```julia
σ_zz = ν(σ_xx + σ_yy)  # 需计算
σ_vm = sqrt(σ_xx² + σ_yy² + σ_zz² - σ_xx×σ_yy - σ_yy×σ_zz - σ_zz×σ_xx + 3σ_xy²)
```

### 3. 输出差异

| 变量 | 平面应力 | 平面应变 |
|------|---------|---------|
| `σ_xx, σ_yy, σ_xy` | ✅ | ✅ |
| `σ_zz` | ❌ (=0) | ✅ 计算 |
| `σ_vm` | 2D公式 | 3D公式 |

## 🎓 应用建议

### 软包电池（平面应力）

```julia
opt.plane_type = :stress

# 特点：
# - 厚度 < 10 mm
# - 铝塑膜外壳
# - 允许自由膨胀
# - 应力较小（~100 MPa）
```

### 圆柱电池（平面应变）

```julia
opt.plane_type = :strain

# 特点：
# - 18650, 21700等
# - 钢壳约束
# - 厚度方向受限
# - 应力较大（~200 MPa）
```

### 方形电池（混合）

对于中等厚度（10-20 mm）的方形电池：
- 如果壳体很硬 → `:strain`
- 如果壳体柔性 → `:stress`
- 需要根据实际约束判断

## 📊 验证方法

### 1. 检查应力比

```julia
σ_ratio = mean(σ_strain) / mean(σ_stress)
# 期望值：1.5 - 2.5 (取决于 ν)
```

### 2. 检查 z 方向应力

```julia
# 平面应力
@assert !haskey(variables, "diffusion stress zz")

# 平面应变
@assert haskey(variables, "diffusion stress zz")
σ_zz = variables["diffusion stress zz"]
# σ_zz 应该约为 ν×(σ_xx + σ_yy)
```

### 3. 能量守恒

```julia
# 应变能密度
U_stress = 0.5 × (σ_xx×ε_xx + σ_yy×ε_yy + σ_xy×γ_xy)
U_strain = 0.5 × (σ_xx×ε_xx + σ_yy×ε_yy + σ_xy×γ_xy + σ_zz×0)
# U_strain > U_stress（约束增加应变能）
```

## 📚 相关文档

1. **详细理论**: `docs/Plane_Stress_vs_Plane_Strain.md`
   - 完整推导
   - 弹性矩阵
   - 修改位置

2. **使用示例**: `example/plane_strain_example.jl`
   - 对比计算
   - 可视化

3. **实现代码**: `src/mechanical.jl`
   - 函数：`diffusion_stress_2D()`
   - 支持选项：`:stress` 或 `:strain`

## 🔍 常见问题

### Q1: 何时使用平面应变？

**A**: 当结构在垂直方向受到约束时：
- 圆柱电池的横截面分析
- 硬壳电池（金属壳）
- 电池模组中间层（上下受压）
- 长条形结构的截面

### Q2: 应力差异有多大？

**A**: 通常平面应变的应力是平面应力的 **1.5-2.5倍**：
- ν = 0.2: 比值 ≈ 1.44
- ν = 0.3: 比值 ≈ 1.51
- ν = 0.4: 比值 ≈ 1.67

### Q3: 如何选择？

**A**: 简单判断规则：
1. 厚度/宽度 < 0.1 → 平面应力
2. 厚度/宽度 > 1 → 平面应变
3. 0.1 < 厚度/宽度 < 1 → 根据约束判断

### Q4: 平面应变时 ν 不能超过 0.45？

**A**: 是的，因为分母 (1-2ν) → 0 当 ν → 0.5：
```julia
D_factor = E / ((1+ν)×(1-2ν))
# ν = 0.45: D_factor = E / (1.45 × 0.1) = 6.9E ✓
# ν = 0.49: D_factor = E / (1.49 × 0.02) = 33.6E ⚠️
# ν = 0.50: D_factor = E / (1.50 × 0.0) = ∞ ❌
```

## ✅ 检查清单

使用前检查：
- [ ] 确定结构类型（薄板/厚体）
- [ ] 检查边界约束（自由/固定）
- [ ] 验证泊松比范围（< 0.45 for strain）
- [ ] 设置 `case.opt.plane_type`
- [ ] 运行计算
- [ ] 检查应力量级（是否合理）
- [ ] 验证 σ_zz（仅平面应变）

---

**参考**:
- 详细文档: `docs/Plane_Stress_vs_Plane_Strain.md`
- 示例代码: `example/plane_strain_example.jl`
- 理论推导: `docs/Diffusion_Stress_Macroscale_Theory.md`
