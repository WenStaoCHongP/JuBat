# 其余文件优化方案汇总

> 日期: 2026-04-01 (修订)
> 约束: 非必要不新增函数，不使用 `_` 前缀

---

## A. Jellyrollmodel.jl (558 行, 新增)

### 问题
`jellyroll_tab_node_indices` 中正/负极耳的二元搜索逻辑完全重复 (~60 行)。

### 优化方案

**保持内联，不提取子函数。** 仅在注释中标注重复区域：

```julia
# ── 正极耳搜索 (与下方负极耳逻辑对称，未提取以避免新增函数) ──
# ... 正极耳搜索 ~30 行 ...

# ── 负极耳搜索 (与上方正极耳逻辑对称) ──
# ... 负极耳搜索 ~30 行 ...
```

类型替换：`case.multi_spme_layout["key"]` → `case.geometry.key`

---

## B. SPMe.jl (202 行, 修改)

### 问题
`SPMe()` (标准路径) 与 `SPMe_element()` (单元路径) 有 ~60% 重复逻辑。

### 优化方案

**保持内联，不提取公共函数。** 两个函数各自保留完整逻辑。

类型替换：
- `case.multi_spme_layout["key"]` → `case.layout.key`
- `variables["key"]` 键名保持不变（向后兼容）

### 约束
- `SPMe()` main 分支逻辑不动
- `SPMe_variables()` 不动

---

## C. CycleSolver.jl (716 行, 新增)

### 问题
1. 主函数 `solve_cycling` 过长 (~264 行, 行 209-472)
2. CZM 更新逻辑内嵌
3. `_ensure_multi_spme_layout!` 在此定义（应被 `MultiSPMeLayout` 构造器替代）

### 优化方案

**保持 `solve_cycling` 内联，不拆分。** 仅做：

1. **`_ensure_multi_spme_layout!` (行 695) 替换**：

```julia
# 旧 (28 行):
function _ensure_multi_spme_layout!(case::Case)
    # ... 计算 ne, n_chem, nT ...
    empty!(case.multi_spme_layout)
    case.multi_spme_layout["ne"] = ne
    # ... 6 行 Dict 赋值 ...
end

# 新 (~3 行):
# 所有调用点改为:
if case.layout === nothing
    case.layout = MultiSPMeLayout(ne, n_chem, nT)
end
```

2. **`_compute_czm_effective_params` / `_compute_czm_strain_inputs` / `_update_czm_damage!`**:

这三个函数有 `_` 前缀，按规范需要重命名：

| 旧名 | 新名 |
|------|------|
| `_compute_czm_effective_params` | `compute_czm_effective_params` |
| `_compute_czm_strain_inputs` | `compute_czm_strain_inputs` |
| `_update_czm_damage!` | `update_czm_damage!` |

这些都是已有函数的重命名，不是新增。

3. **类型替换**：`case.multi_spme_layout["key"]` → `case.layout.key`

---

## D. CycleData.jl (623 行, 新增)

### 优化方案
- 结构良好，不做改动
- 类型替换：layout Dict 访问改为 struct 字段

---

## E. czm.jl (504 行, 新增) + CzmSolve.jl (515 行, 新增)

### czm.jl
- **不做改动**。结构良好。

### CzmSolve.jl
- 3 种迭代方法的分派保持 if/elseif，不改为函数分派表（避免新增函数）
- 类型替换：涉及 layout 的地方

---

## F. Materialmatrix.jl (376 行, 新增)

### 优化方案
保持原样。混合热材料矩阵和 CZM 双线性律是历史设计，不拆分。

---

## G. mechanical.jl (355 行, 修改)

### 优化方案
- main 分支 `Mechanicaloutput()` 不动
- 新增的 `thermal_diffusion_stress_2D` 保留

---

## H. 基础设施文件

### H1. JuBat.jl (74 行, 修改)

变更：新增 2 个 include（去掉 ThermalHeatSource.jl 和 CouplingInterface.jl，因为未新建）：

```julia
# 新增:
include("CouplingState.jl")     # 在 Initialisation.jl 之前
include("CallModel.jl")         # 在 Solve.jl 之前
```

其余 include 顺序调整按依赖图（02 文档 § 3）。

### H2-H5. 不改动的文件

| 文件 | 状态 | 理由 |
|------|------|------|
| `SetMesh.jl` | 不动 | mesh 构建逻辑不涉及 layout |
| `SetParams.jl` | 不动 | 参数构建不涉及 layout |
| `Tools.jl` | 不动 | 辅助函数结构良好 |
| `Option.jl` | 不动 | 选项定义清晰 |

---

## I. 参数文件 (不动)

| 文件 | 状态 |
|------|------|
| `parameters/Jellyroll.jl` | 不动 |
| `parameters/Ring.jl` | 不动 |
| `parameters/LGM50.jl` | 不动 |
| `parameters/Enertech.jl` | 不动 |
| `parameters/Northrop.jl` | 不动 |

---

## J. 轻量文件 (不动)

| 文件 | 状态 | 理由 |
|------|------|------|
| `SPM.jl` | 不动 | 无需优化 |
| `P2D.jl` | 不动 | 已优化完毕 |
| `Thermal.jl` | 不动 | SPMe lumped 热扩展清晰 |
| `ThermalPolar2D.jl` | 不动 | 结构清晰 |
| `ring.jl` | 不动 | 简单网格生成器 |

---

## K. 果冻卷二维 SPMe + 分布式热 + CZM 参数传递过程分析

本节聚焦 `SetCase/Initialisation/Solve/CallModel/ThermalDistributed/CycleSolver/CzmSolve` 之间的参数流，目的是识别可优化的传参链路与热点。

### K1. 初始化阶段（几何 + 状态）

1. `SetCase` 负责创建无量纲参数 `case.param`、基础电化学网格与索引（`src/SetCase.jl`）。
2. `setup_thermal2D_mesh` 将果冻卷热网格写入 `case.mesh["thermal2D"]`，并将 `layer_weights/interface_pairs/czm_element_map` 写入 `case.geometry`（`src/Jellyrollmodel.jl`）。
3. `ModelInitialisation_MultiSPMe` 生成全局初值 `y0 = [y0_chem_all; T0_nodes]`，并缓存 `case.layout = MultiSPMeLayout(ne, n_chem, nT)`（`src/Initialisation.jl`）。

### K2. 时间推进阶段（Solve → CallModel）

1. `Solve` 在主循环中从上一步 `T_nodes_carry` 计算体积平均温度 `Tm`，回写 `case.param.cell.T0` 影响电化学动力学（`src/Solve.jl`）。
2. `CallModel` 根据 `opt.model == "SPMe" && opt.thermalmodel == "distributed2D"` 分派到 `CallModel_MultiSPMe`（`src/CallModel.jl`）。
3. `CallModel_MultiSPMe` 利用 `case.layout` 将 `yt` 拆分为：
    - 每单元电化学状态 `yt_chem[e]`
    - 热自由度 `T_nodes`
4. 同函数内计算 `areas` 与 `Te_prev`，并将 `I_total/T_nodes/areas` 写入 `variables`，作为分流求解输入。

### K3. 电化学-热耦合阶段（分流 + 逐单元 SPMe + 热源）

1. `solve_branch_currents` 使用 `Te_prev + prefactors + coeffs` 求解 `I_e` 与公共电压 `V`，同时输出截止/失效相关掩码（`src/Parallelsolution.jl`）。
2. `Threads.@threads for e in 1:ne` 调用 `SPMe_element(case, yt_chem[e], ...; I_e=I_e[e], T_e=Te_prev[e])`，完成逐单元电化学更新（`src/CallModel.jl`, `src/SPMe.jl`）。
3. `compute_heat_sources` 将 `variables_elems + I_e + T_e + areas + layer_weights` 转成：
    - `heat_source_fields`
    - 分层热源项（`thermal2D q_rxn_ne/.../q_ncc`）
    - `total heat source`
    （`src/ThermalDistributed.jl`）。

### K4. 热-CZM 双向耦合阶段

1. `ThermalDistributed2D` 读取 `variables["heat_source_fields"]` 与 `case.geometry.layer_weights` 组装 `MT/KT/FT`（`src/ThermalDistributed.jl`）。
2. `ThermalDistributed2D_BC` 遍历 `case.czm_mesh.damage_states`，通过 `compute_gap_conductance(D, δ_n, ...)` 把损伤反馈到导热边界（`src/ThermalDistributed.jl`）。
3. `solve_phase` 阶段末调用 `update_czm_damage!`；后者从 `variables + T_nodes_carry` 提取 `dT_elem/Δsoc_n_elem/Δsoc_p_elem`，传入 `solve_czm_step` 更新损伤（`src/CycleSolver.jl`, `src/CzmSolve.jl`）。

### K5. 跨循环状态传递

`solve_cycling` 通过 `current_state["y"]` 与 `current_state["T_nodes"]` 在放电/静置/充电/静置各相位之间传递状态，实现“电化学状态 + 热场 + 损伤状态”的连续继承（`src/CycleSolver.jl`）。

### K6. 优化关注点（参数传递链路）

1. `case.param.cell.T0` 在 `Solve` 内每步回写，属于全局可变参数；建议后续收敛为 `variables["temperature_ref"]` 形态，减小隐式副作用。
2. `variables` 中同时承载物理量、诊断量、掩码量，键空间较大；建议按 `electrochem/thermal/czm/diagnostics` 分组管理。
3. `CallModel_MultiSPMe` 中“解析状态 → 分流 → 并行 SPMe → 热组装”路径较长，后续可考虑引入轻量中间态结构以减少重复字段写回。

---

## L. 循环嵌套循环定位（显式 + 隐式，热点清单）

> 说明：本节同时统计
> 1) 显式嵌套：函数体内可直接看到的 for 嵌套。
> 2) 隐式嵌套：调用链、推导式、生成器表达式导致的“外层循环 + 内层循环内核”。

| # | 文件 | 函数 | 循环嵌套位置 | 嵌套层级 | 热点 |
|---|------|------|--------------|----------|------|
| 1 | `src/Assemble.jl` | `Assemble` | 行 16 (`for i`) -> 行 17 (`for j`) | 2 层 | 高 |
| 2 | `src/ThermalDistributed.jl` | `apply_convection_bc` | 行 68 (`for e`) -> 行 70 (`for (a,b)`) -> 行 85 (`for (s,w)`) | 3 层 | 高 |
| 3 | `src/ThermalDistributed.jl` | `apply_cool_method` (`surface`) | 行 126 (`for g`) -> 行 130 (`for i`) -> 行 133 (`for j`) | 3 层 | 高 |
| 4 | `src/ThermalDistributed.jl` | `ThermalDistributed2D_BC` | 行 195 (`for (elem_idx,...)`) -> 行 206 (`for (nb,nt)`) | 2 层 | 中 |
| 5 | `src/Parallelsolution.jl` | `newton_iteration` | 行 223 (`for iter`) -> 行 227 (`for e`) | 2 层 | 高 |
| 6 | `src/Parallelsolution.jl` | `line_search` | 行 300 (`for attempt`) -> 行 307 (`for e`) | 2 层 | 高 |
| 7 | `src/CzmSolve.jl` | `newton_raphson_czm` | 行 63 (`for load_step`) -> 行 68 (`for iter`) | 2 层 | 高 |
| 8 | `src/czm.jl` | `create_czm_mesh` | 行 65 (`for n_out`) -> 行 69 (`for n_in`) | 2 层 | 中 |
| 9 | `src/czm.jl` | `assemble_czm_system` | 行 171 (`for (i,elem)`) -> 行 200 (`for (ξ,w)`) | 2 层 | 高 |
| 10 | `src/czm.jl` | `assemble_czm_system` | 行 259 (`for a`) -> 行 261 (`for b`) | 2 层 | 高 |
| 11 | `src/czm.jl` | `assemble_bulk_stiffness` | 行 305 (`for e`) -> 行 317 (`for i`) | 2 层 | 中 |
| 12 | `src/czm.jl` | `assemble_bulk_stiffness` | 行 335 (`for a`) -> 行 336 (`for b`) | 2 层 | 中 |
| 13 | `src/Jellyrollmodel.jl` | `jellyroll_collector_seed_mesh` | 行 85 (`for n_out`) -> 行 88 (`for n_in`) | 2 层 | 中（初始化） |
| 14 | `src/Jellyrollmodel.jl` | `jellyroll_collector_seed_mesh` | 行 156 (`for i`) -> 行 159 (`for j`) | 2 层 | 中（初始化） |
| 15 | `src/CycleSolver.jl` | `compute_czm_strain_inputs` | 行 588 (`for e`) -> 行 593 (`for n`) | 2 层 | 中 |
| 16 | `src/Mechanical.jl` | `thermal_diffusion_stress_2D` | 行 219 (`for g`) -> 行 221 (`for i`) | 2 层 | 中 |

### L1. 可优先优化的嵌套循环（建议）

1. `Assemble` 与 `apply_cool_method(surface)`：可优先评估向量化/预装配缓存（高频且与网格规模强相关）。
2. `newton_iteration` 与 `newton_raphson_czm`：可优先减少迭代内重复计算项（高频迭代内核）。
3. `assemble_czm_system`：双层与块组装共存，建议优先检查稀疏索引构建开销与临时数组分配。

### L2. 隐式嵌套循环（调用链/推导式/生成器）

| # | 类型 | 外层位置 | 内层循环内核 | 嵌套说明 | 热点 |
|---|------|----------|--------------|----------|------|
| 1 | 调用链 | `src/CallModel.jl:86` `Threads.@threads for e in 1:ne` | `src/SPMe.jl:37` `SPMe_element`（内部装配/积分） | 外层单元并行循环 + 单元内离散循环 | 高 |
| 2 | 调用链 | `src/CallModel.jl:77` `solve_branch_currents(...)` | `src/Parallelsolution.jl:200` `newton_iteration`（`iter->e`） | 每步模型调用触发非线性迭代内核 | 高 |
| 3 | 调用链 | `src/CallModel.jl:124` `ThermalDistributed2D(...)` | `src/Assemble.jl:16-17` `for i -> for j` | 热学组装入口调用到双层稀疏装配循环 | 高 |
| 4 | 调用链 | `src/CallModel.jl:128` `ThermalDistributed2D_BC(...)` | `src/ThermalDistributed.jl:195-206` | 边界条件入口调用到界面单元-节点对双层循环 | 中 |
| 5 | 调用链 | `src/CallModel.jl:102` `compute_heat_sources_with_czm(...)` | `src/ThermalDistributed.jl:403,411` | 活跃单元过滤后再全单元清零扫描 | 中 |
| 6 | 调用链 | `src/Parallelsolution.jl:397` `newton_iteration(...)` | `src/Parallelsolution.jl:300,307` `line_search` | Newton 外迭代内再进入 attempt->e 双层循环 | 高 |
| 7 | 调用链 | `src/CzmSolve.jl:63` `for load_step` | `src/CzmSolve.jl:71` `assemble_coupled_system(...)` -> `src/czm.jl:305,317` | 载荷步循环内调用体单元装配循环 | 高 |
| 8 | 调用链 | `src/CzmSolve.jl:68` `for iter` | `src/CzmSolve.jl:71` `assemble_coupled_system(...)` -> `src/czm.jl:171,200` | Newton 迭代内调用内聚单元积分循环 | 高 |
| 9 | 推导式 | `src/CallModel.jl:143` `[variables_elems[e][...] for e in 1:ne]` | 隐式 `for e` | 结果收集阶段的隐式循环 | 低 |
| 10 | 推导式 | `src/Parallelsolution.jl:391-392` `mean([ ... for e in ...])` | 隐式 `for e` | 电压初始化使用推导式循环 | 中 |
| 11 | 生成器 | `src/Parallelsolution.jl:401` `sum(w[e] * I_e[e] for e in active_idx)` | 隐式 `for e` | 归一化阶段生成器循环 | 中 |
| 12 | 生成器 | `src/CycleSolver.jl:120-121` `maximum/mean(... for s in ...)` | 隐式 `for s` | 阶段入口损伤统计隐式循环 | 低 |
| 13 | 生成器 | `src/Mechanical.jl:318-320` `sum(... for i in 1:4)` | 隐式 `for i` | 应变恢复中三处生成器循环 | 中 |

### L3. 统计口径与总量

1. 显式嵌套循环：16 处（见 L 主表）。
2. 隐式嵌套循环：13 处（见 L2）。
3. 合计嵌套循环：29 处。

> 注：若同一调用链在不同时间步/相位重复触发，按“结构位置”计 1 处，不按运行次数累计。

---

## M. 嵌套循环优化建议（表格）

| 优先级 | 目标位置 | 嵌套类型 | 优化建议 | 预期收益 | 实施风险/注意事项 |
|---|---|---|---|---|---|
| P0 | `src/Assemble.jl:16-17` | 显式 | 预分配 `KI/KJ/KV` 后，评估按块写入与缓存友好顺序；减少重复索引搬运 | 矩阵装配耗时下降（高频核心） | 需保证稀疏矩阵索引顺序与数值一致性 |
| P0 | `src/ThermalDistributed.jl:126-133` | 显式 | `apply_cool_method(surface)` 中对 `Ni[g,i]`、`wJ[g]` 做局部缓存，减少三层循环中的重复读取 | 边界装配开销下降 | 不改变边界物理意义与符号约定 |
| P0 | `src/Parallelsolution.jl:223-227` + `300-307` | 显式+隐式调用链 | `newton_iteration` 与 `line_search` 共用中间量（`F/dFdI`）并减少重复计算；必要时设置收敛早停条件 | 分流迭代总步数与单步开销下降 | 需回归验证截止电压判据不漂移 |
| P0 | `src/CzmSolve.jl:63-68` -> `src/czm.jl:171,200` | 隐式调用链 | 将 Newton 迭代内不随迭代变化的几何项/映射项前置缓存；减少 `assemble_coupled_system` 重复构造 | CZM 非线性求解耗时下降 | 需确认缓存失效条件（损伤更新后是否需重建） |
| P1 | `src/ThermalDistributed.jl:68-85` | 显式 | 对边界边集合去重后持久化（当前 `seen` 每次构建），可在网格初始化阶段缓存边列表 | 重复边扫描开销下降 | 增加少量内存占用 |
| P1 | `src/czm.jl:259-261`, `335-336` | 显式 | 8x8 局部矩阵组装可尝试静态小矩阵策略或减少 push! 次数（先计数后一次性写入） | 稀疏条目构建开销下降 | 代码可读性可能下降，需注释 |
| P1 | `src/CallModel.jl:86` -> `src/SPMe.jl:37` | 隐式调用链 | 单元并行路径中减少临时分配（如 `vec`/短生命周期数组），必要时用 `@views` | 并行效率提升、GC 压力下降 | 需核对线程安全与别名写入 |
| P1 | `src/ThermalDistributed.jl:403,411` | 隐式调用链 | `compute_heat_sources_with_czm` 的 active mask 过滤改为单次遍历完成（计算+过滤合并） | 热源后处理开销下降 | 逻辑合并后需保证结果键完整 |
| P2 | `src/CallModel.jl:143`、`src/Parallelsolution.jl:391-392,401` | 推导式/生成器 | 在热点路径将推导式替换为显式循环并复用缓冲区，减少临时对象 | 小幅降低分配与抖动 | 收益依赖问题规模，优先级次于 P0/P1 |
| P2 | `src/Mechanical.jl:318-320` | 生成器 | 将 3 个 `sum(... for i in 1:4)` 合并为单次 `for i in 1:4` 累加 | 小幅减少指令与分配 | 必须保持应变定义公式一致 |
| P2 | `src/CycleSolver.jl:120-121` | 生成器 | 阶段入口统计可与已有损伤遍历合并，避免重复扫描 | 微小收益 | 对整体总耗时影响有限 |

### M1. 建议落地顺序

1. 先做 P0（装配内核 + 非线性迭代内核），这些位置在每步/每迭代重复触发。
2. 再做 P1（调用链上的重复构造与数据搬运）。
3. 最后做 P2（推导式/生成器微优化），作为收尾优化。
