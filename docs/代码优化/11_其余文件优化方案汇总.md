# 其余文件优化方案汇总

> 日期: 2026-04-01
> 说明: 以下文件变更量较小或为参数适配性修改，合并为一个文档

---

## A. Jellyrollmodel.jl (559 行, 新增)

### 问题
`jellyroll_tab_node_indices` 中正/负极耳的二元搜索逻辑完全重复 (~60 行)。

### 优化方案

```julia
# 旧: 两个独立代码块搜索正极耳和负极耳
# 行 405-434: 正极耳搜索 (30 行)
# 行 449-478: 负极耳搜索 (30 行)

# 新: 提取通用搜索函数
function _find_tab_nodes_by_arc(mesh2d, theta_target::Float64;
                                  search_direction=:cw)
    # ~15 行通用二元搜索
    coords = mesh2d.node
    n_nodes = size(coords, 1)

    # 计算所有节点的极角
    angles = atan.(coords[:, 2], coords[:, 1])
    # 沿 search_direction 搜索最近节点
    ...
    return node_indices, arc_lengths
end

# 主函数调用:
function jellyroll_tab_node_indices(case)
    theta_pos = case.param.cell.theta_pos_tab
    theta_neg = case.param.cell.theta_neg_tab
    tab_pos = _find_tab_nodes_by_arc(mesh2d, theta_pos; search_direction=:cw)
    tab_neg = _find_tab_nodes_by_arc(mesh2d, theta_neg; search_direction=:ccw)
    return tab_pos, tab_neg
end
```

**预期**: 消除 ~30 行重复代码。`jellyroll_collector_seed_mesh` (178 行) 暂不拆分（逻辑紧密耦合）。

---

## B. SPMe.jl (202 行, 修改)

### 问题
`SPMe()` (标准路径) 与 `SPMe_element()` (单元路径) 有 ~60% 重复逻辑。

### 优化方案

提取共享的矩阵装配逻辑：

```julia
# 新增内部函数
function _assemble_spme_matrices(case, yt_e, t; I_e=nothing, T_e=nothing)
    """SPMe 和 SPMe_element 共享的矩阵装配核心"""
    # 从当前 SPMe() 和 SPMe_element() 中提取公共逻辑
    # ~40 行
    return M, K, F
end

# SPMe 主函数调用:
function SPMe(case, yt, t; jacobi)
    M, K, F = _assemble_spme_matrices(case, yt, t)
    variables = SPMe_variables(case, yt, t)
    return M, K, F, variables
end

# SPMe_element 调用:
function SPMe_element(case, yt, t, e; I_e, T_e)
    M, K, F = _assemble_spme_matrices(case, yt, t; I_e=I_e, T_e=T_e)
    variables = SPMe_variables(case, yt, t; I_e=I_e, T_e=T_e)
    return M, K, F, variables
end
```

### 约束
- `SPMe()` main 分支逻辑不动，仅提取公共部分
- `SPMe_variables()` 不动（已足够清晰）

---

## C. CycleSolver.jl (730 行, 新增)

### 问题
1. 主函数 `solve_cycling` 过长 (~200 行)
2. CZM 更新逻辑内嵌
3. `_ensure_multi_spme_layout!` 在此定义（应移到 CouplingState.jl）

### 优化方案

```julia
# 拆分主函数为:
function solve_cycling(case, cycle_opt)
    result = _init_cycling_result(cycle_opt)
    initial_capacity = 0.0
    soh_terminated = false

    for cycle in 1:cycle_opt.n_cycles
        # 充电相
        charge_result = _solve_phase(case, cycle_opt, PHASE_CHARGE, cycle)
        # 休息相 1
        rest1_result = _solve_phase(case, cycle_opt, PHASE_REST, cycle)
        # 放电相
        discharge_result = _solve_phase(case, cycle_opt, PHASE_DISCHARGE, cycle)
        # 休息相 2
        rest2_result = _solve_phase(case, cycle_opt, PHASE_REST, cycle)

        # CZM 更新
        _update_czm_damage!(case)

        # 汇总
        cycle_result = _postprocess_cycle(charge_result, rest1_result,
                                            discharge_result, rest2_result, case)
        _append_result!(result, cycle, cycle_result)

        # SOH 检查
        initial_capacity, current_soh = _update_soh!(result, cycle, cycle_result, initial_capacity)
        terminated, soh_terminated = _check_termination(cycle, cycle_result, case, current_soh, cycle_opt)
        terminated && break
    end

    _print_summary(result, initial_capacity, soh_terminated)
    return result
end

"""单相求解"""
function _solve_phase(case, cycle_opt, phase_type, cycle)
    # ~30 行: 设置电流/时间 → Solve() → 后处理
end
```

### `_ensure_multi_spme_layout!` 迁移
此函数移至 `CouplingState.jl`，或被 `MultiSPMeLayout` 构造器替代。

---

## D. CycleData.jl (623 行, 新增)

### 问题
数据导出/加载功能独立完整，无结构性问题。

### 优化方案
- 仅做命名统一：`export_cycle_data_to_csv` → `export_cycle_data`
- CSV 文件名常量化
- 不做结构性改动

---

## E. czm.jl (504 行, 新增) + CzmSolve.jl (515 行, 新增)

### 优化方案
- **czm.jl**: 结构良好，不做改动。`create_czm_mesh` 和 `assemble_czm_system` 职责清晰。
- **CzmSolve.jl**: 3 种迭代方法的分派可简化为：

```julia
# 旧: 手动 if/elseif 分派
if case.opt.czm_iter_method == "basic"
    ...
elseif case.opt.czm_iter_method == "load_substep"
    ...
elseif case.opt.czm_iter_method == "arc_length"
    ...
end

# 新: 函数分派表
const CZM_SOLVERS = Dict(
    "basic" => newton_raphson_czm,
    "load_substep" => czm_load_substep,
    "arc_length" => czm_arc_length,
)

function solve_czm_step(czm_mesh, case; method=case.opt.czm_iter_method)
    solver = get(CZM_SOLVERS, method) do
        error("Unknown CZM solver: $method")
    end
    return solver(czm_mesh, case)
end
```

---

## F. Materialmatrix.jl (376 行, 新增)

### 问题
混合了两种完全不同的职责：
1. 热材料矩阵（各向异性导热、体积热容）
2. CZM 双线性律（牵引-分离、损伤演化、界面热导）

### 优化方案

```julia
# 旧: Materialmatrix.jl 同时包含
# - thermal_capacity_weights_2d()
# - thermal_anisotropic_conductivity_2d()
# - bilinear_traction_state()
# - bilinear_tangent()
# - update_damage()
# - compute_gap_conductance()

# 新: 拆分为两段注释区（不新增文件，因关联紧密）
# ═══ 第一段: 热材料矩阵 ═══
function thermal_capacity_weights_2d(...) end
function thermal_anisotropic_conductivity_2d(...) end

# ═══ 第二段: CZM 本构 ═══
function bilinear_traction_state(...) end
function bilinear_tangent(...) end
function update_damage(...) end
function compute_gap_conductance(...) end
```

---

## G. mechanical.jl (355 行, 修改)

### 优化方案
- main 分支 `Mechanicaloutput()` 不动
- 新增的 `thermal_diffusion_stress_2D` 保留
- 仅重命名：`Mechanicaloutput` → `mechanical_output`（符合命名规范，但需检查所有调用点）

### 约束
由于 main 分支 `Mechanical()` 函数被重命名为 `Mechanicaloutput()`，此重命名已在当前分支完成。不再二次重命名。

---

## H. 基础设施文件

### H1. JuBat.jl (74 行, 修改)

变更：新增 4 个 include + export。

```julia
# 新增:
include("CouplingState.jl")     # 在 Initialisation.jl 之前
include("CallModel.jl")         # 在 Solve.jl 之前
include("ThermalHeatSource.jl") # 在 ThermalDistributed.jl 之前
include("CZMInterface.jl")      # 在 czm.jl 之后
```

其余 include 顺序调整按依赖图（02_全局架构与约束.md § 3）。

### H2. SetMesh.jl (739 行, 修改)

main 分支 mesh 构建逻辑不动。仅影响：
- `Mesh2D`, `ShapeFunction2D`, `CohesiveMesh` 等新增类型保留
- 无需修改

### H3. SetParams.jl (440 行, 修改)

main 分支参数构建不动。仅影响：
- `Cohesive` 结构体、`Scale` 扩展、`Cell` 扩展保留
- `NormaliseParam` 中 `Base.invokelatest` 修复保留

### H4. Tools.jl (191 行, 修改)

新增的 `IntQ4`、`identify_boundary_nodes`、`compute_separation`、`element_nodal_mean`、`thermal2D_volume_average_temperature` 等函数结构良好，不做修改。

### H5. Option.jl (81 行, 修改)

main 分支 `Option` 基础字段不动。新增的 `CycleOption`、`PhaseType`、热学/CZM/debug 字段保留。

---

## I. 参数文件 (轻量适配)

| 文件 | 变更 | 优化方案 |
|------|------|---------|
| `parameters/Jellyroll.jl` | 新增 180 行 | 不动，参数定义不需要重构 |
| `parameters/Ring.jl` | 新增 66 行 | 不动 |
| `parameters/LGM50.jl` | +3 行 `Cohesive()` | 不动 |
| `parameters/Enertech.jl` | +1 行 `Cohesive()` | 不动 |
| `parameters/Northrop.jl` | +1 行 `Cohesive()` | 不动 |

---

## J. 轻量文件 (无改动)

| 文件 | 状态 | 理由 |
|------|------|------|
| `SPM.jl` | 仅空白清理 | 无需优化 |
| `P2D.jl` | -5 行简化 | 已优化完毕 |
| `Thermal.jl` | +31 行 | SPMe lumped 热扩展，结构清晰 |
| `ThermalPolar2D.jl` | 新增 120 行 | 结构清晰 |
| `ring.jl` | 新增 83 行 | 简单网格生成器 |
