# 代码优化分析进度

> 约束：每次提出优化方案时，必须在此文件做简洁记录。
> **注意**: 行号引用基于代码快照，实施时请以函数名搜索定位。

---

## 2026-04-01 全局架构重构 + 结构体重构

**方案**: 激进重构（方案 A），允许新增文件，main 分支代码不动。

| 编号  | 文件                    | 变更类型 | 要点                                                                           |
| --- | --------------------- | ---- | ---------------------------------------------------------------------------- |
| 02  | 全局架构与约束               | 新增   | 4 个新文件规划 (CouplingState/CallModel/ThermalHeatSource/CouplingInterface)，依赖图，命名规范   |
| 03  | 结构体重构方案               | 新增   | `MultiSPMeLayout` + `MeshGeometry` struct 替代 `Dict{String,Any}`，消除 4 处重复布局构建 |
| 04  | SetCase_jl            | 修改   | Case 新增 3 个 Union{Nothing,T} 字段替代 1 个 Dict，便捷构造器兼容 main                      |
| 05  | Initialisation_jl     | 修改   | 辅助函数简化（layout 字段直接访问），非均匀 SOC 应用逻辑提取                            |
| 06  | Variables_jl          | 修改   | distributed2D/CZM 变量块提取为子函数，`Variable_update!` 拆分                            |
| 07  | PostProcessing_jl     | 修改   | distributed2D 热源输出提取子函数，cycling helpers 分组标注                                 |
| 08  | Solve_jl              | 修改   | 717→~200 行，拆出 CallModel.jl (~200行)，纯步进器恢复                                  |
| 09  | Parallelsolution_jl   | 修改   | 主函数 152→~80 行，`CutoffInfo` struct 替代 NamedTuple                              |
| 10  | ThermalDistributed_jl | 修改   | `compute_heat_sources` 迁出为 ThermalHeatSource.jl (~180行)                      |
| 11  | 其余文件汇总                | 新增   | Jellyrollmodel 去重 / SPMe 公共逻辑提取 / CZM 分派表 / 参数文件不动                           |


**核心约束**: main 分支 SPM/SPMe/P2D 路径代码不动，仅重构 Parameters_Design 新增部分。

**命名约束**: 不使用 `_` 为前缀的内部函数；新增函数需经确认。

---

## 2026-04-01 深度审查追加 (47 个问题)

**方法**: 并行审查全部 22 个源文件，按 6 类问题统计。

### 新增决策

| # | 决策 | 结论 |
|---|------|------|
| 1 | `SimulationState` struct 放哪里 | 合并到 `CouplingState.jl`（与 MultiSPMeLayout、MeshGeometry 同文件） |
| 2 | 耦合泄漏 (12 处) 如何处理 | 新建通用耦合接口 `CouplingInterface.jl`，统一处理热→电、电→热、CZM→分流等数据传递 |
| 3 | ThermalDistributed2D vs Ring 90% 重复 | 提取公共 FEM 装配核心 + 坐标映射函数参数化 |
| 4 | GetGS + GSweight 重构 | **不重构**（结构清晰，维持现状） |
| 5 | 温度变量命名不一致 (6 种) | **仅新增代码统一**为 `T_nodes`/`T_elements`/`T_avg`，main 分支不动 |
| 6 | ThermalLumped 是否保留 | **保留不动**（main 分支 SPM/SPMe 路径依赖它） |

### 审查发现的严重问题 (需在方案中补充)

| 问题 | 位置 | 严重度 | 影响的优化方案 |
|------|------|--------|--------------|
| `czm_mesh` 变量作用域不清，可能未定义就使用 | Solve.jl:531 | **高 (潜在 bug)** | 08_Solve |
| `case.param.cell.T0` 在循环中被直接修改 | Solve.jl:210 | **高 (副作用)** | 08_Solve |
| `ThermalDistributed` 直接调用 `jellyroll_element_properties` | ThermalDistributed.jl:15 | 高 (耦合泄漏) | 10_Thermal + CouplingInterface |
| `ThermalDistributed2D` vs `_Ring` 90% 代码重复 | ThermalDistributed.jl:1-47 vs 221-270 | 高 (重复) | 10_Thermal |
| `SimulationState` 通过 Dict 传递，无类型安全 | Solve/CycleSolver/CycleData (6 处) | 高 | 03_结构体 + CouplingState.jl |
| `solve_cycling` ~264 行过长 | CycleSolver.jl:209-472 | 中 | 11_其余文件 |

### 修订影响

- **02_全局架构**: 新增文件列表统一为 `CouplingInterface.jl`（通用耦合接口）
- **03_结构体重构**: 新增 `SimulationState` struct 到 `CouplingState.jl`
- **10_ThermalDistributed**: 新增 FEM 装配核心提取方案
- **新增决策记录**: 6 项决策已记录于上表

---

## 2026-04-07 结构体重构 + Initialisation 实施

**实施内容**: 基于 03（结构体重构）和 05（Initialisation 优化）方案，完成类型安全迁移。

### 新增文件
- `src/CouplingState.jl` — `MultiSPMeLayout` + `MeshGeometry` struct 定义

### 修改文件

| 文件 | 变更 |
|------|------|
| `SetCase.jl` | Case struct: `multi_spme_layout::Dict{String,Any}` → `layout`/`geometry`/`czm_mesh` 3 个类型化字段 + 5 参数兼容构造器 |
| `JuBat.jl` | include CouplingState.jl, export 新函数名 |
| `Initialisation.jl` | 3 个辅助函数重写为 snake_case (`extract_element_state`/`get_thermal_dofs`/`update_state`)，布局缓存改为 `MultiSPMeLayout` 构造器 |
| `Solve.jl` | ~20 处 Dict 访问替换，删除 `layer_weights` try/catch 块，`isempty` → `=== nothing` |
| `CycleSolver.jl` | 删除 `_ensure_multi_spme_layout!` 函数（28 行） |
| `CycleData.jl` | 4 处 Dict 访问替换 |
| `PostProcessing.jl` | 3 处 Dict 访问替换 |
| `Jellyrollmodel.jl` | 6 行 Dict 赋值 → `MeshGeometry` struct 构造 + `layer_weights` 计算 |
| `ThermalDistributed.jl` | `haskey` → 直接访问 `case.czm_mesh`（fail-fast） |

### 设计约束（Fail-Fast）
- 禁止 `haskey`、`get(dict, key, default)`、`try/catch` 吞错误
- 直接访问字段，未初始化就让 Julia 抛异常

### Commit 记录
| Commit | 说明 |
|--------|------|
| `ce0cf62` | feat: add CouplingState.jl with MultiSPMeLayout and MeshGeometry structs |
| `eb23c6f` | refactor: replace multi_spme_layout Dict with typed fields in Case struct |
| `4c99738` | feat: wire up CouplingState.jl in module, update exports |
| `baf6301` | refactor: rewrite Initialisation helpers with typed layout parameter |
| `7c90e0f` | refactor: replace all multi_spme_layout Dict access in Solve.jl |
| `df0276b` | refactor: remove _ensure_multi_spme_layout! from CycleSolver.jl |
| `39b103c` | refactor: replace Dict access in CycleData/PostProcessing/Jellyrollmodel/ThermalDistributed |

---

## 2026-04-07 P0 Bug 修复 + P1 CallModel 拆分 + P2-P4 精简

### P0: czm_mesh 未定义变量
- **位置**: Solve.jl `CallModel_MultiSPMe` 函数内
- **修复**: `czm_mesh` → `case.czm_mesh`

### P1: CallModel 拆分
- `CallModel_MultiSPMe` + `CallModel` 从 Solve.jl 整体迁出到 `CallModel.jl` (188 行)
- Solve.jl: 710→446 行（-264 行），成为纯步进器
- Commit: `3ff3027`

### P2: Parallelsolution 精简 + CutoffInfo struct
- `solve_branch_currents_newton` → `solve_branch_currents`（全局替换）
- 新增 `CutoffInfo` struct 替代 `Dict{String,Any}` 返回值
- 主函数精简: ~152→~80 行
- Parallelsolution.jl: 546→441 行 (-105 行)

### P3: ThermalDistributed 耦合泄漏修复
- `jellyroll_element_properties` 直接调用 → `case.geometry.layer_weights`（带 fallback）

### P4: CycleSolver `_` 前缀函数重命名
- `_compute_czm_effective_params` → `compute_czm_effective_params`
- `_compute_czm_strain_inputs` → `compute_czm_strain_inputs`
- `_update_czm_damage!` → `update_czm_damage!`

### 热点消除: thermal_extras Dict
- `case.thermal_extras::Dict{String,Any}` 字段已从 Case struct 中移除
- 所有访问点已迁移到类型化字段或通过 variables Dict 传递

---

## 进度追踪

| 阶段      | 状态          |
| ------- | ----------- |
| 现状分析    | 已完成 (01)    |
| 架构设计    | 已完成 (02-03) |
| 逐文件方案   | 已完成 (04-11) |
| Spec 修订 | 已完成         |
| 实施计划    | 已完成         |
| 代码实施 — 结构体重构 (03) | **已完成** |
| 代码实施 — Initialisation (05) | **已完成** |
| 代码实施 — Solve/PostProcessing 等连锁修改 | **已完成** |
| 代码实施 — P0 Bug 修复 | **已完成** |
| 代码实施 — P1 CallModel 拆分 | **已完成** |
| 代码实施 — 其余文件 (P2-P4) | **已完成** |
| 验证测试    | 待进行         |

---


