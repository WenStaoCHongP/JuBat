# 代码优化分析进度

> 约束：每次提出优化方案时，必须在此文件做简洁记录。

---

## 2026-04-01 全局架构重构 + 结构体重构

**方案**: 激进重构（方案 A），允许新增文件，main 分支代码不动。

| 编号 | 文件 | 变更类型 | 要点 |
|------|------|---------|------|
| 02 | 全局架构与约束 | 新增 | 4 个新文件规划 (CouplingState/CallModel/ThermalHeatSource/CZMInterface)，依赖图，命名规范 |
| 03 | 结构体重构方案 | 新增 | `MultiSPMeLayout` + `MeshGeometry` struct 替代 `Dict{String,Any}`，消除 4 处重复布局构建 |
| 04 | SetCase_jl | 修改 | Case 新增 3 个 Union{Nothing,T} 字段替代 1 个 Dict，便捷构造器兼容 main |
| 05 | Initialisation_jl | 修改 | 辅助函数简化（layout 字段直接访问），`_apply_nonuniform_soc!` 提取 |
| 06 | Variables_jl | 修改 | distributed2D/CZM 变量块提取为子函数，`Variable_update!` 拆分 |
| 07 | PostProcessing_jl | 修改 | distributed2D 热源输出提取子函数，cycling helpers 分组标注 |
| 08 | Solve_jl | 修改 | 851→~200 行，拆出 CallModel.jl (~200行)，纯步进器恢复 |
| 09 | Parallelsolution_jl | 修改 | 主函数 168→~80 行，`CutoffInfo` struct 替代 NamedTuple |
| 10 | ThermalDistributed_jl | 修改 | `compute_heat_sources` 迁出为 ThermalHeatSource.jl (~180行) |
| 11 | 其余文件汇总 | 新增 | Jellyrollmodel 去重 / SPMe 公共逻辑提取 / CZM 分派表 / 参数文件不动 |

**核心约束**: main 分支 SPM/SPMe/P2D 路径代码不动，仅重构 Parameters_Design 新增部分。

---

## 进度追踪

| 阶段 | 状态 |
|------|------|
| 现状分析 | 已完成 (01) |
| 架构设计 | 已完成 (02-03) |
| 逐文件方案 | 已完成 (04-11) |
| Spec 评审 | 待进行 |
| 实施计划 | 待进行 |
| 代码实施 | 待进行 |
| 验证测试 | 待进行 |
