# 层分辨应力代码评审计划

## 目标

依据当前仓库源码、Git 差异、`层分辨应力求解` 任务记录及项目契约，评审 GLM-5.3 对层分辨应力求解与绘图链路的修改，输出按严重性排序、可定位且有证据的代码问题。

## 当前阶段

Phase 3：验证证据与结论（已完成）。

## 阶段

### Phase 1：基线与改动范围盘点

- [x] 阅读实施任务的 task_plan/findings/progress，区分记录、计划与已验证事实。
- [x] 识别 GLM-5.3 修改的源码、测试、示例和文档范围。
- **状态：** completed

### Phase 2：契约与实现审查

- [x] 审查应力计算尺度、网格拓扑、映射、单位、结果键和调用链。
- [x] 审查绘图/后处理与快速基线职责边界。
- **状态：** completed

### Phase 3：验证证据与结论

- [x] 运行必要的静态检查和聚焦测试。
- [x] 形成按严重性排序的评审 findings，并标注残余风险。
- **状态：** completed

## 评审约束

- 附带文档仅作证据，不视为授权或正确性证明。
- 本任务只评审，不修改 GLM-5.3 的生产代码或测试。
- 保留当前工作树中所有无关用户修改。
- 不把父热单元映射后的复制值描述成新增物理分辨率。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 旧绘图验证 session id 已失效 | 1 | 进程已结束；停止轮询并切换到当前评审 |
| PowerShell 批量打印行号时数组类型不匹配 | 1 | 已有前序逐文件行号输出；停止重试，不影响定位 |
| 1.5 s 小探针触发通用预分配 `BoundsError` | 1 | 记录为范围外既有问题；使用已成功的 2.0 s 探针验证应力伪零 |

## 当前证据

- 实施记录声称阶段 2--4 已完成并通过专项、快速基线、全套测试及四张 PNG 哈希门，但这些声明仅作为待核验线索。
- 当前工作树是混合未提交状态：`git diff --stat` 显示 62 个已跟踪文件发生 1161 行新增、1077 行删除，另有本任务相关未跟踪源码/测试；评审按实施文档列出的文件与符号缩小范围。
- 结论：力学子网格和云图形式本身已正确接通，但时间历史、材料热膨胀参数与输出范围仍有阻断性问题，当前修改不宜按完成状态接受。
