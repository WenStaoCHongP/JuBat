# 层分辨应力代码评审：进度记录

## 会话：2026-08-30

### Phase 1：基线与改动范围盘点

- **状态：** completed
- 已确认用户请求为代码评审，不授权修改生产代码。
- 已将附带 `progress.md` 定位为证据材料，而非执行指令。
- 已建立独立评审记录，避免污染 GLM-5.3 实施任务文档。
- 已完整阅读实施任务的 `task_plan.md`、`findings.md`、`progress.md`。
- 已盘点工作树：这是包含 62 个已跟踪文件及若干未跟踪文件的混合 diff，后续按层分辨应力调用链逐项核验。

### Phase 2：契约与实现审查

- **状态：** completed
- 优先检查在线历史写入是否完整、两条求解路径是否使用力学子网格，以及绘图是否忠实反映实际分辨率。
- 已确认在线应力恢复使用 `case.czm_mesh.node/bulk_element`，最终两张应力云图也使用相同力学体单元网格；未发现回退到热粗网格。
- 已定位三个候选问题：间隔更新导致历史/最终场伪零、额外散点径向剖面违反三图要求、已知非零的 SP/PCC/NCC 热膨胀系数被显式置零。
- 已确认 `Solve` 在历史时间列写入并推进 `t` 之后才执行 CZM 和应力收割，故应力列的时间标签与实际 CZM 求解时刻错位。
- 专项测试 `test/test_layer_resolved_stress.jl` 15/15 通过；但其测试第 81 行将未写历史列保持为零视为成功，未覆盖真实时间历史完整性。
- 小型端到端探针（`nθ=8`、固定 0.5 s、`czm_update_interval=2`）复现相邻非更新列为 0、更新列约 `1.009e5 Pa`。
- `git diff --check` 未通过，主要为 `Solve.jl`、`Variables.jl` 新增行的尾随空白；作为交付卫生问题记录，不与科学错误混淆。
- 已检查现有三张最终场 PNG：温度为热粗网格云图，两张应力为力学体单元网格云图，视觉上不是散点图；网格选择满足用户要求。

### Phase 3：验证证据与结论

- **状态：** completed
- 60 s 文字快速基线 exit 0，19 次 CZM 更新均收敛，最终环向应力 `-1.3952~3.8570 MPa`、切向剪应力 `-0.37392~0.39515 MPa`。
- 最终结论为 4 个问题：时间错位、非更新列伪零、已知 CTE 置零、额外散点剖面；前 3 项影响科学/时间契约，第 4 项违反用户明确的三图范围。
- 未修改 GLM-5.3 的生产代码或测试；仅新增本评审记录并更新 planning 索引。

## 测试结果

| 检查 | 预期 | 实际 | 状态 |
|---|---|---|---|
| `test/test_layer_resolved_stress.jl` | 专项契约通过 | 15/15 通过 | pass |
| `czm_update_interval=2` 小型端到端探针 | 非更新列不得伪装为有效零应力 | 倒数第二列全零、相邻更新列 `1.009e5 Pa` | fail |
| `git diff --check`（相关文件） | 无 whitespace error | `Solve.jl`/`Variables.jl` 等多处尾随空白 | fail |
| `test/test_czm_strain_inputs.jl` | 父热单元映射契约通过 | 20194/20194 通过 | pass |
| `example/力学模块验证/test_electrode_coat_modulus.jl` | 模量/尺度及零扰动路径通过 | 全部通过 | pass |
| `example/testexample.jl` | 60 s 快速行为门通过 | exit 0，19 步，文字指标与当前基线一致 | pass |

## 错误日志

| 时间 | 错误 | 尝试 | 处理 |
|---|---|---:|---|
| 2026-08-30 | 旧绘图验证 session id 不存在 | 1 | 视为旧任务进程已关闭，停止轮询 |
| 2026-08-30 | 1.5 s 探针触发预分配扩容后 `errors[1:v]` BoundsError | 1 | 属于既有通用求解器预分配问题，不用于本次 finding；已有 2.0 s 探针足以复现应力伪零列 |
| 2026-08-30 | PowerShell 批量行号打印的范围数组类型不匹配 | 1 | 使用此前逐文件行号结果完成定位，未重试 |
