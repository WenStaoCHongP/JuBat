# 位移协调源码审查进度

## 2026-08-31

- 启动只读源码审查。
- 按 planning-with-files 技能和项目约定创建本任务三份记录。
- 检索历史记忆：旧快照提示 `phi_pairs` 当时可能没有机械消费者，但当前源码必须重新验证。
- 读取规划总索引时输出过长被截断；已记录错误，后续使用定点读取。
- 下一步：盘点 Git 状态与机械/CZM 源码入口，搜索 `phi_pairs`、节点连接、DOF、约束矩阵、位移跳跃和接触装配。
- 已盘点当前工作树并记录无关脏改动，未触碰它们。
- 全局符号搜索发现当前版本新增了 `merge_phi_pairs`/`mesh_bonded` 完美粘结路径，旧记忆中的 `phi_pairs` 无消费者结论已发生漂移。
- 发现源码直接标注 SP–涂层接触和 `friction_mu` 仍为预留；下一步检查网格合并、bulk/cohesive 装配和 BC 的完整调用链。
- 已检查 `CzmSubmesh`、`build_czm_submesh` 与 `merge_phi_pairs`：相邻层初始共享节点；Φ 配对通过节点拓扑合并实现强式位移相等，不是额外约束方程。
- 搜索确认生产 `CzmMesh` 和 `MechState` 路径引用 `mesh_bonded`；下一步逐行检查 cohesive 界面分裂是否会破坏或有意放松这些共享关系。
- 已检查 `create_czm_mesh`：Φ 合并先完成，随后只在 PE–PCC/NE–NCC 真实面复制节点并重写外层 bulk 连接；SP–涂层未分裂，保持共享节点完美粘结。
- 下一步核对 cohesive `B_local` 的两侧符号、局部坐标旋转、牵引内力/切线装配，以及这些方程与 bulk 方程如何汇总。
- 已核对 cohesive 弱式协调：top−bottom 位移跳跃经局部法/切标架进入牵引律，`BᵀT` 以相反符号回装两侧；有限刚度/损伤允许跳跃。
- 已确认线弹性、GL 几何非线性和 J2 路径共享同一节点/DOF 拓扑；差异在 bulk 残差、切线与状态，不在位移协调定义。
- 下一步检查总残差汇合、边界自由度消元、各求解方法和收敛后状态提交。
- 已确认 bulk 与 cohesive 在同一全局 DOF 空间相加形成总残差/切线；不是分区位移传递。
- 已检查 BC 实现：默认外圈固定、可选内圈固定；线性系统使用相对罚项，位移向量另行显式施加 Dirichlet 值。
- 下一步逐分支检查求解器入口、收敛判据和成功/失败状态提交，确认协调关系在迭代路径中不被绕开。
- 已检查 basic、旧 arc-length 与自适应载荷子步：三者共享同一总系统和协调拓扑，BC 在残差、线性修正和接受位移三处一致处理。
- 已确认 `MechState` 只在收敛时提交位移/损伤；失败试探不会覆盖持久状态。下一步检查当前主调度使用哪一个 arc-length 实现，以及上层对 `converged=false` 的处理。
- 已检查统一调度、新 geo 弧长、`update_czm_damage!` 和 `Solve.jl`：几何非线性弧长在 λ=1 严格验收后提交；主流程对未收敛结果报错，不会将失败位移保存为快照或应力结果。
- 装配/约束/提交链已闭合。下一步用现有拓扑与单元测试验证 Φ 零位移差、cohesive 可分离/牵引和接触预留判断，并复核边界节点识别。
- 已阅读 Φ 合并与双线性 cohesive 测试：前者覆盖拓扑唯一 DOF，后者覆盖法/切向位移跳跃及牵引/损伤响应。
- 发现 czm-off 的 `thermal_diffusion_stress_2D` 只消费 `mesh_bonded`，故全部层间面均为完美粘结；需在最终结论中与在线 CZM 路径明确区分。
- 下一步读取边界识别细节并运行两项已有专项测试。
- 已确认边界识别遍历扩展后 `CohesiveMesh.nnode`，不会混用 raw 网格节点索引。
- 全局引用搜索确认 `friction_mu` 和 `contact` 没有机械消费者，SP–涂层接触/摩擦尚未装配。
- 已运行 Φ 合并专项测试：11/11 断言通过，退出码 0。
- 下一步运行双线性 cohesive 法/切向专项测试，并做规划索引/工作树范围核验。
- 已运行双线性 cohesive 专项测试：234/234 断言通过，退出码 0。
- 已复核规划目录实际为 41 个、文件 136 个，并同步总索引统计/状态。
- 规划范围 `git diff --check` 通过；只有 LF→CRLF 提示，无空白错误。
- 审查完成；未修改生产源码或测试，未触碰用户原有无关脏改动。
