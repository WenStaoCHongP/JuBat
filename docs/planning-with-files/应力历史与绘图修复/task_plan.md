# 应力历史与绘图修复计划

## 目标

修复层分辨应力历史的时间错位和 CZM 非更新步伪零问题，保证最终时刻应力来自最近一次同时间层的有效力学解；保持 SP/PCC/NCC `alphaT=0` 不变；删除额外环向应力径向散点图，只保留最终温度、环向应力和切向剪应力三张云图。

## 当前阶段

Complete。

## 阶段

### Phase 1：调用链与基线核实

- [x] 核实 Solve 时间推进、输出列和 CZM 更新顺序。
- [x] 盘点散点图代码、产物和基线引用。
- **状态：** completed

### Phase 2：窄范围实现

- [x] 让 CZM/应力恢复使用同一历史时刻的温度与 SOC。
- [x] 将最近一次有效应力显式写入每个输出列，禁止未计算列保留预分配零。
- [x] 删除径向散点图代码、产物及相应基线约定。
- **状态：** completed

### Phase 3：验证与交付

- [x] 新增/更新间隔更新和时间对齐回归测试。
- [x] 运行专项测试、60 s 快速基线和三张最终云图验证。
- [x] 运行 `git diff --check` 并核对只改授权范围；完整检查仅命中工作树已有 `Solve.jl` 行尾空白，本轮新增行与授权文件另行核对。
- **状态：** completed

## 约束

- `SP/PCC/NCC.alphaT` 本轮保持 0，不讨论或补充物理值。
- 不改变应力结果键、Pa/MPa 单位、力学子网格或 CZM 本构。
- 非更新步采用“最近一次实际力学求解结果保持”语义，不重新用新载荷配旧位移，也不伪造零值。
- 保留当前混合工作树中所有无关修改。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 基线 TOML 首次校验按不存在的 `[baseline]` 表取值 | 1 | 按文件实际顶层键重新解析，校验通过 |
| `git diff --check` 命中 `Solve.jl` 既有行尾空白 | 1 | 不扩大范围格式化混合工作树；保留披露并核对本轮变更 |
