# 热力网格拓扑修正进度

## 2026-08-06

- 建立跨文件实施任务。
- 已读取 `planning-with-files` 技能说明。
- 项目记忆索引无本任务可复用记录；以当前代码、理论和用户决定为准。
- 当前进行 Phase 1 影响面盘点。
- 完成 Phase 1：采用 `czm_enabled=true` 启用力学子网格，删除独立 `nθ_czm` 角度接口。
- 完成首轮源码实现：力学直接使用热 `theta`，新增 `CzmSubmesh.phi_pairs`，`thermal_elem_map[e]=seg`，插值限定唯一拓扑父热单元并删除最近节点回退。
- 下一步迁移测试/示例调用并运行目标测试，再进入理论文档修正。
- 已迁移所有构网调用到 `czm_enabled=true`；`rg` 确认源码、测试与求解示例中不再存在 `nθ_czm=` 关键字。
- 已重写子网格测试以验证 9 条螺旋、每层 `1:ne_thermal` 父映射和力学 Φ 坐标配对；COH2D4 数量改为严格 `4*n_segments`。
- 尚余两个特征长度分析脚本使用 `nθ_czm` 作为结果变量名，需改为共享 `nθ` 术语。
- 已将两个特征长度脚本改为 `nθ_shared`。
- 首轮目标测试：子网格 4830/4830、插值 22740/22740、插值挂载 2/2 通过；`test_create_czm_mesh` 在一个合法父边界节点上因 Newton 物理残差阈值过松导致参数坐标略超界，正在收紧求解残差后重试。
- 将等参 Newton 物理残差从 `1e-10` 收紧到 `1e-13` 后，目标测试全部通过：子网格 4830、插值 22740、挂载 2、CZM 构造 67288、法向 13456。
- 已修正 `Theory/02`、`Theory/04`、符号表、弱形式、极限检验和 Theory 总览；后续“界面术语统一”任务将口径进一步规范为 2 种类型、每个 8 层重复单元 4 个真实面、完整模型 `4N_seg` 个离散单元，Φ 仅表示跨匝配对。
- 已在 `AGENTS.md` 记录共享周向网格契约、`phi_pairs` 用法及 SP–涂层接触/摩擦后续开发建议。
- 全仓搜索保留 `nθ_czm` 的禁止性说明；固定 92/184 的 cohesive 规模表述已由后续“界面术语统一”任务移除。
- 扩展测试通过：损伤映射 4/4、缓存 9/9、耦合装配 8/8、应变输入 13470/13470、求解签名 4 pass + 1 existing broken、归一化重设计全部通过、smoke 完成 43 步。
- 修正 `test_map_czm_damage.jl` 的旧合成场景：先清零目标热单元上全部既有 cohesive 损伤，再验证前三个指定值的 max 归约，避免四界面拓扑中的第四个既有值干扰。
- 数值拓扑探针确认：热/力学角段 1682/1682，力学节点 15147，体单元 13456，每热单元 8 个体单元，COH2D4 6728，Φ 对 1603。
- 强制 `testexample` 退出码 0，除最大法向分离外全部冻结科学指标一致；分离 `1.3527e-14 → 1.2557e-14 m`，PNG SHA-256 同步变化，故严格基线 FAIL，未重建基线。
