# 静默回退专项审查进度

## 会话：2026-08-06

### 阶段 1：模式扫描与候选收集

- **状态：** 完成
- 已确认用户关注的是掩盖工程错误、让计算以伪造默认状态继续的回退。
- 已确定检查 `src/`，后续以测试和调用上下文去除误报。
- 已完成第一轮非有限值、默认字段和异常捕获模式检索，并记录首批候选。
- 已阅读 `CouplingState.jl` 的应变输入映射和 CZM 提交逻辑，以及 `Solve.jl` 的初始状态和 CZM 调用路径；确认多处会丢失状态或跳过损伤更新后继续计算。
- 已完成显式“回退/重置/忽略”与三元默认值检索，新增归一化、边界条件、插值和导出层候选。
- 已判读 CZM 尺度、边界罚法、热到 CZM 插值、初始位移及外载默认值；保留 5 个高风险候选，排除 2 个明确的可选输入默认值。
- 已审查宏观力学求解与 CZM 损伤到热单元映射；新增“求解失败返回零位移”“退化单元输出零应力”“越界损伤映射输出零损伤”。
- 已审查通用 Q4 积分、热边界缓存、循环阶段包装与 CSV 写入容错；确认 Q4 退化点跳过和循环结果伪完成为高风险，排除惰性缓存与显式导出跳过。
- 已审查循环后处理完整数据流，确认缺失求解结果会被包装成正常时长、温度、容量和松弛指标。
- 已确认 `CycleData.jl` 的回退位于实际导出 API，而非死代码；现有边界测试未覆盖失败语义。
- 已审查变量历史写入、温度状态提取和分流初猜；确认历史维度错误留零、温度状态不足沿用旧值为高风险，排除仅影响迭代初猜的回退。
- 已完成所有 `get(..., default)` 与 `@warn` 路径的全量枚举和初步分类。
- 已验证 CZM 参数入口存在硬断言，因而下调两个被上游阻断的尺度回退风险；开始审查 `clamp`/数值下限类隐式兜底。
- 已完成 `clamp`、数值下限和退化 cohesive 几何的判读，确认多个参数/网格错误会被转换成可计算值。
- 已追踪分流线搜索失败到主耦合调用，确认未收敛电流仍被归一化并用于后续电化学/热计算。
- 已审查空值/零值分支，新增极耳冷却静默取消、未知冷却方法变绝热、未知电芯类型沿默认参数继续、退化边单元跳过等候选。
- 已追踪 `SetMesh` 退化边跳过后的数组使用，确认它会间接抛出边界错误而非静默出结果，故从确定缺陷中移除。
- 已完成小量数值下限复核，新增 Jellyroll 退化周向跨度回退，并确认 CZM 零长度单元会被静默保留为零贡献。
- 已完成字符串选项分派审查，确认循环导出时间格式、纯热模型类型和初始化热模型存在未知值默认分支；CZM 迭代与状态访问的未知值会正确报错。
- 已逐段审查 CSV 与 CZM 快照构造，确认牵引/迭代/残差被硬编码为零，以及数组缺失时补零或少写行。
- 已交叉核对热结果键名，确认循环 `T_mean_end` 当前必然因不存在的键而回退到初始温度。
- 已检查相关测试与统一验证入口；未发现关键失败语义测试，也未发现集中式选项校验。
- 已完成逐处判读、风险分级、误报排除和优先级汇总。

### 阶段 2：逐处语义判读

- **状态：** 完成
- 已阅读所有高价值候选上下文与关键调用链。
- 已排除计时默认零、显式无外载、Newton 初猜、惰性缓存等合理默认行为。

### 阶段 3：交叉验证与分级

- **状态：** 完成
- 已核对求解结果键、测试覆盖、生产导出入口和收敛标志消费路径。
- 形成 P0/P1/P2 分级。

### 阶段 4：交付审查结果

- **状态：** 完成
- 已在 `findings.md` 添加优先级结论和统一修复原则。
- 已更新 `docs/planning-with-files/index.md` 的统计、任务汇总与任务明细。
- 未修改本轮审查涉及的业务源码。
- 2026-08-06 用户确认：`tab` 冷却在无极耳时退化为 `none` 是故意定义的极限情形；已从缺陷清单移除，仅保留未知 `cool_method` 静默变成无冷却的问题。
- 2026-08-06 用户补充审查前提：操作人员不会设置未知 `cool_method`；未知字符串分支也已从缺陷清单移除。

## 文件变更

- 新建 `docs/planning-with-files/23_静默回退专项审查/task_plan.md`
- 新建 `docs/planning-with-files/23_静默回退专项审查/findings.md`
- 新建 `docs/planning-with-files/23_静默回退专项审查/progress.md`

## 检查结果

| 检查 | 预期 | 实际 | 状态 |
|---|---|---|---|
| 规划文件初始化 | 三个文件位于项目约定子目录 | 已创建 | 通过 |
| 宽模式静态扫描 | 覆盖默认值、吞错、非有限值、选项回退、退化网格 | 已覆盖并逐处去误报 | 通过 |
| 关键键名交叉核对 | 生产者与消费者一致 | 发现 `T_mean_end` 必现错配 | 发现缺陷 |
| 收敛标志传播核对 | 未收敛结果不得参与后续物理计算 | 分流和 CZM 均存在继续路径 | 发现缺陷 |

## 错误日志

| 时间 | 错误 | 次数 | 处理 |
|---|---|---:|---|
| 2026-08-06 | 内存关键词检索无匹配，组合命令返回 1 | 1 | 视为无相关历史结论，不重复检索 |
| 2026-08-06 | PowerShell 复杂 `rg` 正则引号未闭合 | 1 | 拆分为简单检索命令 |
| 2026-08-06 | 过度约束的 `rg` 模式无匹配 | 1 | 使用已完成的直接分项检索结果 |
| 2026-08-06 | 选项分派组合正则无匹配 | 1 | 改用简单模式逐处筛选 |
| 2026-08-06 | 测试检索混入不存在路径而返回非零 | 1 | 使用有效目录的已返回结果完成判断 |
