# 任务计划：CZM 热点重构

## 目标

依据仓库内现有 `docs/superpowers/` 规格与计划，精确定位 CZM 性能热点，完成范围收敛、可审查且不改变科学/API 契约的重构，并通过项目规定的聚焦测试与快速行为基线。

## 当前阶段

阶段 5：完成

## 阶段

### 阶段 1：任务基线与工作区审计

- [x] 记录用户意图与仓库约束
- [x] 找到“CZM 热点重构”对应的 spec/plan 或确定应执行的残留项
- [x] 盘点分支、工作区和已有实现，避免覆盖无关改动
- **状态：** 完成

### 阶段 2：契约映射与重构设计

- [x] 映射热点调用链、状态所有权、结果键与测试门
- [x] 记录基线与最小改动边界
- [x] 确定逐步实现顺序与回退条件
- [x] 获得用户对重构范围与设计的明确批准
- [x] 编写方案 A spec 与逐步实施计划
- **状态：** 完成

### 阶段 3：测试驱动实现

- [x] 先建立/运行能够暴露目标热点行为的聚焦测试或基准
- [x] 分小步实现重构
- [x] 每步核对科学结果与公开接口契约
- **状态：** 完成

### 阶段 4：验证

- [x] 运行受影响的 CZM 测试
- [x] 运行 Julia 1.11.2 单线程 `example/testexample.jl`
- [x] 检查 scoped diff、格式与无关改动隔离
- **状态：** 完成

### 阶段 5：交付

- [x] 更新任务三件套与总索引
- [x] 汇总修改、验证结果和已知缺口
- [x] 交付可审查结果
- **状态：** 完成

## 关键问题

1. 用户所指“热点重构”对应哪个现有 superpowers plan，哪些条目尚未落地？
2. 当前工作区是否含用户未提交改动，目标文件是否重叠？
3. 如何证明重构只降低热点开销而不改变 CZM 收敛、状态提交与科学输出？

## 决策

| 决策 | 理由 |
|---|---|
| 使用已安装的 Superpowers 流程，并以仓库内 `docs/superpowers/` 文档作为治理基线 | 14 个 Superpowers 技能已安装且本轮可用；`brainstorming` 要求设计获批后才能改源码 |
| 使用 `planning-with-files` 保存执行状态 | 任务预计超过 5 次工具调用，且 AGENTS.md 规定三件套位置 |
| 在明确现有计划残留与工作区归属前不修改源码 | 避免重复已完成优化及覆盖无关脏改动 |
| 将现有内聚力参数视为正确实验值并冻结 | 用户明确确认；本批只做求解性能重构，不开展参数校准或修改本构 |
| 方案 A 首批仅覆盖 basic、非几何路径 | 最新热点证据只覆盖该路径；其余求解分支保持原样 |
| spec 路径为 `docs/superpowers/specs/2026-09-01-czm-basic-factorization-cache-design.md` | 固化参数、范围、缓存判据、失败语义与验收门 |
| 实施计划路径为 `docs/superpowers/plans/2026-09-01-czm-basic-factorization-cache.md` | 以 RED-GREEN 小步执行并保留 scoped 审查点 |

## 错误记录

| 错误 | 次数 | 处理 |
|---|---:|---|
| 按文件名搜索 `CZM.*热点/hotspot` 无匹配 | 1 | 下一步改为在计划正文与近期目录中检索语义和残留项 |
| 真实矩阵实验的 `include_string` 虚拟文件名使 `@__DIR__` 指向仓库上一级 | 1 | 用探针绝对路径作为 include filename，保持其相对 include 语义 |
| 新任务误用已被 `41_位移协调源码审查` 占用的编号 | 1 | 迁移三件套到 `42_CZM热点重构` 并同步总索引 |
| production 接线测试未传三组热化学单元数组，触发 MethodError | 1 | 按 bulk 单元数传入维度正确的显式零载荷数组，重新运行 RED 门 |
| 记录上述错误时补丁上下文与 progress 实际位置不符 | 1 | 先查看文件尾部，再用准确上下文一次性更新测试和错误记录 |
| 计划中的 `output/testexample/metrics.toml` 实际不存在 | 1 | 以冻结档案 `Simplify/baseline/testexample/metrics.toml` 为门，逐项对照本轮控制台输出 |
| 新 spec 头部 3 行含 Markdown 尾随空格 | 1 | 只清理本任务 spec 的 3 行，不触碰无关 CRLF/用户文件 |

## 约束提醒

- 不用零值、回退或吞错掩盖无效工程状态。
- 保留 CZM 公开接口、状态所有权、收敛语义、结果键和单位。
- 混合工作区只修改/验证本任务范围。
- 任一强制科学指标变化即停止并定位。
