# 堆芯塌陷力学建模：发现与证据

## 用户决策链（本任务的根本输入）

1. 全路线 Batch 0'–8 分批执行，每批独立验收门。
2. 各向异性建模决策：**不存在本征正交各向异性，每个材料都是各向同性的，各向异性只由多种材料层叠加展现**；Theory 中的相应表述属于理解错误，须修复理论。
3. SP–电极界面先按完美接触（完美粘结）假设，接触/摩擦后置 Batch 8；其余模型（K_G、J2、多圈状态、反馈闭环）先行。
4. `czm_enabled` 为唯一主开关，内部控制塑性/接触/几何非线性子选项，默认全关。
5. operator-splitting 先行；两份文档（superpowers spec + planning-with-files）按各自规则保存。

## Batch 0' 修订记录（2026-08-17 执行）

### 修订对象：`Theory/03_本构理论.md`（唯一被修改的理论文件）

| 位置 | 修订内容 |
|---|---|
| §2.1 材料表 + 注 2.1b | 涂层/隔膜本构从"横观各向同性（退化）5 常数"改为"各向同性线弹性 2 常数"；注 2.1b 重写为"逐层各向同性与叠层等效各向异性"声明，明示旧表述废除 |
| §2.1.2 末（约 L134） | "横观各向同性层的二维约化"改为"各向同性层的统一二维约化（全 8 层）"，删除附录 C 悬空引用 |
| §2.1.6 前后（L350、注 2 热力学一致性） | 删除"横观各向同性退化处理"措辞 |
| 注 2.3 符号统一 | 全 8 层统一裸写 E_i, ν_i；E∥/E⊥ 明确为叠层等效量 |
| §2.3.5 输出 1/2 | E∥/E⊥ 用途限定为"工况 R 叠层等效截面刚度"；工况 C 的 n 向刚度由逐层 E_i 显式给出 |
| §2.4.2 CLT Q̄ | 逐层面内常数 E∥^i/ν∥^i/G∥^i 改为各向同性 E_i/ν_i/G_i；删除"片材面内正交各向异性"分支句 |
| §2.4.4 整节重写 | 新标题"逐层各向同性闭合与叠层等效刚度"；(2.35) 逐层稳定性、(2.36) 逐层各向同性平面应力矩阵、(2.37) 等效参数集（E∥/E⊥/G⊥ + 标定 ν_sn^eff）、(2.38) 等效矩阵（形式上正交各向异性但为叠层结构属性，与热学 k_t/k_r 同源）、(2.39)/(2.40) 工况选择；Step 5 参数门槛改为"E_i/ν_i 缺失即报错并指明层" |
| §2.4.5 / §2.6 | 接口传递、C_Q4 分量来源、ε_zz 后处理（原三维 ν_sz/ν_nz 正交各向异性闭合改为各向同性 ε_zz=ε_z*−(ν_i/E_i)(σ_ss+σ_nn)）、自查门槛第 3/4 条同步更新 |

### 联动核查结论（无需改动）

- `Theory/02`：§1.3.7 K_G 段只谈运动学，无本构常数要求。
- `Theory/04`：界面运动学不含各向异性常数。
- `Theory/05/06`：热源/热导为叠层串联并联（k_r/k_t），与修订后哲学一致。
- `Theory/07`：`C_eff^ps` 引用为通用符号，随 §2.4.4 新定义自动一致。
- `Theory/08`：C1–C5 与参数默认表（σ_y0/H/μ_SP）不含正交各向异性常数。
- `Theory/09` 附录A：E∥/ν∥/E⊥/G⊥ 条目本身即叠层等效定义，保留。
- `Theory/10` 附录B、`Theory/00`、`Theory/CLAUDE.md`：无本征各向异性声明。

### 验证

- grep 全 Theory：`横观各向同性` 仅剩注 2.1b 的废除声明；`正交各向异性` 仅用于描述叠层等效矩阵的**形式**；无 `E_s^i/G_{sn}^i/ν_{sz}/ν_{nz}` 残留。
- 公式 tag 顺序 2.35→2.40 单调；无 2.37a 残留。
- 无尾随空白。
- 全套测试（Julia 1.11.2、单线程、GKSwstype=100、--startup-file=no）：26 通过 / 1 失败；唯一失败 `unit_czm_eigenstrain.jl` 为界面术语统一任务 progress.md:32-33 记录的既有缺口，与本次纯文档修订无关。

## 代码现状基线（为 Batch 1 准备，源自差异评审 + 现场核验）

- `src/Czm.jl:268-338` `assemble_bulk_stiffness`：逐层各向同性平面应力 D_mat——与修订后理论 §2.4.4 (2.36) 一致，无需本构改动。
- `src/Materialmatrix.jl:30-57` `thermal_anisotropic_conductivity_2d`：逐层 λ 串/并联均质化——理论 §5 同源，一致。
- `src/parameters/Jellyroll.jl`：现有 E/ν/E_coat/λ 即理论所需全部本构参数；Batch 2 无需新增本构参数，Batch 3 仅需 σ_y0/H。
- 可复用：8 层 Q4 子网格（`Jellyrollmodel.jl:601-688`）、cohesive 节点复制（`CzmMesh.jl`）、`phi_pairs`、三种求解策略与状态回滚（`CzmSolve.jl`）、`czm_layout.u_prev` 持久化（`CouplingState.jl`）。
- 主要缺口：常刚度缓存与 `f_int_bulk=K_bulk*u` 线性假设（`Czm.jl:546-578`）、无 K_G/J2/接触状态、`phi_pairs` 无求解器消费者、CycleSolver 无跨圈几何/塑性交接。

## 风险提示

- 完美粘结假设（Batch 4'）会抑制/抬高 SP 先皱门槛（Theory 04:174、07:209），C3/C5 与完整 C4 宣称必须等 Batch 8；C4-lite 结果须带声明。
- `unit_czm_eigenstrain.jl` 既有失败须在 Batch 1 开工时登记为前置状态，避免与后续回归混淆。
