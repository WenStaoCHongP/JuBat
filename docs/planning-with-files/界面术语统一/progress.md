# 界面术语统一：进度记录

## 2026-08-17

- 已读取 `planning-with-files` 技能并按项目约定建立独立任务目录。
- 已确认工作树存在大量用户修改；本任务只做窄范围术语修订。
- 已完成首轮全局检索，定位 Theory、对照文档、源码注释和测试说明中的 `N_Φ`、92/184、“2 个界面”等混用。
- 当前阶段：建立规范术语与检索清单。
- 已核对 `CzmMesh.jl`、`Jellyrollmodel.jl` 与聚焦测试：当前拓扑按四个真实面生成单元，精确断言为 `n_cohesive == 4 * n_segments`；`phi_pairs` 独立保存跨匝配对。
- 已判定 Theory 的固定 92/184 不是当前离散单元总数，必须从活跃模型术语中移除。
- 已修订 `Theory/01_符号与守恒律公理.md`，建立 2 种类型 / 4 个真实面 / `4N_seg` 离散单元 / `N_pair^Φ` 跨匝配对的总符号表。
- 已重写 `Theory/02_几何与运动学.md` 的界面计数段：`Φ` 只保留跨匝映射语义，位移跳跃与弱形式界面改用 `Γ_coh`，并明确末端部分匝由 `N_seg` 自然计入。
- 当前阶段：修订 Theory 与对照文档。
- 已修订 `Theory/04_CZM.md`：材料组合表现为四个真实面、计数改为 `4N_seg`，热阻串联因子改用真实面数 4 而非类型数 2，并同步所有相关偏导符号。
- 已同步 `Theory/07_弱形式与求解.md`、`08_极限检验.md`、`09_附录A_符号表.md` 与 `Theory/CLAUDE.md`；弱形式积分域统一为 `Γ_coh`，固定 92/184 已从活跃模型规模表述中移除。
- 已清理 `Theory/03_本构理论.md` 中把 cohesive 面称为“Φ 内界面”的残留。
- 已在 `md/对照/00_总览与索引.md` 建立工程对照口径，并同步 md 02、md 06、对照卷 02/06 与源码函数索引。
- 已仅用注释明确 `CohesiveMesh.n_layers` 的遗留语义，并修正聚焦测试注释；未重命名字段、未修改 Julia 执行逻辑。
- 已在 `AGENTS.md` 固化同一计数约定，避免后续重新混用。
- 当前阶段：静态审计与针对性验证。
- 静态审计确认活跃 Theory、对照、源码与测试中不再出现 `N_Φ=92/184`、`N_coh=2` 或“每匝 2 个 cohesive”等旧计数；历史记录仅以“旧术语/已清理”形式保留。
- 首次 PowerShell 尾随空白检查因 `"$f:$line"` 的变量边界解析失败；改为 `"${f}:$line"` 后成功执行。报告的尾随空白均为既有未触碰行，聚焦 `git diff --check` 无错误。
- Julia 1.11.2、单线程、`GKSwstype=100`、`--startup-file=no` 运行 `test/test_create_czm_mesh.jl`：两个测试集分别 67288/67288、13456/13456 通过。
- 源码和测试只修改注释；未改公开字段、断言或运行逻辑。
- 当前阶段：更新规划索引并交付。
- 已更新 `docs/planning-with-files/index.md`：28 个任务目录、90 个文件（已跟踪 48、未跟踪/新增 42），并新增本任务的汇总行与详情段。
- 任务完成；最终交付以统一术语、涉及范围和验证结果为主。
- 用户要求将当前成果分批提交远程仓库；已开始核对脏工作树、提交边界、当前分支与远程跟踪关系，避免混入无关改动。
- 当前分支为 `codex/src-physics-modularization`，尚未配置上游；`origin` 指向用户仓库。首次只读远程探测受沙箱网络限制失败，待本地提交完成后按要求申请网络权限推送。
- 全仓 `git diff --check` 首次发现单元级验证文档两处 Markdown 行尾空格，已删除并准备复查。
- 完整测试首次运行因沙箱禁止写入 `C:\Users\user\.julia\compiled` 的预编译 pidfile 而退出；这是环境权限错误，未进入测试执行，下一次使用相同命令申请受控权限运行。
- 获批后以同一固定命令完成全套测试：27 个独立测试文件中 26 个通过，`unit_czm_eigenstrain.jl` 失败；其 60 项断言中 58 通过、2 失败。输出显示四个界面的法向分离均为零或负值、`D_max=0`，而失败断言要求 `D_max>0` 且至少一个正开度界面损伤。未通过放宽断言或伪造损伤处理，先核对既有验证缺口记录。
- 已核对 `docs/superpowers/findings/2026-07-27-unit-czm-eigenstrain-verification-gap.md` 与静默回退审查记录：58/60 是已知参数自洽缺口，不是当前术语工作引入的回归；保留失败断言与显式诊断。
- 第一批 `git add` 首次因沙箱禁止创建 `.git/index.lock` 失败，未形成暂存内容；下一次对 Git 元数据写入申请受控权限。

## 待办

- 逐段核对公式上下文，避免把纯术语修订变成未授权的数值模型修改。
- 修订活跃文档与注释。
- 运行残留术语检索与聚焦测试。
- 更新 `docs/planning-with-files/index.md`。
