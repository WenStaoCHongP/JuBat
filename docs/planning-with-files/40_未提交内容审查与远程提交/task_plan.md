# 未提交内容审查与远程提交计划

## 目标

审查当前混合工作区的全部未提交内容，区分应版本化的源码/文档与本地配置/运行产物；对纳入范围运行相称验证，形成清晰提交并推送当前分支到 `origin`。

## 阶段

- [x] 阶段 1：读取技能、记忆线索、Git 状态与远程关系。
- [x] 阶段 2：逐项审查已跟踪和未跟踪改动，确定提交/保留边界。
- [x] 阶段 2.5：确认分离计算接口取舍并补最小回归覆盖。
- [x] 阶段 3：运行定向验证与差异检查。
- [x] 阶段 4：同步规划索引并定向暂存、复核、提交。
- [x] 阶段 5：推送当前分支到 `origin`，核验本地/远程状态。

## 当前状态

已完成。旧分离接口已按用户授权删除，唯一入口为 `compute_separation(czm_mesh, elem, u)`；行为修复与 FEM 专题文档已分别提交并推送。旧设计稿、过时探针、本地工具状态、运行产物及行尾-only `Initialisation.jl` 保持未提交。

## 约束

- 保留用户未授权清理的运行产物与本地配置，不以提交为由删除或回退。
- 不把生成图片、日志、探针结果与源码文档混入提交。
- 对行为修改至少执行相关定向测试；涉及 `src/` 时按项目约定运行 `example/testexample.jl` 快速基线。
- 推送目标为当前分支对应的 `origin/codex/src-physics-modularization`。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| `isapprox(::Tuple, ::Tuple; atol=...)` 在 Julia 1.11 无方法 | 1 | 改为两个分量分别比较，不重复调用同一无效写法 |
| `src/Tools.jl` 混合 CRLF/LF 导致 `git diff --check` 报尾随空白 | 1 | 仅对该候选提交文件做 LF 机械规范化，再复查逻辑差异 |
| `dos2unix` 不在环境中 | 1 | 使用受限于单个已解析工作区文件的 PowerShell UTF-8/LF 机械规范化 |
| LF 规范化后暴露两处既有 docstring 行尾空格 | 1 | 定点移除，不改动函数行为 |
| 单个补丁重复声明同一规划文件更新 | 1 | 合并为一次 Update File 操作后重试 |
