# 力学结构体重构 Task 10 执行计划

## 目标

仅完成 `docs/superpowers/plans/2026-08-30-mechanics-struct-refactor.md` 中的 Task 10，保留 Task 1-9 与其他未提交工作，并按计划中的明确验收条件验证。

## 阶段

- [x] 阶段 1：读取 Task 10、现有规划索引与工作区状态，确认精确范围和前置状态。
- [x] 阶段 2：映射 Task 10 涉及的调用方、契约和测试，记录实现决策。
- [x] 阶段 3：实施 Task 10 的最小修改。
- [x] 阶段 4：运行 Task 10 定向验证、差异检查及适用的快速行为基线。
- [x] 阶段 5：整理结果并同步规划索引。
- [x] 阶段 6：按用户授权定向暂存 Task 1–10 逻辑文件集并提交。

## 当前状态

Task 10 Step 1（文档同步）及验证完成。用户已明确授权 Step 2；提交按 Task 1–10 逻辑边界定向暂存，不使用 `git add -A`，并保留运行输出、探针及其他无关工作区改动。

## 约束

- 不覆盖或回退用户及前序任务的现有修改。
- 不把缺失、NaN 或不收敛的工程状态静默替换为零或旧值。
- 保持公共名称、结果键、单位和科学行为，除非 Task 10 明确要求变更。
- 所有脚本结果遵守 `output/<脚本名>/` 约定。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 主计划全文与索引、状态并行输出被截断 | 1 | 改用标题行号定位后读取 Task 10 局部区间 |
| PowerShell 下把 `src/*.jl` 作为 `rg` 路径参数导致 Windows 路径错误 | 1 | 结果已由 `src/JuBat.jl` 命中充分确认；后续目录搜索改用 `rg ... src -g '*.jl'` |
| 单个 `apply_patch` 同时 Delete/Add 同一路径被拒绝 | 1 | 改为每个文件先定点删除、再立即用 `apply_patch` 新增；不重复无效补丁格式 |
| 全工作区 `git diff --check` 报错 | 1 | 其中多数为 Task 10 前已有源码空白；Task 10 新文档有 2 处 Markdown 行尾空格和 7 个 EOF 空行，先定点修复，再对 Task 10 路径单独复查并保留全局既有问题说明 |
