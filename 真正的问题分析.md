# 真正的问题：电流设置与参数不匹配

## 问题根源对比

### ✅ thermal_example.jl（LG M50参数 - 正常）

```julia
param_dim = JuBat.ChooseCell("LG M50")
I1C = 5  // thermal_example.jl 第12行
Crates = 1
opt.Current = x-> I1C * Crates  // 5A × 1C = 5A
```

**参数文件（LGM50.jl）：**
```julia
cell.I1C = 5.      // 第90行
cell.capacity = 5  // 第92行
```

**归一化：**
```
I_typ = cell.I1C = 5A
实际电流 = 5A
无量纲电流 I_nd = 5A / 5A = 1.0 ✓

放电时间 = 3600s = 1小时
放电容量 = 5A × 1h = 5Ah（正好是容量）
→ 完整放电，电压从4.13V降到2.5V ✓
```

---

### ❌ testexample.jl（Jellyroll参数 - 异常）

```julia
param_dim = JuBat.ChooseCell("Jellyroll")
Crates = 1.0  // 第40行
i = 5 * Crates  // 第41行 - 5A × 1C = 5A ❌ 错误！
opt.Current = x -> i
```

**参数文件（Jellyroll.jl）：**
```julia
cell.I1C = 60      // 第102行
cell.capacity = 60 // 第104行
```

**归一化：**
```
I_typ = cell.I1C = 60A
实际电流 = 5A  ❌ 应该是 60A × 1C = 60A！
无量纲电流 I_nd = 5A / 60A = 0.083 ❌ 太小了！

放电时间 = 360s = 6分钟（或3600s = 1小时）
放电容量 = 5A × 0.1h = 0.5Ah（仅占容量的 0.5/60 = 0.83%）
→ 几乎没放电，电压几乎不变 ✗
```

---

## 详细影响分析

### 1. 电压降过小

```
正常情况（thermal_example）:
  完整放电 5Ah/5Ah = 100%
  → 电压降：4.13V → 2.5V ✓

异常情况（testexample）:
  仅放电 0.5Ah/60Ah = 0.83%
  → 电压降：4.13V → 4.176V（仅0.046V）✗
```

用户报告的结果验证：
```
testexample_simple_coupling.jl 输出：
  实际时间步数: 365
  最终电压: 4.176 V  ← 几乎没变！
  温度升高: 1.35 K   ← 很小！
  平均热源: 0.07 W   ← 太小了！
```

### 2. 热源过小

热源主要来自：
1. **反应热**: Q_rxn ∝ I × η  
2. **欧姆热**: Q_ohm ∝ I²
3. **可逆热**: Q_rev ∝ I × T × dU/dT

由于 I_nd = 0.083（而不是1.0），导致：
```
Q_actual / Q_expected = (0.083)² = 0.0069

即热源仅为预期的 0.7%！
```

**验证：**
```
实际热源 = 0.07 W
预期热源 = 0.07 / 0.0069 ≈ 10 W（合理范围）
```

### 3. 温升不明显

```
热功率 = 0.07 W
热容 = m × cp ≈ 0.85 kg × 1329 J/kg/K ≈ 1130 J/K
时间 = 3600 s

温升 = Q × t / (m × cp) = 0.07 × 3600 / 1130 ≈ 0.22 K

但实际温升 = 1.35 K（比计算值大，可能因为：
  1. 对流散热系数 h = 150 W/m²/K 在计算中
  2. 实际放电时间可能不同
  3. 温度场不均匀）
```

---

## 正确的电流设置方法

### 方法1：根据I1C设置（推荐）

```julia
# testexample.jl 修改第40-42行
param_dim = JuBat.ChooseCell("Jellyroll")
Crates = 1.0  # C-rate
i = param_dim.cell.I1C * Crates  # 使用参数文件中的I1C
opt.Current = x -> i
```

这样：
```
i = 60A × 1C = 60A
I_nd = 60A / 60A = 1.0 ✓
```

### 方法2：使用小容量参数（调试用）

如果只是为了测试，可以修改参数文件：

```julia
# src/parameters/Jellyroll.jl 修改第102-104行
cell.I1C = 5      # 改为5A（从60A）
cell.capacity = 5 # 改为5Ah（从60Ah）
```

然后testexample.jl中的`i = 5 * Crates`就正确了。

**但这不是真实的Jellyroll电池参数！**

---

## 其他发现的参数问题（次要）

### 1. 正极电导率过低

```julia
PE.sig = 0.18 S/m  // Jellyroll.jl 第26行
```

对比：
- LG M50: `PE.sig` 未明确设置（可能从默认值或文献）
- Northrop: `PE.sig = 3.8 S/m`（第23行）

0.18 S/m 太低了！正常值应该在 1-100 S/m。

**影响：**
- 增大欧姆热（∝ I²/σ）
- 增大极化
- 但由于电流太小（0.083×60A=5A），影响被掩盖了

### 2. dUdT = 0（可逆热为0）

```julia
PE.dUdT = x-> 0 * x  // 第36行
NE.dUdT = x-> 0 * x  // 第64行
```

对比：
- LG M50: 也是0（第32、59行）
- Northrop: 有完整的函数

**影响：**
- 减少总热源约10-30%
- 但不是主要问题

---

## 修改建议

### 优先级1：修正电流设置 ⭐⭐⭐

**修改 `example/testexample.jl` 第41行：**

```julia
# 修改前
i = 5 * Crates  # 电流 (A)

# 修改后
i = param_dim.cell.I1C * Crates  # 使用参数文件中的I1C
```

**预期结果：**
```
电流 = 60A × 1C = 60A
放电时间 = 360s → 放电6Ah（10%容量）
电压降 ≈ 0.3-0.5V（合理）
热源 ≈ 10-50W（合理）
温升 ≈ 10-30K（合理）
```

### 优先级2：调整放电时间（可选）

**修改 `example/testexample.jl` 第53行：**

```julia
# 修改前
opt.time = [0.0, 360]  # 仿真时间 (s) - 仅6分钟

# 修改后
opt.time = [0.0, 3600]  # 仿真时间 (s) - 1小时，完整放电
```

### 优先级3：修正参数（辅助）

**修改 `src/parameters/Jellyroll.jl`：**

```julia
# 第26行
PE.sig = 10.0  # 从0.18改为10.0

# 第36、64行 - 添加熵变系数（可选，参考Northrop.jl）
PE.dUdT = x-> -0.001 * (...)
NE.dUdT = x-> 0.001 * (...)
```

---

## testexample_simple_coupling.jl 也需要同样修改

**修改 `example/testexample_simple_coupling.jl` 第45行：**

```julia
# 修改前
i = 5 * Crates  # 电流 (A)

# 修改后  
i = param_dim.cell.I1C * Crates  # 60A
```

---

## 总结

### 问题根源
- ❌ **电流设置错误**：使用了5A而不是60A
- ❌ **归一化失配**：I_nd = 5/60 = 0.083 而不是 1.0
- ❌ **放电不足**：仅放电0.83%容量，电压几乎不变

### 解决方案
✅ 修改电流为 `i = param_dim.cell.I1C * Crates`
✅ 延长放电时间到3600s（可选）
✅ 修正 PE.sig = 10.0（辅助）

### 预期结果（修改后）
- 电压：4.13V → 2.5V（完整放电）
- 热源：10-50W（合理）
- 温升：10-30K（明显）

---

## 附：不同参数文件对比

| 参数文件 | I1C | capacity | 适用电流 | thermal_example使用 |
|---------|-----|----------|---------|-------------------|
| LG M50 | 5A | 5Ah | 5A | ✓ |
| Northrop | 60A | 60Ah | 60A | |
| Jellyroll | 60A | 60Ah | 60A | |
| Enertech | 2.28A | 2.28Ah | 2.28A | |

**关键：电流必须与参数文件的 I1C 匹配！**
