# 电压降对比分析：thermal_example vs testexample_simple_coupling

## 问题

修改电流后，testexample_simple_coupling的电压降仍然不明显，与thermal_example相比差异很大。

---

## 参数对比

### thermal_example.jl（LG M50 - 正常）

```julia
param_dim = JuBat.ChooseCell("LG M50")
param_dim.cell.v_l = 2.5

I1C = 5                          // 5A
Crates = 1                       // 1C
opt.Current = x-> I1C * Crates   // 5A
opt.time = [0 3600/Crates[i]]    // 3600s
opt.dt = [1 10] / Crates[i]      // [1, 10]
opt.model = "SPMe"
opt.thermalmodel = "lumped"
```

**参数文件（LGM50.jl）：**
```julia
cell.I1C = 5.0
cell.capacity = 5.0
PE.theta_100 = 0.263849
PE.theta_0 = 0.853974
PE.cs0 = 17038
NE.theta_100 = 0.910612
NE.theta_0 = 0.0263472
NE.cs0 = 29866
PE.U = x-> -0.8090*x + 4.4875 - ...  // OCP曲线
NE.U = x-> 1.97938*exp(-39.3631*x) + ...
```

**关键计算：**
```
电流 = 5A
放电时间 = 3600s = 1小时
放电容量 = 5A × 1h = 5Ah

初始SOC计算：
  theta_p0 = 0.853974
  theta_n0 = 0.0263472
  
  SOC0 = (theta_n0 - theta_0_n) / (theta_100_n - theta_0_n)
       = (0.0263472 - 0.0263472) / (0.910612 - 0.0263472)
       = 0 / 0.88426 = 0%  ❌ 这个计算不对
       
  实际SOC应该从正极计算：
  SOC0 = (theta_0_p - theta_p0) / (theta_0_p - theta_100_p)
       = (0.853974 - 0.853974) / (0.853974 - 0.263849)
       = 0% ❌ 也不对

  让我重新理解：
  cs0 = 17038 (正极初始浓度)
  cs_max = 63104
  SOC_p = cs0 / cs_max = 17038 / 63104 = 27%
  
  cs0 = 29866 (负极)
  cs_max = 33133
  SOC_n = cs0 / cs_max = 29866 / 33133 = 90%
```

等等，这里有问题。让我重新查看cs_max。

---

### testexample_simple_coupling.jl（Jellyroll - 修改后）

```julia
param_dim = JuBat.ChooseCell("Jellyroll")
param_dim.cell.v_l = 2.5
param_dim.cell.v_h = 4.2

Crates = 1.0
i = param_dim.cell.I1C * Crates  // 60A（修改后）
opt.Current = x -> i
opt.time = [0.0, 3600]           // 3600s
opt.dt = [0.5, 10.0]
opt.model = "SPMe"
opt.thermalmodel = "distributed2D"
opt.simple_thermal_coupling = true
```

**参数文件（Jellyroll.jl）：**
```julia
cell.I1C = 60
cell.capacity = 60
PE.theta_100 = 0.263849    // 与LG M50相同
PE.theta_0 = 0.853974      // 与LG M50相同
PE.cs0 = 17038             // 与LG M50相同
NE.theta_100 = 0.910612    // 与LG M50相同
NE.theta_0 = 0.0263472     // 与LG M50相同
NE.cs0 = 29866             // 与LG M50相同
PE.U = ...                 // 与LG M50相同
NE.U = ...                 // 与LG M50相同
```

**关键计算：**
```
电流 = 60A（修改后）
放电时间 = 3600s = 1小时
放电容量 = 60A × 1h = 60Ah
```

---

## 关键发现：初始SOC可能不是100%！

让我检查cs0的含义：
