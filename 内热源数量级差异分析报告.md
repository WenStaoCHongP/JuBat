# 内热源数量级差异分析报告

## 执行摘要

采用集总模型的内热源计算时，`thermal_example` 和 `testexample_simple_coupling` 两个例子计算得到的内热源数量级相差约 **1035 倍**。

- **thermal_example (集总模型)**: ~0.77 W ✓ 合理
- **testexample_simple_coupling (简化耦合)**: ~798 W ✗ 偏大1035倍

## 根本原因

### 不同的无量纲化尺度

两个模型使用了完全不同的无量纲化哲学：

| 模型 | 尺度定义 | 数值 | 单位 | 物理意义 |
|------|----------|------|------|----------|
| 集总模型 | `I_typ × phi` | 0.1285 | W | 电功率尺度 |
| 简化耦合模型 | `k × T_ref / L_th²` | 5.68×10⁶ | W/m³ | 热传导尺度 |

**尺度差异**: 将两者转换为等效功率尺度后，相差约 **1035 倍**

```
(k × T / L²) × V_cell   133.0 W
─────────────────────── = ────── ≈ 1035
    I_typ × phi         0.129 W
```

## 详细分析

### 1. 集总模型的实现

**文件**: `src/Thermal.jl` (第61-62行)

```julia
heat_internal_nd = Q_rxn + Q_ohm + Q_rev     # 无量纲
heat_internal_W = heat_internal_nd × I_typ × phi  # 有量纲 [W]
```

**数值**:
- 无量纲热源: 5.7-6.2 (从日志观察)
- 有量纲热源: 0.73-0.80 W
- 尺度因子: I_typ × phi = 5 × 0.0257 = 0.1285 W

### 2. 简化耦合模型的实现

**文件**: `src/CallModel_SimpleCoupling.jl` (第61-93行)

```julia
Q_layers = _compute_layer_heat_sources(...)  # 无量纲体积热源密度
q_elem[e] = sum(layer_weights[e, i] × Q_layers[i])
total_heat_W = q_th × sum(q_elem .× elem_volumes)  # [W]
```

**关键**: `q_th` 的计算 (`src/SetParams.jl` 第262行)

```julia
L_th = param_dim.cell.Rout  # Jellyroll: 0.0105 m
q_ref = k_ref * param_dim.scale.T_ref / L_th^2
```

**数值**:
- L_th = 0.0105 m (外半径)
- k_th = 2.1 W/(m·K)
- T_ref = 298 K
- q_th = 2.1 × 298 / (0.0105)² = **5.68×10⁶ W/m³**

### 3. 问题所在

`q_th = k × T / L²` 这个公式来自**傅里叶热传导方程**的无量纲化：

```
∂T/∂t = α ∇²T + q
```

无量纲化后：
- 温度尺度: T_ref
- 长度尺度: L_th
- 时间尺度: L_th² / α
- 热源尺度: k × T_ref / L_th²

这在**纯热传导问题**中是合理的，但在**电池产热问题**中：

✗ 热源不是由温度梯度驱动的
✗ 热源来自电化学反应和欧姆热
✓ 热源应该与电功率相关: **q ~ I × V / V_cell**

### 4. L_th 的影响

`q_th ∝ 1 / L_th²`，因此 L_th 的选择极其敏感：

| L_th 选择 | 值 | q_th (W/m³) | Q_total (W) |
|-----------|-----|-------------|-------------|
| Rout (当前) | 10.5 mm | 5.68×10⁶ | 798 |
| 径向间隙 | 8.58 mm | 8.50×10⁶ | 1193 |
| 电化学层厚度 | 173 μm | 2.10×10¹⁰ | 2.95×10⁶ |
| 高度 | 70 mm | 1.28×10⁵ | 3.0 |

**观察**: 
- 用越小的 L_th，q_th 越大
- 用电化学层厚度最糟糕（~300万倍）
- 即使用 Rout，仍偏大1000倍

## 数值验证

假设无量纲热源值 = 6.0（从实际运行日志观察到）：

### 集总模型
```
Q_total = 6.0 × 0.1285 W = 0.771 W  ✓
```
与实测值 (0.73-0.80 W) 一致

### 简化耦合模型（当前实现）
```
Q_total = 6.0 × 5.68×10⁶ W/m³ × 2.34×10⁻⁵ m³
        = 798 W  ✗
```
偏大约1035倍！

### 简化耦合模型（建议实现）
```
q_th_new = I_typ × phi / V_cell
         = 5 × 0.0257 / 2.34×10⁻⁵
         = 5483 W/m³

Q_total = 6.0 × 5483 × 2.34×10⁻⁵
        = 0.771 W  ✓
```
与集总模型完全一致！

## 解决方案

### 推荐修改

修改 `src/SetParams.jl` 第257-270行：

```julia
# ============== 当前实现 ==============
# Characteristic thermal length: use outer radius if available
L_th = param_dim.cell.Rout
k_ref = param_dim.PE.lambda > 0 ? param_dim.PE.lambda : 
        (param_dim.NE.lambda > 0 ? param_dim.NE.lambda : 1.0)
rho_c_ref = param_dim.cell.rho * param_dim.cell.heat_Q
q_ref = k_ref * param_dim.scale.T_ref / L_th^2  # ← 问题所在
t_th = rho_c_ref * L_th^2 / k_ref
h_ref = param_dim.cell.h * L_th / k_ref

# ============== 建议修改 ==============
# For electrochemical heat generation, use power-based scaling
L_th = param_dim.cell.Rout
k_ref = param_dim.PE.lambda > 0 ? param_dim.PE.lambda : 
        (param_dim.NE.lambda > 0 ? param_dim.NE.lambda : 1.0)
rho_c_ref = param_dim.cell.rho * param_dim.cell.heat_Q

# Use power-based heat source scaling instead of Fourier scaling
q_ref = param_dim.scale.I_typ * param_dim.scale.phi / param_dim.cell.volume

t_th = rho_c_ref * L_th^2 / k_ref
h_ref = param_dim.cell.h * L_th / k_ref
```

### 理由

1. **物理一致性**: 电池热源来自电功率损耗，应该用电功率尺度
2. **数值合理性**: 确保总热源在合理范围 (0.5-2 W)
3. **模型一致性**: 集总模型和分布式模型使用相同的尺度
4. **单位正确性**: 
   - `I_typ × phi` 单位为 W
   - `volume` 单位为 m³
   - `q_ref` 单位为 W/m³ ✓

## 验证计划

修改后需要验证：

1. **运行 thermal_example.jl**
   - 预期: 不受影响（集总模型不使用 q_th）
   - 输出: ~0.77 W

2. **运行 testexample_simple_coupling.jl**
   - 预期: 总热源从 ~800 W 降到 ~0.77 W
   - 验证: 与集总模型一致

3. **物理合理性检查**
   - 温度场应该仍然合理
   - 温升应该在几K以内
   - 热源分布应该反映电化学活性

## 影响评估

### 受影响的文件

1. `src/SetParams.jl` - 需要修改
2. `src/CallModel_SimpleCoupling.jl` - 不需要修改（逻辑正确）
3. `src/ThermalDistributed.jl` - 不需要修改
4. `example/testexample_simple_coupling.jl` - 不需要修改

### 向后兼容性

- **集总模型**: 不受影响
- **分布式模型**: 数值会改变，但更合理
- **建议**: 更新所有使用分布式热模型的测试案例

## 结论

### 造成内热源数量级差异的原因

1. **不同的无量纲化哲学**
   - 集总模型: 基于电功率 (I×V)
   - 分布式模型: 基于傅里叶热传导 (k×T/L²)

2. **尺度因子差异巨大**
   - 相差约1035倍（考虑体积转换后）

3. **L_th 选择不当**
   - 用 Rout 作为热特征长度
   - 导致 q_th 数值过大
   - L_th 的平方反比关系放大误差

4. **物理意义不匹配**
   - 傅里叶尺度适用于热传导
   - 电池产热应该用电功率尺度

### 最终建议

**立即修改** `SetParams.jl` 中 `q_th` 的定义，改用：

```julia
q_ref = param_dim.scale.I_typ * param_dim.scale.phi / param_dim.cell.volume
```

这样可以：
- ✓ 确保数值合理
- ✓ 保持模型间一致性
- ✓ 物理意义清晰
- ✓ 便于验证和调试

---

**报告生成时间**: 2025-12-08  
**分析工具**: `/workspace/tools/analyze_scale_numerics.py`  
**相关文档**: 
- `/workspace/docs/内热源差异分析_简要版.md`
- `/workspace/docs/内热源数量级差异分析.md`
