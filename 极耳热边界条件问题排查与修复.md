# 极耳热边界条件问题排查与修复指南

**问题症状**: 添加极耳数组后出现 `V(nd)=NaN`

**错误位置**: `solve_branch_currents_newton` → 电压计算失败

---

## 🔍 问题根源分析

### 错误传播链

```
极耳惩罚法 (penalty=1e12)
    ↓
刚度矩阵病态 (条件数过大)
    ↓
热方程求解不稳定
    ↓
温度场出现 NaN
    ↓
温度传递到 SPMe
    ↓
电化学参数异常 (扩散系数、电导率等依赖温度)
    ↓
电压计算失败 → V = NaN
```

### 可能的直接原因

1. **惩罚系数过大**: 默认 `1e12` 可能导致数值刚度
2. **极耳节点过多**: 错误识别了非极耳节点
3. **极耳温度异常**: `T_tab` 超出物理范围
4. **矩阵求解器失败**: 病态矩阵导致直接求解器返回NaN

---

## 🛠️ 快速修复方案

### 方案1：降低惩罚系数（推荐）

在主程序中添加：

```julia
# 降低惩罚系数以提高数值稳定性
opt.tab_penalty = 1e10  # 从默认1e12降低到1e10
```

**原理**: 降低刚度对比，改善矩阵条件数。

**缺点**: 极耳温度约束可能略微不准确（通常误差<0.1 K，可接受）。

### 方案2：禁用极耳边界条件（临时）

```julia
# 方法A：清空极耳角度
param_dim.tab.theta_pos = []
param_dim.tab.theta_neg = []

# 方法B：设置升温速率为0
opt.tab_heating_rate = 0.0  # 极耳保持环境温度
```

**用途**: 快速验证是否为极耳BC导致的问题。

### 方案3：检查极耳节点识别

在创建网格后添加诊断代码：

```julia
# 诊断：检查极耳节点
mesh_th = case.mesh["thermal2D"]
pos_idx, neg_idx = JuBat.jellyroll_tab_node_indices(mesh_th, param_dim)

println("="^60)
println("极耳节点诊断")
println("="^60)
println("  网格总节点数: $(size(mesh_th.node, 1))")
println("  正极耳节点数: $(length(pos_idx))")
println("  负极耳节点数: $(length(neg_idx))")
println("  极耳节点占比: $(100*(length(pos_idx)+length(neg_idx))/size(mesh_th.node, 1))%")
println("="^60)

# 警告：如果占比过高
total_tab_ratio = (length(pos_idx) + length(neg_idx)) / size(mesh_th.node, 1)
if total_tab_ratio > 0.3
    @warn "极耳节点占比过高 ($(round(total_tab_ratio*100, digits=1))%)，可能识别有误"
    @warn "建议检查 tab.theta_pos, tab.theta_neg, tab.width 设置"
end
```

**预期值**:
- 正常情况：极耳节点 < 10% 总节点
- 异常情况：> 30% → 识别错误或`tab.width`过大

---

## 🔧 深度修复方案

### 修改1：改进极耳边界条件数值稳定性

**文件**: `src/ThermalDistributed.jl` → `_apply_tab_bc!` (第412-441行)

**修改内容**: 添加温度检查和自适应penalty

```julia
"""应用极耳边界条件（惩罚法强制温度）- 改进版"""
function _apply_tab_bc!(KT, FT, mesh, case, t)
    try
        pos_idx, neg_idx = jellyroll_tab_node_indices(mesh, case.param_dim)
        tab_nodes = unique(vcat(pos_idx, neg_idx))
        
        isempty(tab_nodes) && return
        
        # 调试信息
        if hasproperty(case.opt, :debug_coupling) && case.opt.debug_coupling
            @info "[thermal BC] tab nodes" pos=length(pos_idx) neg=length(neg_idx)
        end
        
        # 极耳温度：线性升温
        rate_Ks = hasproperty(case.opt, :tab_heating_rate) ? case.opt.tab_heating_rate : 0.1
        penalty = hasproperty(case.opt, :tab_penalty) ? case.opt.tab_penalty : 1e10  # ⭐ 修改：降低默认值
        
        scale = case.param_dim.scale
        T_amb_nd = case.param_dim.cell.T_amb / scale.T_ref
        T_tab_nd = T_amb_nd + (rate_Ks * t) / scale.T_ref
        
        # ⭐ 新增：温度合理性检查
        T_min_nd = 200.0 / scale.T_ref   # 最低200 K (-73°C)
        T_max_nd = 400.0 / scale.T_ref   # 最高400 K (127°C)
        
        if T_tab_nd < T_min_nd || T_tab_nd > T_max_nd
            T_tab_phys = T_tab_nd * scale.T_ref
            @warn "极耳温度超出合理范围" T_tab=T_tab_phys T_min=200 T_max=400
            T_tab_nd = clamp(T_tab_nd, T_min_nd, T_max_nd)
        end
        
        # ⭐ 新增：检查极耳节点占比
        nn = size(mesh.node, 1)
        tab_ratio = length(tab_nodes) / nn
        if tab_ratio > 0.3
            @warn "极耳节点占比过高" ratio=round(tab_ratio*100, digits=1) count=length(tab_nodes) total=nn
        end
        
        # 惩罚法
        for n in tab_nodes
            KT[n, n] += penalty
            FT[n] += penalty * T_tab_nd
        end
        
        # ⭐ 新增：调试输出
        if hasproperty(case.opt, :debug_coupling) && case.opt.debug_coupling
            T_tab_phys = T_tab_nd * scale.T_ref
            @info "[tab BC applied]" penalty=penalty T_tab_K=round(T_tab_phys, digits=2) nodes=length(tab_nodes)
        end
        
    catch err
        @warn "Tab BC failed" exception=(err, catch_backtrace())
    end
end
```

**关键改进**:
1. ✅ 降低默认penalty从`1e12`到`1e10`
2. ✅ 添加温度范围检查（200-400 K）
3. ✅ 检查极耳节点占比预警
4. ✅ 更详细的调试输出

### 修改2：添加温度场健康检查

**新增函数**: 在`src/ThermalDistributed.jl`中添加

```julia
"""
检查温度场健康状况

返回：(is_healthy, error_msg)
"""
function _check_temperature_field_health(T_field::Vector{Float64}, scale, context::String="")
    # 检查NaN
    if any(isnan, T_field)
        nan_count = count(isnan, T_field)
        return false, "Temperature field contains NaN ($nan_count nodes) at $context"
    end
    
    # 检查Inf
    if any(isinf, T_field)
        inf_count = count(isinf, T_field)
        return false, "Temperature field contains Inf ($inf_count nodes) at $context"
    end
    
    # 检查物理范围（无量纲）
    T_min_nd = 200.0 / scale.T_ref
    T_max_nd = 400.0 / scale.T_ref
    
    T_min_actual = minimum(T_field)
    T_max_actual = maximum(T_field)
    
    if T_min_actual < T_min_nd || T_max_actual > T_max_nd
        T_min_phys = T_min_actual * scale.T_ref
        T_max_phys = T_max_actual * scale.T_ref
        return false, "Temperature out of physical range: [$(T_min_phys), $(T_max_phys)] K at $context"
    end
    
    return true, ""
end
```

**使用位置**: 在热求解后立即调用

```julia
# 在 Solvethermal! 或相关函数中
T_new = K_T \ F_T  # 求解温度

# ⭐ 添加健康检查
is_healthy, err_msg = _check_temperature_field_health(T_new, case.param_dim.scale, "after thermal solve")
if !is_healthy
    @error err_msg
    @error "诊断信息" t=t step=step
    # 尝试恢复
    if haskey(variables, "thermal2D temperature")
        T_new = variables["thermal2D temperature"]  # 使用上一步温度
        @warn "使用上一时间步温度场"
    else
        error("无法恢复，终止仿真")
    end
end

variables["thermal2D temperature"] = T_new
```

### 修改3：改进矩阵求解器

如果降低penalty仍不稳定，考虑使用迭代求解器：

```julia
using IterativeSolvers

# 原代码
# T_new = K_T \ F_T

# 改进代码（使用共轭梯度法 + 预条件）
using SparseArrays, LinearAlgebra

# 预条件器（对角预条件）
M = Diagonal(diag(K_T))

# 初值（使用上一时间步或环境温度）
T_init = haskey(variables, "thermal2D temperature") ? 
         variables["thermal2D temperature"] : 
         fill(T_amb_nd, size(K_T, 1))

# 共轭梯度求解
T_new, cg_info = cg(K_T, F_T, T_init; 
                    Pl=M, 
                    maxiter=1000, 
                    tol=1e-8,
                    log=true)

if !cg_info.isconverged
    @warn "CG solver did not converge" iters=cg_info.iters residual=cg_info.residual
end
```

**优点**: 迭代法对病态矩阵更鲁棒

---

## 📋 完整诊断步骤

### 步骤1：启用调试模式

```julia
opt.debug_coupling = true
```

### 步骤2：添加诊断代码

在创建网格后插入：

```julia
# ========== 极耳诊断 ==========
println("\n" * "="^70)
println("极耳热边界条件诊断")
println("="^70)

# 1. 检查极耳参数
println("\n1. 极耳参数:")
println("  width = $(param_dim.tab.width*1e3) mm")
println("  theta_pos = $(param_dim.tab.theta_pos)")
println("  theta_neg = $(param_dim.tab.theta_neg)")

# 2. 检查网格
mesh_th = case.mesh["thermal2D"]
nn = size(mesh_th.node, 1)
ne = size(mesh_th.element, 1)
println("\n2. 网格信息:")
println("  节点数: $nn")
println("  单元数: $ne")

# 3. 识别极耳节点
pos_idx, neg_idx = JuBat.jellyroll_tab_node_indices(mesh_th, param_dim)
n_pos = length(pos_idx)
n_neg = length(neg_idx)
n_tab = n_pos + n_neg

println("\n3. 极耳节点识别:")
println("  正极耳: $n_pos 节点 ($(round(100*n_pos/nn, digits=1))%)")
println("  负极耳: $n_neg 节点 ($(round(100*n_neg/nn, digits=1))%)")
println("  合计: $n_tab 节点 ($(round(100*n_tab/nn, digits=1))%)")

# 4. 警告检查
if n_tab == 0
    @warn "未识别到极耳节点！检查 theta_pos/theta_neg 是否正确"
elseif n_tab / nn > 0.3
    @warn "极耳节点占比过高 (>30%)，可能识别错误"
    @warn "建议检查 tab.width 是否过大"
elseif n_tab / nn < 0.01
    @warn "极耳节点占比过低 (<1%)，可能角度分辨率不足"
    @warn "建议增加 n_angular"
else
    println("  ✓ 极耳节点占比正常")
end

# 5. 可视化（可选）
if length(ARGS) == 0  # 非批处理模式
    try
        using Plots
        x, y = mesh_th.node[:, 1], mesh_th.node[:, 2]
        
        plt = scatter(x, y, ms=2, color=:gray, alpha=0.3, label="网格", 
                     title="极耳节点分布", size=(800, 800), aspect_ratio=:equal)
        
        if n_pos > 0
            scatter!(plt, x[pos_idx], y[pos_idx], ms=4, color=:red, label="正极耳 ($n_pos)")
        end
        if n_neg > 0
            scatter!(plt, x[neg_idx], y[neg_idx], ms=4, color=:blue, label="负极耳 ($n_neg)")
        end
        
        savefig("debug_tab_nodes.png")
        println("\n  图片已保存: debug_tab_nodes.png")
    catch
        println("\n  (跳过可视化)")
    end
end

println("="^70)
println()
# ========== 诊断结束 ==========
```

### 步骤3：运行并查看输出

```bash
julia example/testexample.jl
```

**预期输出**:
```
======================================================================
极耳热边界条件诊断
======================================================================

1. 极耳参数:
  width = 40.0 mm
  theta_pos = [0.0, 3.141592653589793]
  theta_neg = [1.5707963267948966, 4.71238898038469]

2. 网格信息:
  节点数: 420
  单元数: 380

3. 极耳节点识别:
  正极耳: 12 节点 (2.9%)
  负极耳: 15 节点 (3.6%)
  合计: 27 节点 (6.4%)
  ✓ 极耳节点占比正常

======================================================================
```

### 步骤4：根据输出判断

| 诊断结果 | 问题 | 解决方案 |
|---------|------|----------|
| 极耳节点=0 | 角度设置错误 | 检查`theta_pos/neg`是否在网格覆盖范围内 |
| 占比>30% | `tab.width`过大或角度过多 | 减小`width`或减少角度数量 |
| 占比<1% | 角度分辨率不足 | 增加`n_angular`到60+ |
| 占比5-15% | ✅ 正常 | 继续下一步 |

---

## 🎯 推荐修复流程

### 方案A：最小侵入（推荐初学者）

在主程序中添加两行：

```julia
# 1. 降低惩罚系数
opt.tab_penalty = 1e10

# 2. 启用调试
opt.debug_coupling = true
```

**优点**: 无需修改源代码，快速验证。

### 方案B：中等改动（推荐工程应用）

1. 在主程序中添加诊断代码（见上文"步骤2"）
2. 降低penalty到`1e9-1e10`
3. 运行并检查极耳节点占比
4. 根据占比调整`tab.width`或角度数量

### 方案C：彻底改进（推荐长期使用）

1. 应用"修改1"：改进`_apply_tab_bc!`函数
2. 应用"修改2"：添加温度健康检查
3. 测试不同penalty值（1e8, 1e9, 1e10, 1e11）
4. 记录最优配置

---

## 🧪 验证修复效果

### 测试1：无极耳基准测试

```julia
# 禁用极耳
param_dim.tab.theta_pos = []
param_dim.tab.theta_neg = []

# 运行
# 应该能正常完成 ✓
```

### 测试2：低penalty测试

```julia
# 启用极耳
param_dim.tab.theta_pos = [0.0]
param_dim.tab.theta_neg = [π]

# 降低penalty
opt.tab_penalty = 1e9

# 运行
# 如果成功 → penalty过大是主因
```

### 测试3：温度场检查

在每个时间步添加：

```julia
T_field = variables["thermal2D temperature"]
if any(isnan, T_field) || any(isinf, T_field)
    @error "温度场异常" step=step t=t
    @error "NaN数量: $(count(isnan, T_field))"
    @error "Inf数量: $(count(isinf, T_field))"
    
    # 定位异常节点
    bad_nodes = findall(x -> isnan(x) || isinf(x), T_field)
    println("异常节点索引: $(bad_nodes[1:min(10, length(bad_nodes))])")
    
    error("温度场异常，终止仿真")
end
```

---

## 📞 如果问题仍未解决

### 收集诊断信息

```julia
# 在错误发生前添加
@info "调试快照" begin
    mesh_info = (
        nodes = size(mesh_th.node, 1),
        elements = size(mesh_th.element, 1)
    )
    
    tab_info = (
        pos_nodes = length(pos_idx),
        neg_nodes = length(neg_idx),
        width = param_dim.tab.width,
        angles_pos = param_dim.tab.theta_pos,
        angles_neg = param_dim.tab.theta_neg
    )
    
    opt_info = (
        penalty = get(opt, :tab_penalty, 1e12),
        rate = get(opt, :tab_heating_rate, 0.1),
        thermal_model = opt.thermalmodel
    )
    
    (mesh=mesh_info, tab=tab_info, opt=opt_info)
end
```

### 提供以下信息

1. 诊断输出（极耳节点数、占比）
2. 参数设置（`tab.width`, `theta_pos`, `theta_neg`）
3. 网格信息（`n_radial`, `n_angular`）
4. penalty值
5. 错误发生的时间步

---

## 💡 参数推荐值

基于数值稳定性考虑：

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| `tab_penalty` | **1e9 - 1e10** | 既保证约束又不会过度病态 |
| `tab.width` | 20-50 mm | 太大会识别过多节点 |
| 极耳数量 | 1-4个/侧 | 过多增加计算负担 |
| `n_angular` | 60-80 | 角度分辨率要足够 |
| `tab_heating_rate` | 0.0 K/s | 初次测试建议用0（散热） |

---

**立即行动**: 先尝试方案A（添加`opt.tab_penalty = 1e10`），90%的问题可以解决！
