# 精确边界识别使用说明

## 核心思想

**基于螺旋线方程的精确判断**，通过计算节点到理论螺旋线的距离来确定节点是否在边界上。

### 螺旋线方程

- **内螺旋**：r(θ) = a + b·θ
- **外螺旋**：r(θ) = a + b·θ + t_repeat

### 判断方法

对于节点 (x, y)：

1. **转换为极坐标**：(r, φ)

2. **计算累计角度 θ_cum**：
   - 内螺旋：θ_cum = (r - a) / b
   - 外螺旋：θ_cum = (r - a - t_repeat) / b

3. **检查 θ 范围**：θ_cum ∈ [θ_min, θ_max]

4. **验证到螺旋线的距离**：
   - 理论位置：(x_theo, y_theo) = r_theo · (cos(θ_cum), sin(θ_cum))
   - 实际距离：dist = √[(x - x_theo)² + (y - y_theo)²]
   - 判断：dist ≤ tol

---

## API 使用

### 函数签名

```julia
edge_boundary(mesh, node_idx, param_dim; 
              which=:inner/:outer, 
              theta_range=(θ_min, θ_max), 
              tol=1e-4)
```

### 参数说明

| 参数 | 类型 | 说明 |
|------|------|------|
| `mesh` | Mesh | 网格对象 |
| `node_idx` | Int | 节点索引 |
| `param_dim` | Params | 参数对象 |
| `which` | Symbol | `:inner`（内螺旋）或 `:outer`（外螺旋），默认 `:inner` |
| `theta_range` | Tuple/Nothing | θ 范围 (θ_min, θ_max)，默认 nothing（自动） |
| `tol` | Float64 | 距离容差（单位：m），默认 1e-4 |

### 默认 θ 范围

如果不指定 `theta_range`：
- **内边界**：θ ∈ [0, 2π]（第1圈）
- **外边界**：θ ∈ [2π(N-1), 2π*N]（第N圈）

---

## 使用示例

### 示例1：识别第1圈的内螺旋

```julia
# 使用默认范围 [0, 2π]
is_inner = edge_boundary(mesh, i, param_dim; which=:inner)

# 或显式指定范围和容差
is_inner = edge_boundary(mesh, i, param_dim; 
                         which=:inner, 
                         theta_range=(0.0, 2π), 
                         tol=1e-4)
```

### 示例2：识别第N圈的外螺旋

```julia
# 使用默认范围 [2π(N-1), 2π*N]
is_outer = edge_boundary(mesh, i, param_dim; which=:outer)

# 或显式指定
p = jellyroll_spiral_params(param_dim)
N = Int(p.n_wind)
is_outer = edge_boundary(mesh, i, param_dim; 
                         which=:outer, 
                         theta_range=(2π*(N-1), 2π*N), 
                         tol=1e-4)
```

### 示例3：识别任意圈的螺旋线段

```julia
# 识别第5圈的内螺旋
is_inner_5 = edge_boundary(mesh, i, param_dim; 
                           which=:inner, 
                           theta_range=(2π*4, 2π*5), 
                           tol=1e-4)

# 识别第0圈的外螺旋（collector_seed_mesh 实际覆盖）
is_outer_0 = edge_boundary(mesh, i, param_dim; 
                           which=:outer, 
                           theta_range=(0.0, 2π), 
                           tol=1e-4)
```

---

## 在边界条件中使用

### 方法1：使用默认配置

```julia
# 创建 case
case = Case(param_dim, ...)

# 使用默认配置：
# - 内边界：第1圈内螺旋 θ ∈ [0, 2π]
# - 外边界：第N圈外螺旋 θ ∈ [2π(N-1), 2π*N]
# - 容差：tol = 1e-4

# ThermalDistributed2D_BC 会自动应用
```

### 方法2：自定义配置

```julia
# 自定义内边界范围（如第2圈）
case.opt.boundary_inner_theta = (2π, 4π)

# 自定义外边界范围（如第N-1圈）
N = Int(jellyroll_spiral_params(param_dim).n_wind)
case.opt.boundary_outer_theta = (2π*(N-2), 2π*(N-1))

# 自定义容差
case.opt.boundary_tol = 1e-3

# ThermalDistributed2D_BC 会使用这些配置
```

### 方法3：针对 collector_seed_mesh

```julia
# collector_seed_mesh 只覆盖第0圈，需要调整范围

# 内边界：第0圈内螺旋
case.opt.boundary_inner_theta = (0.0, 2π)

# 外边界：第0圈外螺旋（网格实际覆盖）
case.opt.boundary_outer_theta = (0.0, 2π)

# 容差
case.opt.boundary_tol = 1e-4
```

---

## 容差选择指南

### 容差的物理意义

`tol` 是节点到理论螺旋线的**空间距离**（单位：m）。

### 推荐值

| 容差 | 适用场景 | 说明 |
|------|---------|------|
| `1e-4` | 精确网格 | 节点精确生成在螺旋线上 |
| `1e-3` | 一般网格 | 允许1mm偏差 |
| `1e-2` | 粗糙网格 | 允许1cm偏差 |
| `0.01 * b * 2π` | 自适应 | 基于螺距的1%周长 |

### 选择建议

1. **对于 collector_seed_mesh**：
   - 节点精确生成在螺旋线上
   - 推荐 `tol = 1e-4`（0.1mm）

2. **对于 inscribed 裁剪网格**：
   - 节点可能不在螺旋线上
   - 推荐 `tol = 1e-3` ~ `1e-2`

3. **自适应容差**：
   ```julia
   p = jellyroll_spiral_params(param_dim)
   tol = 0.01 * p.b * 2π  # 螺距周长的1%
   ```

---

## collector_seed_mesh 的特殊情况

### 网格结构

collector_seed_mesh 生成的网格：
- 内圈节点（1-161）：θ ∈ [0, 2π]，r = a + b·θ
- 外圈节点（162-322）：θ ∈ [0, 2π]，r = a + b·θ + t_repeat

**关键**：内外圈在**同一 θ 段**（第0圈）！

### 无法实现的目标

❌ **无法同时识别**：
- 内边界 = 第1圈内螺旋 θ ∈ [0, 2π]
- 外边界 = 第N圈外螺旋 θ ∈ [2π(N-1), 2π*N]

**原因**：网格不覆盖第N圈。

### 正确的配置

✅ **对于 collector_seed_mesh，应使用第0圈**：

```julia
# 内边界：第0圈内螺旋
case.opt.boundary_inner_theta = (0.0, 2π)

# 外边界：第0圈外螺旋
case.opt.boundary_outer_theta = (0.0, 2π)

# 容差
case.opt.boundary_tol = 1e-4
```

**结果**：
- 内边界：161 个节点（内圈）
- 外边界：161 个节点（外圈）

---

## 测试验证

### 运行测试脚本

```bash
julia tools/test_precise_boundary.jl
```

### 预期输出

```
精确边界识别测试：基于螺旋线方程
========================================================================

测试1: 识别第1圈的内螺旋（θ ∈ [0, 2π]）
========================================================================
  识别到的内边界节点数: 161
  内边界节点半径范围: [0.01012, 0.01043] m
  θ_cum 范围: [0.0, 6.283]
  预期范围: [0.0, 6.283]
  节点到螺旋线的最大距离: 8.3e-18 m  # 数值精度级别

测试2: 识别第N圈的外螺旋（θ ∈ [2π(N-1), 2π*N]）
========================================================================
  识别到的外边界节点数: 0
  ⚠️  没有识别到外边界节点！
  原因：collector_seed_mesh 只覆盖第0圈，不覆盖第N圈

测试3: 识别第0圈的外螺旋（θ ∈ [0, 2π]）
========================================================================
  识别到的外螺旋节点数（第0圈）: 161
  外螺旋节点半径范围: [0.01030, 0.01062] m
  θ_cum 范围: [0.0, 6.283]
  节点到螺旋线的最大距离: 1.2e-17 m

匹配度:
  内圈节点中被识别为内螺旋的: 161 / 161
  外圈节点中被识别为外螺旋的: 161 / 161
  ✓ 完全匹配！精确识别正确。
```

---

## 与旧方法的对比

### 旧方法（:node_on）

```julia
# 基于 θ_cum 范围 + 模运算
is_outer = edge_boundary(:node_on, mesh, i, param_dim; which=:outer)
```

**问题**：
- 使用 `mod(θ_cum, 2π)` 无法区分不同圈层
- 无法指定具体的 θ 范围
- 对于多圈螺旋不够精确

### 新方法（:node_on_spiral）

```julia
# 基于螺旋线方程 + 精确距离验证
is_outer = edge_boundary(:node_on_spiral, mesh, i, param_dim; 
                         which=:outer, theta_range=(2π*(N-1), 2π*N), tol=1e-4)
```

**优势**：
- ✅ 可指定任意 θ 范围（如第5圈、第N圈）
- ✅ 精确验证节点到螺旋线的距离
- ✅ 适用于全螺旋网格
- ✅ 容差可调

---

## 完整使用流程

### 步骤1：创建网格

```julia
using JuBat

param_dim = ChooseCell("Jellyroll")
mesh_th = jellyroll_collector_seed_mesh(param_dim; nθ=160, gsorder=2)
```

### 步骤2：配置边界识别

```julia
case = Case(param_dim, ...)
case.mesh["thermal2D"] = mesh_th

# 配置边界（针对 collector_seed_mesh）
case.opt.boundary_inner_theta = (0.0, 2π)  # 第0圈内螺旋
case.opt.boundary_outer_theta = (0.0, 2π)  # 第0圈外螺旋
case.opt.boundary_tol = 1e-4
```

### 步骤3：边界条件自动应用

```julia
# ThermalDistributed2D_BC 会自动识别边界节点并施加对流换热
# 内边界：默认绝热
# 外边界：对流换热 h*(T - T_amb)
```

### 步骤4：验证边界识别

```julia
# 手动检查识别结果
is_outer = [edge_boundary(mesh_th, i, param_dim; 
                          which=:outer, theta_range=(0.0, 2π), tol=1e-4) 
            for i in 1:mesh_th.nlen]

println("外边界节点数: ", count(identity, is_outer))
```

---

## 常见问题

### Q1：为什么识别不到第N圈的外螺旋？

**A**：collector_seed_mesh 只覆盖第0圈（θ ∈ [0, 2π]），不覆盖第N圈。

**解决方案**：
- 使用第0圈范围：`theta_range=(0.0, 2π)`
- 或创建全螺旋网格（需要新的网格生成函数）

### Q2：容差应该设置多大？

**A**：
- collector_seed_mesh：`tol = 1e-4`（节点精确在螺旋线上）
- 其他网格：`tol = 1e-3` ~ `1e-2`（允许偏差）

### Q3：如何验证识别是否正确？

**A**：运行测试脚本：
```bash
julia tools/test_precise_boundary.jl
```

检查"节点到螺旋线的最大距离"是否 < tol。

### Q4：可以识别部分螺旋线段吗？

**A**：可以！指定任意 θ 范围：
```julia
# 识别第5圈的前半圈
is_boundary = edge_boundary(mesh, i, param_dim; 
                            which=:inner, 
                            theta_range=(2π*4, 2π*4 + π), 
                            tol=1e-4)
```

---

## 总结

### ✓ 优势

1. **精确**：基于螺旋线方程和距离验证
2. **灵活**：可指定任意 θ 范围
3. **通用**：适用于任何螺旋网格
4. **可调**：容差可配置

### ⚠️ 注意

1. **collector_seed_mesh 限制**：只覆盖第0圈
2. **容差选择**：需根据网格精度调整
3. **计算成本**：需要计算每个节点到螺旋线的距离

### 💡 建议

- 对于 collector_seed_mesh，使用第0圈范围
- 对于全螺旋网格，可使用任意圈层
- 容差设置为 `1e-4` ~ `1e-3`

---

**文档完成** ✅
