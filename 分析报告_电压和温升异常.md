# Jellyroll参数电压降和温升异常分析报告

## 问题描述

1. **Thermal.jl集总模型**：电压曲线为4.13-2.5V（正常）
2. **testexample.jl（多SPMe模式）**：电压降仅为0.001V，温度升高不明显（异常）

两者都使用相同的Jellyroll参数，但结果差异巨大。

---

## 参数对比分析

### 1. 几何参数（Jellyroll.jl）

| 参数 | 值 | 说明 |
|------|-----|------|
| 外半径 Rout | 0.0325 m | 圆柱电池外半径 |
| 内半径 Rin | 0.00192 m | 芯轴半径 |
| 高度 width | 0.0521 m | 电池高度 |
| 展开长度 | 938.77 m | 螺旋展开后的长度 |
| **电化学面积 area** | **48.93 m²** | length × width，用于电流归一化 |
| 体积 | 0.000175 m³ | π(R²out - R²in) × width |

### 2. 电极几何参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 正极厚度 | 75.6 μm | |
| 负极厚度 | 85.2 μm | |
| 隔膜厚度 | 12 μm | |
| **总厚度 L** | **172.8 μm** | 用于电化学归一化 |

### 3. 材料参数

| 材料 | 热导率 (W/m/K) | 密度 (kg/m³) | 比热容 (J/kg/K) |
|------|----------------|--------------|----------------|
| 正极 | 1.04 | 2895 | 1270 |
| 负极 | 1.58 | 1555 | 1437 |
| 隔膜 | 0.16 | 1100 | 1978 |

**计算结果：**
- 有效热导率（径向）λ_r ≈ 0.47 W/m/K
- 有效热导率（切向）λ_t ≈ 1.11 W/m/K
- 电池平均密度 ρ ≈ 4869 kg/m³
- 有效比热容 cp ≈ 1329 J/kg/K

### 4. 电化学参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 1C电流 I1C | 60 A | |
| 容量 | 60 Ah | |
| 正极比表面积 as_p | 3.68×10⁵ 1/m | |
| 负极比表面积 as_n | 3.67×10⁵ 1/m | |
| 正极电导率 σ_p | 0.18 S/m | ⚠️ **非常低！** |
| 负极电导率 σ_n | 100 S/m | |
| **dUdT** | **0** | ⚠️ **可逆热为0** |

---

## 尺度因子分析

### 5. 关键尺度因子（SetParams.jl）

#### 电化学尺度：
```
I_typ = I1C = 60 A
φ = R·T_ref/F = 8.314×298/96485 = 0.02568 V
area = 48.93 m²
```

#### 热尺度（Scheme B）：
```
L_th = Rout = 0.0325 m              （特征热长度）
k_ref = PE.lambda = 1.04 W/m/K      （参考热导率）
ρc_ref = ρ × cp = 6.47×10⁶ J/m³/K  （体积热容）
q_ref = k_ref × T_ref / L_th² = 2.92×10⁵ W/m³  （参考体积热源）
t_th = ρc_ref × L_th² / k_ref = 6572 s  （热扩散时间）
```

#### 尺度匹配检查：
```
典型电化学功率 = I_typ × φ = 60 × 0.02568 = 1.54 W
典型功率密度 = 1.54 / 0.000175 = 8800 W/m³
热源尺度 q_ref = 292000 W/m³

比值: q_chem / q_ref = 8800 / 292000 = 0.03
```

**这意味着：典型电化学热源仅为热尺度的3%！这是正常的归一化。**

---

## 问题诊断

### 问题1：电压降过小（~0.001V vs 正常~1.6V）

#### 根本原因：多SPMe架构的误用

**testexample.jl的架构：**
```julia
opt.per_element_spme = true  # 启用多SPMe模式
nθ = 80  # 80个周向单元
```

**多SPMe模式的设计逻辑：**
1. 将2D热网格的每个单元（80个）视为独立的电化学单元
2. 每个单元运行独立的SPMe模型
3. 通过分流求解器（solve_branch_currents_newton）分配电流
4. **所有单元共享相同的电压**（并联结构）

**问题所在：**
```
正常单电池： V = OCV - I × R_total
           电压降 = I × R_total ≈ 5A × 0.3Ω ≈ 1.5V ✓

多SPMe模式： 所有单元并联共享电压
           每个单元电流 ≈ 5A / 80 ≈ 0.0625A
           单元电压降 = 0.0625A × R_unit
           总电压 = 单元电压（不累加！）
           电压降 ≈ 0.001V ✗
```

#### 物理意义错误：
- **Jellyroll电池是单层螺旋卷绕结构**，不是80层并联
- 电流应该均匀分布（或根据局部阻抗略有差异），而不是分成80份并联
- 正确的模型应该是：
  - 单个SPMe模型（电化学）
  - 2D分布式热模型（温度场）
  - 热源从SPMe均匀分配到所有热单元

#### 验证：testexample_simple_coupling.jl

```julia
opt.simple_thermal_coupling = true  # 简化耦合模式
opt.per_element_spme = false       # 关闭多SPMe
```

**结果：电压曲线正常！** 这证实了多SPMe架构用于Jellyroll电池是不合适的。

---

### 问题2：温升不明显

#### 原因A：dUdT = 0

```julia
PE.dUdT = x-> 0 * x  // Jellyroll.jl 第36行
NE.dUdT = x-> 0 * x  // Jellyroll.jl 第64行
```

**影响：**
- 可逆热源 Q_rev = 0
- 仅剩反应热和欧姆热
- 可逆热通常占总热源的10-30%，影响中等

**对比：**
- LG M50参数：dUdT 也是 0（第32、59行）
- Northrop参数：dUdT 非零（有具体函数）

所以dUdT=0不是特殊问题，但会降低总热源。

#### 原因B：热源计算公式问题（CallModel_MultiSPMe）

查看`src/Solve.jl`第510-529行的热源计算：

```julia
# 反应热
Q_rxn_NE = as_n * abs(j_n_e) * abs(eta_n_e[e])
Q_rxn_PE = as_p * abs(j_p_e) * abs(eta_p_e[e])

# 欧姆热
Q_ohm_s_NE = I_e_local^2 / (3.0 * sig_n_eff)
Q_ohm_s_PE = I_e_local^2 / (3.0 * sig_p_eff)
Q_ohm_e_NE = I_e_local^2 / (3.0 * kappa_ne)
Q_SP = I_e_local^2 / kappa_sp
```

**单位分析：**

1. **反应热公式** `Q = as * j * η`
   - as: [1/m]（无量纲，已归一化）
   - j: [无量纲电流密度]
   - η: [无量纲电势]
   - 结果：[无量纲] ✓ 正确

2. **欧姆热公式** `Q = I² / (3σ)`
   - I: [无量纲电流]
   - σ: [无量纲电导率]
   - 结果：[无量纲] ✓ 公式形式正确

**但是：归一化尺度不匹配！**

电流归一化使用 `I_typ = 60 A`：
```julia
I_e = I_total  // I_total已经用I_typ归一化
I_e_local = I_e[e]  // 无量纲，量级~1.0
```

但热源应该使用 `q_ref` 归一化。转换公式：
```
Q_SI = I²/(σ·L) × (1/area) × I_typ² / (sig_ref)
     = I_nd² / sig_nd × I_typ² / (sig_ref × L × area)
```

这里需要仔细检查归一化是否一致。

#### 原因C：电流过小导致热源过小

如果每个单元分到的电流仅 0.0625A（5A/80），则：
```
热源 ∝ I²
单元热源 = (0.0625)² / (5)² × Q_total = 0.00016 × Q_total
```

**这会导致单元热源极小，温升不明显！**

---

## 核心问题总结

### 1. 架构选择错误

| 特征 | testexample.jl（多SPMe） | testexample_simple_coupling.jl（简化） |
|------|--------------------------|---------------------------------------|
| 电化学模型 | 80个独立SPMe | 1个SPMe |
| 热模型 | 2D分布式 | 2D分布式 |
| 电流分配 | 非线性分流求解 | 均匀分配 |
| 电压计算 | 共享电压（并联） | 串联电压 |
| 物理合理性 | ❌ 不适合Jellyroll | ✓ 合理 |
| 电压曲线 | ✗ 0.001V（错误） | ✓ 正常 |
| 温升 | ✗ 不明显 | ✓ 正常 |

**结论：多SPMe模式设计用于真正的多单元并联系统（如电池包），不适合单个Jellyroll电池。**

### 2. 参数次要问题

#### 问题A：正极电导率过低
```
PE.sig = 0.18 S/m  // Jellyroll.jl 第26行
```

这远低于典型值（~10-100 S/m），会导致：
- 欧姆热过大
- 极化严重
- 但不影响电压降的量级（因为架构问题更严重）

#### 问题B：dUdT = 0
```
PE.dUdT = x-> 0 * x
NE.dUdT = x-> 0 * x
```

建议参考Northrop参数添加熵变系数：
```julia
PE.dUdT = x-> -0.001 * (0.199521039 - 0.928373822*x + ...)
NE.dUdT = x-> 0.001 * (0.005269056 + 3.299265709*x - ...)
```

---

## 推荐解决方案

### 方案1：使用简化耦合模式（推荐）✅

修改 `testexample.jl`：
```julia
# 修改前
opt.per_element_spme = true

# 修改后
opt.simple_thermal_coupling = true
opt.per_element_spme = false
```

**优点：**
- 物理意义正确（单电化学单元 + 2D温度场）
- 电压曲线正常
- 温升正常
- 内存占用低（~3GB）
- 计算速度快（快5-10倍）

**缺点：**
- 假设电流均匀分布（但对于Jellyroll这是合理的）

### 方案2：修正多SPMe架构（不推荐，工作量大）

需要修改核心逻辑：
1. 将单元改为串联而非并联
2. 重新设计电压计算（累加而非共享）
3. 调整电流分配算法
4. 修正热源归一化

**不推荐理由：**
- 工作量巨大，需要重构核心代码
- 物理意义仍不清晰（Jellyroll本身是单层结构）
- 简化模式已经足够且更合理

### 方案3：修正参数（辅助措施）

```julia
# Jellyroll.jl
PE.sig = 10.0  # 提高到合理值（原值0.18太低）
PE.dUdT = x-> -0.001 * (...)  # 添加熵变系数
NE.dUdT = x-> 0.001 * (...)
```

---

## 验证步骤

1. **验证简化耦合模式**：
   ```bash
   julia example/testexample_simple_coupling.jl
   ```
   - 检查电压曲线：应该从4.13V降至2.5V ✓
   - 检查温升：应该有明显升温（~5-10K） ✓

2. **对比多SPMe模式**：
   ```bash
   julia example/testexample.jl
   ```
   - 电压降：0.001V ✗
   - 温升：不明显 ✗

3. **检查输出变量**：
   ```julia
   # testexample.jl 第176-195行
   I_e_hist = result["thermal2D element current"]
   I_e_final = I_e_hist[:, end] .* case.param.scale.I_typ
   
   println("单元电流范围: ", extrema(I_e_final), " A")
   println("平均单元电流: ", mean(I_e_final), " A")
   println("总电流: ", sum(I_e_final .* areas ./ sum(areas)), " A")
   ```

---

## 附录：关键代码位置

| 功能 | 文件 | 行数 | 说明 |
|------|------|------|------|
| Jellyroll参数定义 | `src/parameters/Jellyroll.jl` | 全文 | 所有参数 |
| 参数归一化 | `src/SetParams.jl` | 196-272 | ChooseCell函数 |
| 热尺度计算 | `src/SetParams.jl` | 257-270 | Scheme B |
| 多SPMe初始化 | `src/Initialisation.jl` | 127-254 | ModelInitialisation_MultiSPMe |
| 多SPMe求解 | `src/Solve.jl` | 344-718 | CallModel_MultiSPMe |
| 热源计算 | `src/Solve.jl` | 461-543 | 逐单元热源 |
| 分流求解 | `src/SPMe.jl` | 630-713 | solve_branch_currents_newton |
| 集总热模型 | `src/Thermal.jl` | 14-68 | ThermalLumped函数 |

---

## 结论

**电压降过小和温升不明显的根本原因是：多SPMe架构将Jellyroll电池错误地建模为80个并联单元，导致：**

1. **电压计算错误**：所有单元共享电压（并联），而不是累加（串联）
2. **电流分配不当**：每个单元分到极小电流（~0.06A），导致热源过小
3. **物理意义错误**：Jellyroll是单层螺旋结构，不是多层并联

**推荐解决方案：使用简化耦合模式（`simple_thermal_coupling = true`），这是物理上正确且高效的选择。**

**参数次要问题：**
- 正极电导率过低（0.18 S/m）
- 缺少熵变系数（dUdT = 0）

但这些不是主要问题，架构选择才是关键。
