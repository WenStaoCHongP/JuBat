# 数据组织原则与修复方案

**日期**: 2025-11-17  
**问题**: CallModel_MultiSPMe 违反数据组织原则  
**严重程度**: 🔴 **架构错误**

---

## ⚠️ **核心问题**

在`CallModel_MultiSPMe`和相关函数中，**21处违规**尝试在`Case`结构体中动态添加字段，但Julia的结构体字段是固定的！

### 违规代码示例

```julia
// ❌ 错误：尝试添加字段到Case
case.multi_spme_layout = Dict(...)           // 违规1
case.thermal2D_element_area_cache = A        // 违规2
case.I_e_cache = copy(I_e)                   // 违规3

// ❌ 错误：尝试从Case读取不存在的字段
layout = case.multi_spme_layout              // 会报错！
areas = case.thermal2D_element_area_cache    // 会报错！
```

---

## 📚 **数据组织原则**

### Case结构体（固定5个字段）

```julia
mutable struct Case
    param_dim::Params                                  // 有量纲参数
    param::Params                                      // 无量纲参数
    opt::Option                                        // 求解器选项
    mesh::Dict{String, Mesh}                          // 网格字典
    index::Dict{String, Union{Array{Int64}, Int64}}   // 索引字典
end
```

**用途**: 存储**配置信息**，在整个求解过程中**不变**

**允许访问**:
- ✅ `case.param_dim.*` - 读取物理参数
- ✅ `case.param.*` - 读取无量纲参数
- ✅ `case.opt.*` - 读取/修改求解选项
- ✅ `case.mesh[key]` - 读取网格对象
- ✅ `case.index[key]` - 读取索引

**禁止操作**:
- ❌ `case.new_field = value` - 不能添加新字段
- ❌ 存储计算结果到case
- ❌ 存储缓存数据到case

### variables字典（动态计算结果）

```julia
variables = Dict{String, Union{Array{Float64}, Float64}}()
```

**用途**: 存储**计算结果**和**临时数据**，每个时间步重新计算

**存储内容**:
- ✅ 物理量（电压、过电位、浓度等）
- ✅ 几何数据（面积、体积等）
- ✅ 中间结果（热源、电流分布等）
- ✅ 元数据（布局信息、索引等）

**生命周期**: 
- 在`CallModel`中创建
- 在当前时间步使用
- 在`Variable_update!`中记录到历史
- 下一时间步重新计算

---

## 🔍 **违规清单（21处）**

### 文件: src/Initialisation.jl（8处）

```julia
// 第213行
case.multi_spme_layout = Dict{String, Any}()  // ❌

// 第215-220行
case.multi_spme_layout["ne"] = ne             // ❌
case.multi_spme_layout["n_chem"] = n_chem     // ❌
case.multi_spme_layout["nT"] = nT             // ❌
case.multi_spme_layout["n_total"] = ...       // ❌
case.multi_spme_layout["chem_range"] = ...    // ❌
case.multi_spme_layout["thermal_range"] = ... // ❌

// 第258, 261行（以及其他helper函数）
layout = case.multi_spme_layout               // ❌
```

### 文件: src/Solve.jl（13处）

```julia
// 第21行（Solve函数中）
layout = case.multi_spme_layout               // ❌

// 第256, 260行（CallModel_MultiSPMe中）
if !haskey(case, :multi_spme_layout) ...      // ❌
layout = case.multi_spme_layout               // ❌

// 第279-288行
if haskey(case, :thermal2D_element_area_cache)  // ❌
    case.thermal2D_element_area_cache           // ❌
else
    case.thermal2D_element_area_cache = A       // ❌
end

// 第308行
I_e_prev = haskey(case, :I_e_cache) ? case.I_e_cache : nothing  // ❌

// 第319行
case.I_e_cache = copy(I_e)                    // ❌
```

---

## 🔧 **修复方案**

### 策略A: 使用Option字段存储配置（推荐）

**原理**: `case.opt`是`Option`类型，可以添加新字段

**步骤1**: 修改`Option`结构体添加多SPMe标志

```julia
// src/SetCase.jl
mutable struct Option
    # ... 现有字段 ...
    
    # 多SPMe模式标志
    per_element_spme::Bool
    
    # 添加构造函数默认值
    function Option()
        opt = new()
        # ... 其他默认值 ...
        opt.per_element_spme = false
        return opt
    end
end
```

**步骤2**: 在初始化时设置标志而非布局字典

```julia
// src/Initialisation.jl - ModelInitialisation_MultiSPMe
function ModelInitialisation_MultiSPMe(case::Case; ...)
    # 设置标志
    case.opt.per_element_spme = true
    
    # 布局信息作为返回值的一部分
    layout = Dict{String, Any}(
        "ne" => ne,
        "n_chem" => n_chem,
        "nT" => nT,
        # ...
    )
    
    return y0, layout  # 返回元组！
end
```

**步骤3**: 在Solve中接收布局信息

```julia
// src/Solve.jl
function Solve(case::Case)
    if case.opt.per_element_spme && case.opt.thermal_enabled
        y0, layout = ModelInitialisation_MultiSPMe(case)
        # 将layout传递给CallModel
    else
        y0 = ModelInitialisation(case)
        layout = nothing
    end
    
    # 主循环
    while ...
        variables = CallModel_MultiSPMe(case, yt, t, layout, jacobi=...)
    end
end
```

**步骤4**: CallModel接收layout作为参数

```julia
// src/Solve.jl
function CallModel_MultiSPMe(case::Case, yt::Array{Float64}, t::Float64, 
                             layout::Dict{String,Any}; jacobi::String)
    # 使用传入的layout，不从case读取
    ne = layout["ne"]
    n_chem = layout["n_chem"]
    # ...
end
```

### 策略B: 使用variables传递所有数据（最符合原则）

**修改1**: helper函数接收layout参数

```julia
// src/Initialisation.jl
function MultiSPMe_extract_element_state(y::Array{Float64}, e::Int, 
                                         n_chem::Int, ne::Int)
    # 不再需要case和layout
    idx_start = (e - 1) * n_chem + 1
    idx_end = e * n_chem
    return vec(y[idx_start:idx_end])
end
```

**修改2**: 主循环传递布局信息

```julia
// src/Solve.jl
if case.opt.per_element_spme
    y0, layout_info = ModelInitialisation_MultiSPMe(case)
    ne = layout_info["ne"]
    n_chem = layout_info["n_chem"]
    nT = layout_info["nT"]
else
    y0 = ModelInitialisation(case)
    ne = n_chem = nT = 0
end

# 主循环
while ...
    if case.opt.per_element_spme
        variables = CallModel_MultiSPMe(case, yt, t, ne, n_chem, nT; jacobi=...)
    else
        M, K, F, variables, yphi = CallModel(case, yt, t, jacobi=...)
    end
end
```

### 策略C: 使用variables["_metadata"]传递（中间方案）

```julia
// 初始化时
y0 = ModelInitialisation_MultiSPMe(case)
initial_variables = StandardVariables(case, 1)
initial_variables["_layout_ne"] = Float64(ne)
initial_variables["_layout_n_chem"] = Float64(n_chem)
initial_variables["_layout_nT"] = Float64(nT)

// CallModel中
function CallModel_MultiSPMe(case::Case, yt::Array{Float64}, t::Float64, 
                             prev_variables::Dict; jacobi::String)
    # 从上一步的variables读取布局
    ne = Int(prev_variables["_layout_ne"])
    n_chem = Int(prev_variables["_layout_n_chem"])
    nT = Int(prev_variables["_layout_nT"])
    # ...
end
```

---

## 🎯 **推荐方案：方案A + 部分B**

### 原因

1. **配置信息放Option**: `per_element_spme`是配置，应该在`case.opt`中
2. **布局信息显式传递**: `layout`作为参数传递，不存储
3. **计算结果放variables**: 面积、电流等放variables

### 实施步骤

#### 1. 修改Option结构体

```julia
// src/SetCase.jl
mutable struct Option
    # ... 现有字段 ...
    per_element_spme::Bool
    
    Option() = new(
        # ... 现有默认值 ...
        false  # per_element_spme
    )
end
```

#### 2. 修改ModelInitialisation_MultiSPMe

```julia
function ModelInitialisation_MultiSPMe(case::Case; ...)
    # 设置标志
    case.opt.per_element_spme = true
    
    # 计算布局
    ne = case.mesh["thermal2D"].nele
    n_chem = ...
    nT = ...
    
    # 初始化状态向量
    y0 = ...
    
    # 返回布局信息
    layout = Dict{String,Int64}(
        "ne" => ne,
        "n_chem" => n_chem,
        "nT" => nT
    )
    
    return y0, layout  # 返回元组
end
```

#### 3. 修改helper函数（去除case依赖）

```julia
// 只需要尺寸信息，不需要完整case对象
function MultiSPMe_extract_element_state(y::Array{Float64}, e::Int, n_chem::Int)
    idx_start = (e - 1) * n_chem + 1
    idx_end = e * n_chem
    return vec(y[idx_start:idx_end])
end

function MultiSPMe_get_thermal_dofs(y::Array{Float64}, ne::Int, n_chem::Int)
    nT = length(y) - ne * n_chem
    return vec(y[(ne * n_chem + 1):end])
end

function MultiSPMe_update_element_state!(y::Array{Float64}, e::Int, 
                                         yt_e_new::Array{Float64}, n_chem::Int)
    idx_start = (e - 1) * n_chem + 1
    idx_end = e * n_chem
    copyto!(view(y, idx_start:idx_end), vec(yt_e_new))
end

function MultiSPMe_update_thermal_dofs!(y::Array{Float64}, T_new::Array{Float64}, 
                                       ne::Int, n_chem::Int)
    idx_start = ne * n_chem + 1
    copyto!(view(y, idx_start:length(y)), vec(T_new))
end
```

#### 4. 修改CallModel_MultiSPMe签名

```julia
function CallModel_MultiSPMe(case::Case, yt::Array{Float64}, t::Float64,
                             ne::Int, n_chem::Int, nT::Int; jacobi::String)
    # 不再从case读取layout
    # ne, n_chem, nT作为参数传入
    
    mesh_th = case.mesh["thermal2D"]
    
    # 提取状态向量
    T_nodes = MultiSPMe_get_thermal_dofs(yt, ne, n_chem)
    
    yt_chem = Vector{Vector{Float64}}(undef, ne)
    for e in 1:ne
        yt_chem[e] = MultiSPMe_extract_element_state(yt, e, n_chem)
    end
    
    # 计算面积（每次重新计算，不缓存）
    areas = zeros(Float64, ne)
    ngs = length(mesh_th.gs.detJ)
    @inbounds for g in 1:ngs
        e = mesh_th.gs.ele[g]
        areas[e] += mesh_th.gs.weight[g] * mesh_th.gs.detJ[g]
    end
    
    # 存储到variables（而非case）
    variables = StandardVariables(case, 1)
    variables["thermal2D element area"] = areas
    
    # 从上一步variables获取I_e_prev（而非case缓存）
    # 这需要从Solve传入prev_variables
    # 暂时使用nothing作为初值
    I_e_prev = nothing
    
    # ... 其余逻辑 ...
end
```

#### 5. 修改Solve主循环

```julia
function Solve(case::Case)
    # 初始化
    if case.opt.per_element_spme && case.opt.thermal_enabled
        y0, layout = ModelInitialisation_MultiSPMe(case)
        ne = layout["ne"]
        n_chem = layout["n_chem"]
        nT = layout["nT"]
    else
        y0 = ModelInitialisation(case)
        ne = n_chem = nT = 0
        layout = nothing
    end
    
    # 主循环
    yt = copy(y0)
    prev_variables = nothing
    
    while t < t_end
        if case.opt.per_element_spme && layout !== nothing
            variables = CallModel_MultiSPMe(
                case, yt, t, ne, n_chem, nT; 
                jacobi=jacobi, prev_variables=prev_variables
            )
            # 注意：需要修改CallModel_MultiSPMe返回格式
            M, K, F = variables["_M"], variables["_K"], variables["_F"]
        else
            M, K, F, variables, yphi = CallModel(case, yt, t, jacobi=jacobi)
        end
        
        # 时间推进...
        
        prev_variables = variables  # 传递给下一步
    end
end
```

---

## ✅ **修改检查清单**

### src/SetCase.jl
- [ ] 添加 `Option.per_element_spme::Bool` 字段
- [ ] 设置默认值 `false`

### src/Initialisation.jl
- [ ] 修改 `ModelInitialisation_MultiSPMe` 返回 `(y0, layout)`
- [ ] 删除所有 `case.multi_spme_layout` 赋值
- [ ] 修改 `MultiSPMe_extract_element_state` 接收 `n_chem`参数
- [ ] 修改 `MultiSPMe_get_thermal_dofs` 接收 `ne, n_chem` 参数
- [ ] 修改 `MultiSPMe_update_element_state!` 接收 `n_chem` 参数
- [ ] 修改 `MultiSPMe_update_thermal_dofs!` 接收 `ne, n_chem` 参数

### src/Solve.jl
- [ ] 修改 `Solve` 接收 `ModelInitialisation_MultiSPMe` 返回的元组
- [ ] 解包 `layout` 为 `ne, n_chem, nT`
- [ ] 修改 `CallModel_MultiSPMe` 签名添加 `ne, n_chem, nT` 参数
- [ ] 删除 `case.multi_spme_layout` 读取
- [ ] 删除 `case.thermal2D_element_area_cache` 缓存
- [ ] 删除 `case.I_e_cache` 缓存
- [ ] 面积每次重新计算（或从prev_variables获取）
- [ ] I_e从prev_variables["I_e"]获取（而非case缓存）

### src/JuBat.jl
- [ ] 检查导出的函数签名是否匹配

### 测试脚本
- [ ] 更新测试脚本适配新的API

---

## 📚 **设计原则总结**

### DO ✅

1. **配置放Option**
   ```julia
   case.opt.per_element_spme = true
   ```

2. **计算结果放variables**
   ```julia
   variables["thermal2D element area"] = areas
   variables["I_e"] = I_e
   ```

3. **临时数据显式传递**
   ```julia
   function CallModel_MultiSPMe(case, yt, t, ne, n_chem, nT; ...)
   ```

4. **历史数据从prev_variables获取**
   ```julia
   I_e_prev = haskey(prev_variables, "I_e") ? prev_variables["I_e"] : nothing
   ```

### DON'T ❌

1. **不要添加Case字段**
   ```julia
   case.new_field = value  # ❌ 会报错！
   ```

2. **不要在Case中缓存**
   ```julia
   case.cache_data = expensive_calculation()  # ❌
   ```

3. **不要用haskey检查Case**
   ```julia
   if haskey(case, :some_field)  # ❌ Case不是字典
   ```

4. **不要从Case读取计算结果**
   ```julia
   areas = case.element_areas  # ❌ Case只有配置
   ```

---

## 🎓 **为什么这样设计？**

### 1. **Julia类型系统**
- 结构体字段在定义时固定
- 不能运行时添加字段（除非用字典）

### 2. **数据职责分离**
- **Case**: "我是谁"（配置）
- **variables**: "我在哪"（状态）

### 3. **可维护性**
- 代码看到`case.xxx`就知道是配置
- 代码看到`variables["xxx"]`就知道是计算结果

### 4. **可测试性**
- Case可以预先构造，多次复用
- variables每次独立计算，无副作用

---

**创建日期**: 2025-11-17  
**优先级**: 🔴 **最高**  
**状态**: ⏳ 待实施修复
