# 极耳热边界条件快速参考

## 📋 一句话总结

**极耳热边界条件通过惩罚法强制极耳节点温度为 `T_tab = T_amb + rate×t`，在启用2D热模型且设置极耳角度后自动应用。**

---

## 🚀 快速启用（3步）

### 步骤1：设置极耳位置
```julia
# 在参数文件（如 Jellyroll.jl）或主程序中
param_dim.tab.width = 40e-3              # 极耳宽度 [m]
param_dim.tab.theta_pos = [0.0, π]       # 正极耳角度 [rad]
param_dim.tab.theta_neg = [π/2, 3π/2]    # 负极耳角度 [rad]
```

### 步骤2：启用2D热模型
```julia
opt.thermal_enabled = true
opt.thermalmodel = "distributed2D"
opt.thermal_dim = "2D"
```

### 步骤3：运行仿真
```julia
# 创建案例和网格
case = JuBat.SetCase(param_dim, opt)
mesh_th, _ = JuBat.jellyroll_collector_seeded_mesh(param_dim; n_radial=20, n_angular=60)
case.mesh["thermal2D"] = mesh_th

# 时间循环（极耳BC自动应用）
JuBat.Solvethermal!(case, variables, t, dt)
```

**完成！** 极耳边界条件已自动应用。

---

## ⚙️ 可选配置

### 自定义极耳温度演化
```julia
# 升温速率（默认0.1 K/s）
opt.tab_heating_rate = 0.5  # K/s

# 0.0 → 散热边界（极耳保持环境温度）
# >0  → 发热边界（极耳升温，模拟焊接发热）
# <0  → 强制冷却（液冷系统）
```

### 调整惩罚系数（通常不需要）
```julia
opt.tab_penalty = 1e12  # 默认值，范围 1e10-1e13
```

### 启用调试信息
```julia
opt.debug_coupling = true
# 输出：[thermal BC] tab nodes pos=12 neg=15
```

---

## 📍 极耳角度定义

### 坐标系
- 原点：Jellyroll中心
- 0°方向：x轴正方向（右）
- 正方向：逆时针
- 范围：(-π, π] 或 [0, 2π)

### 示例
```
            π/2 (90°)
              │
      π (180°)┼───── 0° (x轴)
              │
           3π/2 (270°)
```

### 常见配置
```julia
# 单个极耳
tab.theta_pos = [0.0]           # 正极耳在右侧
tab.theta_neg = [π]             # 负极耳在左侧

# 对称双极耳
tab.theta_pos = [0.0, π]        # 正极耳在左右
tab.theta_neg = [π/2, 3π/2]     # 负极耳在上下

# 多极耳
tab.theta_pos = [0, π/3, 2π/3]  # 3个正极耳
tab.theta_neg = [π, 4π/3, 5π/3] # 3个负极耳
```

---

## 🔍 验证极耳节点

### 检查识别的节点数
```julia
pos_idx, neg_idx = JuBat.jellyroll_tab_node_indices(mesh_th, param_dim)
println("正极耳节点: $(length(pos_idx))")
println("负极耳节点: $(length(neg_idx))")
```

### 可视化（需要Plots）
```julia
using Plots
x, y = mesh_th.node[:, 1], mesh_th.node[:, 2]

scatter(x, y, ms=2, color=:gray, alpha=0.3, label="网格")
scatter!(x[pos_idx], y[pos_idx], ms=4, color=:red, label="正极耳")
scatter!(x[neg_idx], y[neg_idx], ms=4, color=:blue, label="负极耳")
savefig("tab_nodes.png")
```

---

## 📐 实现原理（简化版）

### 物理模型
```
T_tab(t) = T_amb + rate_Ks × t
```

### 数值实现（惩罚法）
```julia
# 对每个极耳节点
KT[n, n] += 1e12           # 添加大刚度
FT[n] += 1e12 × T_tab      # 添加载荷

# 效果：强制 T[n] ≈ T_tab
```

### 自动调用流程
```
Solvethermal!
  └─> ThermalDistributed2D_BC(KT, FT, case, t)
        ├─> _apply_convection_bc!(...)      # 外边界对流
        └─> _apply_tab_bc!(...)              # 极耳温度 ⭐
```

---

## 📂 代码位置

| 功能 | 文件 | 行数 |
|------|------|------|
| 边界条件入口 | `src/ThermalDistributed.jl` | 336-351 |
| 极耳BC应用 | `src/ThermalDistributed.jl` | 412-441 |
| 节点识别 | `src/Jellyrollmodel.jl` | 363-395 |
| Tab参数定义 | `src/SetParams.jl` | 133-139 |
| Option选项 | `src/Option.jl` | 3-41 |

---

## ⚠️ 常见问题

### Q1: 极耳不生效？
**检查**：
- [ ] `theta_pos`和`theta_neg`是否非空？
- [ ] 热模型是否为`distributed2D`？
- [ ] 网格角度分辨率足够？（推荐`n_angular ≥ 40`）

### Q2: 识别的节点数为0？
**原因**：极耳角度超出网格范围或`tab.width`太小

**解决**：
- 检查网格覆盖的角度范围
- 增加`tab.width`
- 调整极耳角度

### Q3: 温度约束不准确？
**原因**：惩罚系数太小

**解决**：
```julia
opt.tab_penalty = 1e13  # 增大到1e13
```

### Q4: 如何禁用极耳BC？
**方法1**（推荐）：
```julia
tab.theta_pos = []  # 清空角度数组
tab.theta_neg = []
```

**方法2**：在代码中注释掉
```julia
# _apply_tab_bc!(KT, FT, mesh, case, t)  # 注释此行
```

---

## 🎯 典型应用场景

### 场景1：正常工作（散热）
```julia
opt.tab_heating_rate = 0.0
# T_tab = T_amb = 298 K（常量）
```

### 场景2：大电流焊接发热
```julia
opt.tab_heating_rate = 1.0  # 1 K/s
# 模拟接触电阻发热
```

### 场景3：液冷系统
```julia
opt.tab_heating_rate = -0.5  # -0.5 K/s
# 模拟主动冷却
```

### 场景4：热失控测试
```julia
# 指数升温（需修改代码）
T_tab = T_amb + 100 * (1 - exp(-t/60))
```

---

## 📊 参数推荐值

| 参数 | 推荐值 | 范围 | 说明 |
|------|--------|------|------|
| `tab.width` | 20-50 mm | 10-100 mm | 极耳物理宽度 |
| `tab_heating_rate` | 0.0 K/s | -5 ~ +5 K/s | 升温速率 |
| `tab_penalty` | 1×10¹² | 1e10 ~ 1e13 | 惩罚系数 |
| `n_angular` | 60 | ≥40 | 角度网格数 |

---

## 🔗 相关文档

- **详细说明**：`docs/极耳热边界条件说明.md`
- **理论背景**：`docs/Thermal2D_Distributed_README.md`
- **网格生成**：`src/Jellyrollmodel.jl` 注释
- **使用示例**：`example/testexample.jl`

---

## 📝 示例代码（完整）

```julia
using JuBat

# 1. 参数
param_dim = JuBat.ChooseCell("Jellyroll")
param_dim.tab.width = 40e-3
param_dim.tab.theta_pos = [0.0, π]
param_dim.tab.theta_neg = [π/2, 3π/2]

# 2. 选项
opt = JuBat.Option()
opt.thermal_enabled = true
opt.thermalmodel = "distributed2D"
opt.tab_heating_rate = 0.1  # 0.1 K/s升温
opt.debug_coupling = true   # 调试信息

# 3. 案例
case = JuBat.SetCase(param_dim, opt)

# 4. 网格
mesh_th, _ = JuBat.jellyroll_collector_seeded_mesh(
    param_dim; 
    n_radial=20, 
    n_angular=60
)
case.mesh["thermal2D"] = mesh_th

# 5. 验证
pos_idx, neg_idx = JuBat.jellyroll_tab_node_indices(mesh_th, param_dim)
println("✓ 正极耳节点: $(length(pos_idx))")
println("✓ 负极耳节点: $(length(neg_idx))")

# 6. 运行（在时间循环中）
# JuBat.Solvethermal!(case, variables, t, dt)
# 极耳BC自动应用 ✅
```

---

**版本**: v1.0  
**日期**: 2025-12-29  
**快速查询**: Ctrl+F 搜索关键词
