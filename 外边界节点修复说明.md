# 外边界节点分类修复说明

## 问题诊断

### 原始问题
运行 `debug_outer_nodes.jl` 时发现：
- **预期**：外圈节点应该有 161 个（nθ+1，首尾重合）
- **实际**：
  - 默认容差分类出 200 个（过多，误判了内圈节点）
  - 严格容差分类出 98 个（过少）
  - eps_theta_default = 3.26 弧度（远超合理范围）

### 根本原因

**网格生成逻辑**（`jellyroll_collector_seed_mesh`）：
```julia
# 内圈和外圈使用相同的 θ 范围（如 [0, 2π]）
x_in, y_in = spiral_xy(s_in=0)        # 内螺旋：r = a + b*θ
x_out, y_out = spiral_xy(s_out=t_repeat)  # 外螺旋：r = a + b*θ + t_repeat
```
- 内圈节点（1-161）：θ ∈ [0, 2π]，半径 r_in = a + b*θ
- 外圈节点（162-322）：θ ∈ [0, 2π]，半径 r_out = a + b*θ + t_repeat
- **关键**：两组节点在同一圈层（θ 范围相同），只是半径相差 t_repeat

**原始 `edge_boundary` 判断逻辑**：
```julia
elseif which === :outer
    N = max(1, Int(p.n_wind))  # N = 158
    θ_start, θ_end = 2.0*pi*(N-1), 2.0*pi*N  # [986, 993]
    return (θ_cum_out >= θ_start - eps_theta) && (θ_cum_out <= θ_end + eps_theta)
```
- 期望外圈节点在**第 N 圈**（θ_cum_out ∈ [986, 993]）
- 但实际外圈节点的 θ_cum_out ∈ [0.039, 6.28]（第 0 圈）
- **结果**：所有外圈节点都被判定为 false！

### debug_outer_nodes.jl 的输出分析

```
外圈节点 (应该在 [2π*(N-1), 2π*N] 范围):
  Sample theta_cum_out outer[1:5] = [0.039, 0.078, 0.118, 0.157, 0.196]
  预期范围: [986.46, 992.74]  # 这是错误的预期！

与预期的比较:
  ✓ 内圈 theta_cum 在预期范围内
  ❌ 外圈 theta_cum 超出预期范围！
```

**分析**：
- 实际范围 [0.039, 0.196] 是**正确的**（外圈节点确实在第 0 圈）
- "预期范围 [986, 993]" 是**错误的预期**（基于错误的判断逻辑）

---

## 修复方案

### 修复 1：自适应容差（已应用）

**问题**：对于小螺距（b = 3.07e-5），默认容差 1e-4 导致：
```julia
eps_theta = tol / b = 1e-4 / 3.07e-5 = 3.26 弧度（>半圈！）
```

**修复**：
```julia
# src/Jellyrollmodel.jl 第306-307行
bval = p.b == 0.0 ? 1e-12 : p.b
tol_default = min(1e-4, 0.05 * abs(bval))  # 确保 eps_theta < 0.05 弧度
```

**效果**：
- 新的 tol_default = min(1e-4, 0.05 × 3.07e-5) = 1.54e-6
- 新的 eps_theta = 1.54e-6 / 3.07e-5 = 0.05 弧度（~3°，合理）

### 修复 2：外圈判断逻辑（本次修复）

**核心思想**：
对于 `collector_seed_mesh` 网格，外圈节点和内圈节点在**同一圈层**，判断时应该检查"是否在任意一圈的外螺旋上"，而不是"是否在第 N 圈的外螺旋上"。

**实现方法**：
将累计角度 θ_cum_out 模 2π，检查是否在 [0, 2π] 范围：

```julia
# src/Jellyrollmodel.jl 第320-326行
elseif which === :outer
    # 对于 collector_seed_mesh 网格：内外圈在同一θ段，只是半径偏移
    # 所以外圈判断应该检查"在任意一圈的外螺旋上"，而不是"第N圈"
    # 方法：将 θ_cum_out 模 2π，检查是否在 [0, 2π] 范围
    θ_mod = mod(θ_cum_out, 2.0*pi)
    θ_start, θ_end = 0.0, 2.0*pi
    return (θ_mod >= θ_start - eps_theta) && (θ_mod <= θ_end + eps_theta)
```

**同步修改** `:theta_range` 返回值：
```julia
# src/Jellyrollmodel.jl 第292-295行
elseif which === :outer
    # 对于 collector_seed_mesh 网格：外圈与内圈在同一θ段
    # 返回 [0, 2π] 表示"任意一圈的外螺旋"
    return 0.0, 2.0*pi
```

---

## 修复后的预期结果

运行 `debug_outer_nodes.jl` 应该得到：

```
网格覆盖的θ范围分析:
  节点半径范围: [0.01012, 0.01043] m
  内半径 Rin: 0.010 m
  外半径 Rout: 0.020 m
  内圈节点θ范围: [0.0, 6.283]  # [0, 2π]
  外圈节点θ范围（基于内螺旋公式）: [62.82, 69.10]  # 比内圈多 t_repeat/b

b = 3.068e-5
t_repeat = 0.0001928
n_wind (N) = 158

Default tol classification:
  count = 161  # ✓ 正确！
  inner misclassified = 0  # ✓ 没有误判
  eps_theta_default = 0.05 弧度  # ✓ 合理范围

内圈节点 (应该在 [0, 2π] 范围):
  Sample theta_cum_in inner[1:5] = [0.0, 0.039, 0.078, 0.118, 0.157]
  预期范围: [0, 6.283]
  ✓ 内圈 theta_cum 在预期范围内

外圈节点 (应该在 [0, 2π] 范围，模 2π):
  Sample theta_cum_out outer[1:5] = [0.039, 0.078, 0.118, 0.157, 0.196]
  实际 θ_mod = mod(theta_cum_out, 2π) = [0.039, 0.078, 0.118, 0.157, 0.196]
  预期范围: [0, 6.283]
  ✓ 外圈 theta_cum (模2π) 在预期范围内
```

---

## 技术要点

### 1. Collector-Seeded Mesh 的特点
- **优势**：每个条带单元天然跨越完整层序（NE/SP/PE/PCC/NCC），无需复杂的层权重计算
- **结构**：
  - 内螺旋：r_in(θ) = a + b*θ，作为"导轨"
  - 外螺旋：r_out(θ) = a + b*θ + t_repeat，偏移一个完整层序厚度
  - 条带单元：连接对应的内外节点 [(in_i, out_i, out_{i+1}, in_{i+1})]
- **关键**：内外螺旋共享相同的 θ 参数化

### 2. 累计角度 vs. 模周期角度
- **θ_cum**：累计角度，随着螺旋绕圈不断增大（0 → 2π → 4π → ... → 2π*N）
- **θ_mod = mod(θ_cum, 2π)**：周期性角度，始终在 [0, 2π] 范围
- 对于 collector_seed_mesh：
  - 内圈：θ_cum_in = (r - a) / b
  - 外圈：θ_cum_out = (r - a - t_repeat) / b
  - 关系：θ_cum_out = θ_cum_in - t_repeat/b（外圈比内圈多偏移的角度）

### 3. 边界分类的通用性
修复后的 `edge_boundary` 函数：
- **内圈**：始终检查 θ_cum_in ∈ [0, 2π]（第一圈）
- **外圈**：检查 mod(θ_cum_out, 2π) ∈ [0, 2π]（任意一圈的外螺旋）
- 适用于 collector_seed_mesh 网格（覆盖单圈或多圈均可）

---

## 验证清单

- [x] **修复 1**：自适应容差，避免 eps_theta 过大
- [x] **修复 2**：外圈判断使用模 2π 逻辑
- [x] **同步更新**：`:theta_range` 返回值保持一致
- [ ] **运行验证**：`julia tools/debug_outer_nodes.jl`（需要 Julia 环境）
- [ ] **预期结果**：分类出 161 个外圈节点，0 个误判

---

## 总结

**问题本质**：`edge_boundary` 的原始逻辑假设外圈节点在"第 N 圈"（最外层），但 `collector_seed_mesh` 生成的网格中，内外圈在**同一圈层**（θ 范围相同），只是半径不同。

**修复核心**：将外圈判断从"检查是否在第 N 圈"改为"检查是否在任意一圈的外螺旋上"（使用模 2π）。

**影响范围**：
- 边界条件施加（对流换热、极耳加热等）将正确识别外圈节点
- 热网格分析工具（如 `debug_outer_nodes.jl`）输出正确
- 对 collector_seed_mesh 以外的网格（如 uniform_mesh）无影响

**状态**：✅ 修复已应用，等待用户在 Julia 环境中验证
