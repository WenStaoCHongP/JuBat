# Jellyroll 参数归一化深度分析

## 1. 归一化框架

### 1.1 关键归一化公式（SetParams.jl 第 237-240 行）

```julia
# 电流密度尺度
scale.j = I_typ / (a0 × L × A)

# 容量时间尺度
scale.ts_p = F × cs_max_p × A × L / I_typ
scale.ts_n = F × cs_max_n × A × L / I_typ
```

其中：
- `I_typ = I1C` (标称电流，单位 A)
- `a0 = 1/r0 = 1/(1e-6) = 1e6 m⁻¹` (比表面积尺度)
- `L = scale.L` (特征长度，通常是 1e-6 m，但实际用电极总厚度)
- `A = cell.area` (受流面积，单位 m²)

### 1.2 容量计算

从时间尺度可以推导出容量：

```
ts = F × cs_max × A × L / I_typ

对于 1C 放电：I = I1C，放电时间 t = Q/I1C

如果 t = ts，则：
Q = I1C × ts = I1C × (F × cs_max × A × L / I1C)
  = F × cs_max × A × L

转换为 Ah：
Q [Ah] = F × cs_max × A × L / 3600
```

---

## 2. Jellyroll 参数的实际计算

### 2.1 输入参数（Jellyroll.jl）

```julia
# 电极厚度
PE.thickness = 7.56e-5 m
NE.thickness = 8.52e-5 m
SP.thickness = 1.2e-5 m
L_total = 1.728e-4 m  (总厚度)

# 最大浓度
PE.cs_max = 63104 mol/m³
NE.cs_max = 33133 mol/m³

# 电池参数
cell.I1C = 5 A
cell.capacity = 5 Ah
cell.area = 48.95 m²  (!)
cell.length = 938.77 m
cell.width = 0.05213 m

# Faraday 常数
F = 96485.33 C/mol
```

### 2.2 归一化尺度计算

```
scale.L = PE.thickness + NE.thickness + SP.thickness
        = 7.56e-5 + 8.52e-5 + 1.2e-5
        = 1.728e-4 m

scale.I_typ = cell.I1C = 5 A

scale.a0 = 1e6 m⁻¹

scale.j = I_typ / (a0 × L × A)
        = 5 / (1e6 × 1.728e-4 × 48.95)
        = 5 / 8458.56
        = 5.912e-4 A/m
```

### 2.3 容量计算

```
Q_p = F × cs_max_p × A × L / 3600
    = 96485 × 63104 × 48.95 × 1.728e-4 / 3600
    = 96485 × 63104 × 8.459e-3 / 3600
    = 14.58 Ah

Q_n = F × cs_max_n × A × L / 3600
    = 96485 × 33133 × 48.95 × 1.728e-4 / 3600
    = 7.66 Ah

Q_限制 = min(Q_p, Q_n) = 7.66 Ah
```

**⚠️ 关键发现：计算容量（7.66 Ah）≠ 设定容量（5 Ah）**

比值：7.66 / 5 = **1.53 倍**

### 2.4 时间尺度计算

```
ts_p = F × cs_max_p × A × L / I_typ
     = 96485 × 63104 × 48.95 × 1.728e-4 / 5
     = 10527 s = 2.92 h

ts_n = F × cs_max_n × A × L / I_typ
     = 96485 × 33133 × 48.95 × 1.728e-4 / 5
     = 5527 s = 1.54 h

理论 1C 放电时间 = Q_限制 / I1C
                 = 7.66 / 5
                 = 1.53 h
```

---

## 3. 对比 LG M50

### 3.1 LG M50 参数

```julia
# 电极厚度（LGM50.jl）
PE.thickness = 75.6e-6 m
NE.thickness = 85.2e-6 m
SP.thickness = 12e-6 m
L_total = 1.728e-4 m  (相同！)

# 最大浓度
PE.cs_max = 63104 mol/m³  (相同！)
NE.cs_max = 33133 mol/m³  (相同！)

# 电池参数
cell.I1C = 5 A
cell.capacity = 5 Ah
cell.area = 0.1027 m²  (!)
cell.length = 1.58 m
cell.width = 0.065 m
```

### 3.2 LG M50 容量计算

```
Q_p = F × cs_max_p × A × L / 3600
    = 96485 × 63104 × 0.1027 × 1.728e-4 / 3600
    = 0.0306 Ah  ⚠️ 远小于 5 Ah！

Q_n = 0.0161 Ah

Q_限制 = 0.0161 Ah  ⚠️ 只有设定值的 0.3%！
```

**这不对！说明我的理解有误。**

---

## 4. 重新审视归一化

### 4.1 问题所在

上面的容量公式：
```
Q = F × cs_max × A × L / 3600
```

这个公式的物理意义是什么？

让我重新推导：

```
电池总的锂离子数 = cs × V

其中 V 是活性材料体积：
V = A × L_electrode

对于正极：
V_p = A × L_p

总的锂容量（负极锂化）：
Q = (cs_max - cs_min) × V × F / 3600

如果 Δcs = cs_max (完全利用)：
Q = cs_max × A × L_p × F / 3600
```

**但这里的 L_p 应该是正极厚度，而不是总厚度 L！**

### 4.2 修正的容量公式

```
Q_p = cs_max_p × (PE.eps_s × A × PE.thickness) × F / 3600
Q_n = cs_max_n × (NE.eps_s × A × NE.thickness) × F / 3600
```

其中 `eps_s` 是固相体积分数。

对于 Jellyroll：
```
PE.eps_s = 1 - PE.eps - PE.eps_fi
         = 1 - 0.335 - 0.025
         = 0.64

NE.eps_s = 1 - NE.eps - NE.eps_fi
         = 1 - 0.25 - 0.0326
         = 0.7174

Q_p = 63104 × 0.64 × 48.95 × 7.56e-5 × 96485 / 3600
    = 63104 × 0.64 × 3.571e-3 × 96485 / 3600
    = 3.94 Ah

Q_n = 33133 × 0.7174 × 48.95 × 8.52e-5 × 96485 / 3600
    = 2.72 Ah

Q_限制 = 2.72 Ah
```

仍然小于 5 Ah，但更接近了。

---

## 5. 查看代码中的实际容量计算

让我查看 JuBat 是如何计算 SOC 和容量的。

从 `scale.ts_p` 的定义：
```julia
scale.ts_p = F × cs_max × A × L / I_typ
```

这里的 `L` 是 `scale.L`，而 `scale.L` 在第 236 行定义为：
```julia
scale.L = PE.thickness + NE.thickness + SP.thickness
```

**这是总厚度，不是单个电极厚度！**

这意味着 `ts_p` 不是真实的容量时间尺度，而是一个归一化尺度因子。

---

## 6. 重新理解 `cell.area` 的含义

### 6.1 在 1D 电化学模型中

1D 伪二维模型（P2D/SPMe）假设：
- 电流垂直于电极平面流动
- 沿电极厚度方向求解
- 忽略横向（平面内）变化

在这种情况下，`cell.area` 是：
- **电极的横截面积**
- 电流流过的面积
- 用于定义总电流：`I_total = j_avg × A`

### 6.2 对于方形电池（LG M50）

```
cell.area = length × width × no_layers
          = 1.58 × 0.065 × 1
          = 0.1027 m²
```

这是单层电极的面积。

### 6.3 对于圆柱形 Jellyroll

Jellyroll 是螺旋卷绕结构，关键问题是：
**应该用单层面积还是总展开面积？**

#### 选项 A：单层等效面积

```
单层周长 = 2πR_avg ≈ 0.113 m
单层面积 = 周长 × 高度 = 0.113 × 0.052 = 0.0059 m²
```

物理意义：
- 1D 模型把多层螺旋等效为单层
- 电流从内到外流动，受流面积是单层面积
- 总电流 = j × A_单层

#### 选项 B：总展开面积（当前设置）

```
总展开长度 = 938.77 m
总展开面积 = 938.77 × 0.052 = 48.95 m²
```

物理意义：
- 考虑所有层的总面积
- 但在 1D 模型中，电流是串联流过各层的
- 使用总面积会导致电流密度过低

### 6.4 电流流动方式

对于 Jellyroll：
- **径向电流**：从中心流向外围（或反之）
- 电流必须**串联**通过所有层
- 因此受流面积应该是**单层面积**，而非总展开面积

类比：
```
串联电阻：
R_total = R1 + R2 + ... + Rn

并联电阻：
1/R_total = 1/R1 + 1/R2 + ... + 1/Rn

Jellyroll 的层是串联的（电流必须通过所有层）
→ 应该用单层面积
```

---

## 7. 结论

### 7.1 参数设置问题

**当前 Jellyroll.jl 的 `cell.area = 48.95 m²` 是不正确的。**

原因：
1. 使用了螺旋展开后的总长度（938.77 m）
2. 但在 1D 电化学模型中，电流是串联通过各层的
3. 应该使用单层等效面积

### 7.2 正确的定义

```julia
# 方法 1: 基于平均半径
R_avg = (cell.Rout + cell.Rin) / 2
cell.length = 2 * π * R_avg  # 单层周长
cell.area = cell.length * cell.width  # 单层面积

# 方法 2: 基于容量反推
Q_target = 5 Ah
A_required = Q_target × 3600 / (F × cs_max_limiting × L_limiting × eps_s_limiting)

# 方法 3: 使用内半径（保守）
cell.length = 2 * π * cell.Rin
cell.area = cell.length * cell.width
```

### 7.3 容量不匹配问题

即使修正了 `cell.area`，计算容量仍可能与设定值不匹配。

可能原因：
1. `cell.capacity = 5 Ah` 只是一个标签，不参与计算
2. 实际容量由电极参数决定：`cs_max, thickness, eps_s, area`
3. Jellyroll 参数文件中的电极参数可能不对应 5 Ah 容量

### 7.4 归一化的一致性

关键要求：
```
理论容量计算 ≈ 设定容量

Q_calc = F × Δcs × eps_s × A × L_electrode / 3600 ≈ cell.capacity
```

如果不匹配，需要调整参数。

---

## 8. 推荐修正方案

### 方案 A：修正面积（最简单）

```julia
# 修改 Jellyroll.jl
R_avg = (cell.Rout + cell.Rin) / 2  # ≈ 0.018 m
cell.length = 2 * π * R_avg          # ≈ 0.113 m (单层周长)
cell.area = cell.width * cell.length # ≈ 0.0059 m² (单层面积)
```

预期结果：
- 计算容量 ≈ 0.27 Ah (仍不匹配 5 Ah)
- 但电流密度会正常 (50 A/m²)
- 1C 放电会在合理时间内完成

### 方案 B：调整电极参数

如果要达到 5 Ah 容量：

```
Q_target = 5 Ah
A_单层 = 0.0059 m²
L_p = 7.56e-5 m
eps_s_p = 0.64
cs_max_p = 63104 mol/m³

需要的等效面积:
A_eff = Q_target × 3600 / (F × cs_max × eps_s × L_p)
      = 5 × 3600 / (96485 × 63104 × 0.64 × 7.56e-5)
      = 6.16 m²

这比单层面积大 6.16 / 0.0059 ≈ 1044 倍！
```

这意味着：
- 要么需要 1044 层并联
- 要么 Jellyroll 的容量标称值 5 Ah 不正确
- 要么电极参数（cs_max, thickness）不正确

### 方案 C：重新校准所有参数

基于实际 21700 圆柱电池（典型 5 Ah）：
1. 查找真实的电极参数
2. 根据几何尺寸计算合理的面积
3. 验证容量计算与标称容量匹配

---

## 9. 下一步行动

1. **确认 Jellyroll 的真实电池规格**
   - 21700? 18650? 26650?
   - 标称容量：5 Ah
   - 几何尺寸：Rout=0.0325 m, Rin=0.00192 m, H=0.052 m

2. **检查电极参数来源**
   - cs_max, thickness, eps_s 是否对应这个容量的电池？
   - 可能需要重新校准

3. **建议临时修正**
   ```julia
   # 使用单层面积 + 缩放因子
   R_avg = (cell.Rout + cell.Rin) / 2
   A_single = 2 * π * R_avg * cell.width
   
   # 计算需要的缩放因子以匹配 5 Ah
   capacity_calc = F * cs_max_n * eps_s_n * A_single * NE.thickness / 3600
   scale_factor = cell.capacity / capacity_calc
   
   cell.area = A_single * scale_factor
   ```

---

## 10. 附录：公式汇总

### 电化学归一化

```
scale.L = PE.thickness + NE.thickness + SP.thickness
scale.j = I_typ / (a0 × L × A)
scale.ts = F × cs_max × A × L / I_typ
scale.phi = R × T_ref / F
scale.sig = L × I_typ / (phi × A)
```

### 容量计算

```
Q [Ah] = F × Δcs × eps_s × A × L_electrode / 3600

对于完全利用（Δcs = cs_max）：
Q = F × cs_max × eps_s × A × L_electrode / 3600
```

### 电流密度

```
j [A/m²] = I / A
j_app [A/m³] = I / (A × L_electrode)  # 单位体积电流
```

### 时间尺度

```
t_capacity [s] = Q × 3600 / I = (F × cs_max × eps_s × A × L) / I
t_diffusion [s] = L²/Ds
t_thermal [s] = ρ × C × L² / λ
```

---

**总结**: 从归一化角度看，`cell.area = 48.95 m²` 确实导致了：
1. 计算容量远大于标称容量（7.66 Ah vs 5 Ah）
2. 电流密度过低（0.0006 A/m vs 正常的 50 A/m）
3. 放电时间远超预期（1.53 h 仍不够完全放电）

**根本原因**：在 1D 串联模型中，应该使用单层等效面积，而不是螺旋展开的总面积。
