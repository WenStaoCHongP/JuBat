# 符号约定完整验证

## 📐 理论推导

### 物理方程
```
ρc ∂T/∂t = ∇·(k∇T) + q
```

### 弱形式（对测试函数 φ）
```
∫ φ ρc ∂T/∂t dΩ = ∫ φ ∇·(k∇T) dΩ + ∫ φ q dΩ
```

### 分部积分
```
∫ φ ∇·(k∇T) dΩ = -∫ ∇φ·k∇T dΩ + ∫_∂Ω φ k∂T/∂n dS
```

### 对流边界条件
```
-k ∂T/∂n = h(T - T_amb)
⇒ k ∂T/∂n = -h(T - T_amb) = -hT + hT_amb
```

### 代入边界积分
```
∫_∂Ω φ k∂T/∂n dS = ∫_∂Ω φ(-hT + hT_amb) dS
                   = -∫_∂Ω hφT dS + ∫_∂Ω hφT_amb dS
```

### 完整弱形式
```
∫ φ ρc ∂T/∂t dΩ = -∫ ∇φ·k∇T dΩ - ∫_∂Ω hφT dS + ∫ φ q dΩ + ∫_∂Ω hφT_amb dS
```

### 离散形式
```
M dT/dt = -K^vol T - K^bc T + F^vol + F^bc
        = -(K^vol + K^bc) T + F
```

其中（都是**正定矩阵**）：
- `K^vol = ∫ ∇N·k∇N dΩ` （体积扩散）
- `K^bc = ∫_∂Ω hNN dS` （边界对流）
- `F = ∫ Nq dΩ + ∫_∂Ω hN T_amb dS` （热源+边界）

## ✅ 代码验证

### 1. 体积扩散项（各向异性，第162-164行）
```julia
cxx = -Kxx .* wJ  // -(∫∇N·k∇N) ✓
cxy = -Kxy .* wJ  // -(∫∇N·k∇N) ✓
cyy = -Kyy .* wJ  // -(∫∇N·k∇N) ✓
```
**符号：** `-` （正确）✓

### 2. 体积扩散项（各向同性，第182-183行）
```julia
KT_x = Assemble(..., (-λ_iso_nd .* wJ), ...)  // -(∫∇N·k∇N) ✓
KT_y = Assemble(..., (-λ_iso_nd .* wJ), ...)  // -(∫∇N·k∇N) ✓
```
**符号：** `-` （正确）✓

### 3. 边界对流刚度项（第276-278行）
```julia
ke11 += -wt * (N1*N1)  // -(∫hNN) ✓
ke12 += -wt * (N1*N2)  // -(∫hNN) ✓
ke22 += -wt * (N2*N2)  // -(∫hNN) ✓
```
**符号：** `-` （正确）✓

### 4. 边界对流载荷项（第279-280行）
```julia
fe1 += wt * (T_amb * N1)  // +(∫hN T_amb) ✓
fe2 += wt * (T_amb * N2)  // +(∫hN T_amb) ✓
```
**符号：** `+` （正确）✓

### 5. 体积热源载荷项（第207行）
```julia
FT .+= Assemble1D(Vi, Ni, coeff_f, nnode)  // +(∫Nq) ✓
```
**符号：** `+` （正确）✓

## ✅ 与电化学约定的一致性

### 电化学约定
查看 `ElectrodeDiffusion.jl`：
```julia
coeff = - Ds_eff .* ...  // 已包含负号
K = Assemble(..., coeff, ...)
```
**约定：** `M du/dt = K u + F` （K包含负号）

### 热模型约定（修复后）
```julia
KT = -(K^vol + K^bc)  // KT包含负号
FT = F^vol + F^bc      // FT是正的
```
**约定：** `M dT/dt = KT T + FT` （KT包含负号）

**结论：** 约定**完全一致** ✓

## 🧪 物理意义验证

### 温度边界行为

**边界节点方程：**
```
M_ii dT_i/dt = -K^vol_ij T_j - K^bc_ii T_i + F^bc_i
             = -K^vol_ij T_j - h∫N_i^2 dS · T_i + h∫N_i dS · T_amb
```

**稳态（dT/dt = 0）：**
```
h∫N_i^2 dS · T_i = -K^vol_ij T_j + h∫N_i dS · T_amb
```

**如果内部温度 = T_amb：**
```
h∫N_i^2 · T_i = h∫N_i · T_amb
⇒ T_i = T_amb  ✓ （符合物理）
```

**如果内部有热源（T_j > T_amb）：**
```
-K^vol_ij T_j < 0 （内部向边界传热）
⇒ T_i 会略高于 T_amb  ✓ （符合物理）
```

### 温度最小值验证

**物理要求：** 无冷源时，T_min ≥ min(T_initial, T_amb)

**修复前：**
- 边界符号错误 → 边界节点温度异常
- T_min = 0.40 < T_amb = 1.0 ❌

**修复后（预期）：**
- 所有符号正确 → 物理行为正确
- T_min ≥ T_amb = 1.0 ✓

## 📝 总结

| 项目 | 符号 | 验证结果 |
|------|------|---------|
| 体积扩散（各向异性） | `-` | ✓ 正确 |
| 体积扩散（各向同性） | `-` | ✓ 正确 |
| 边界对流（刚度） | `-` | ✓ 正确 |
| 边界对流（载荷） | `+` | ✓ 正确 |
| 体积热源（载荷） | `+` | ✓ 正确 |
| 与电化学一致性 | - | ✓ 正确 |
| 物理意义 | - | ✓ 正确 |

**结论：** 所有符号约定**完全正确**！✅

符号约定总结：
- **KT = -(K^vol + K^bc)**（所有刚度项都包含负号）
- **FT = F^vol + F^bc**（所有载荷项都是正的）
- **方程形式：M dT/dt = KT T + FT**（与电化学统一）
