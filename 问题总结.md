# 问题总结：testexample.jl电压降过小和温升不明显

## 问题现象

| 模型 | 电压曲线 | 温升 | 状态 |
|------|---------|------|------|
| Thermal.jl（集总模型） | 4.13V → 2.5V | 正常 | ✓ 正常 |
| testexample.jl（多SPMe） | 电压降仅0.001V | 不明显 | ✗ 异常 |

## 根本原因

### ⚠️ 架构选择错误（主要原因）

**testexample.jl使用了多SPMe模式：**
```julia
opt.per_element_spme = true  # 80个独立SPMe
nθ = 80  # 80个周向热单元
```

**问题：多SPMe将热网格的80个单元当作80个并联的电化学单元**

```
错误的架构：
┌─────────────────────────────────────┐
│  热单元1 (SPMe1) ──┐                │
│  热单元2 (SPMe2) ──┤                │
│         ...        ├── 共享电压V    │  总电压 = 单元电压
│  热单元80(SPMe80)──┘                │
└─────────────────────────────────────┘
   每个单元分到电流 ≈ 5A/80 = 0.0625A
   电压降 ≈ 0.0625A × R ≈ 0.001V ✗

正确的物理模型：
┌─────────────────────────────────────┐
│  单个SPMe（电化学）                  │
│  ↓                                  │
│  热源均匀分配到80个热单元（温度场）  │
└─────────────────────────────────────┘
   总电流 = 5A
   电压降 ≈ 5A × R ≈ 1.5V ✓
```

**物理意义：**
- Jellyroll电池是**单层螺旋卷绕结构**，不是80层并联
- 电流在整个电池中均匀流动，不是分成80份
- 多SPMe模式设计用于真正的多单元并联系统（如电池包），不适合单个电池

## 次要原因

### 1. 参数问题

#### A. 正极电导率过低
```julia
PE.sig = 0.18 S/m  // Jellyroll.jl 第26行
```
- 正常值：10-100 S/m
- 影响：增大欧姆热，但不是主要问题

#### B. 缺少熵变系数
```julia
PE.dUdT = x-> 0 * x  // 第36行
NE.dUdT = x-> 0 * x  // 第64行
```
- 导致可逆热源为0
- 影响：减少总热源10-30%
- 注：LG M50参数也是0，所以不是特殊问题

## 解决方案

### ✅ 推荐：使用简化耦合模式

修改 `testexample.jl`：
```julia
# 修改前
opt.per_element_spme = true

# 修改后  
opt.simple_thermal_coupling = true
opt.per_element_spme = false
```

**验证：`testexample_simple_coupling.jl` 已经使用此模式，结果正常！**

**优点：**
- ✓ 物理意义正确（单SPMe + 2D温度场）
- ✓ 电压曲线正常
- ✓ 温升正常  
- ✓ 内存占用低（3GB vs 30GB）
- ✓ 计算速度快（快5-10倍）

**缺点：**
- 假设电流均匀分布（但对Jellyroll这是合理的）

### 辅助：改进参数（可选）

```julia
# src/parameters/Jellyroll.jl

# 1. 提高正极电导率
PE.sig = 10.0  # 从0.18改为10.0

# 2. 添加熵变系数（参考Northrop参数）
PE.dUdT = x-> -0.001 * (0.199521039 - 0.928373822*x + 
                        1.364550689*x.^2 - 0.6115448939*x.^3) ./
              (1 - 5.661479886*x + 11.47636191*x.^2 - 
               9.82431213*x.^3 + 3.048755063*x.^4)

NE.dUdT = x-> 0.001 * (0.005269056 + 3.299265709*x - 
                       91.79325798*x.^2 + 1004.911008*x.^3 - 
                       5812.278127*x.^4 + 19329.7549*x.^5 - 
                       37147.8947*x.^6 + 38379.18127*x.^7 - 
                       16515.05308*x.^8) ./
              (1 - 48.09287227*x + 1017.234804*x.^2 - 
               10481.80419*x.^3 + 59431.3*x.^4 - 
               195881.6488*x.^5 + 374577.3152*x.^6 - 
               385821.1607*x.^7 + 165705.8597*x.^8)
```

## 关键代码位置

| 文件 | 行数 | 内容 |
|------|------|------|
| `example/testexample.jl` | 65 | `opt.per_element_spme = true` ← 改为false |
| `example/testexample.jl` | 69 | 添加 `opt.simple_thermal_coupling = true` |
| `src/parameters/Jellyroll.jl` | 26 | `PE.sig = 0.18` ← 改为10.0 |
| `src/parameters/Jellyroll.jl` | 36, 64 | `dUdT = x-> 0*x` ← 添加熵变函数 |

## 详细分析报告

完整技术分析请查看：**`分析报告_电压和温升异常.md`**

包含：
- 完整参数对比
- 尺度因子计算
- 热源公式单位分析
- 多SPMe架构详细剖析
- 物理意义解释
